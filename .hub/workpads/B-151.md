# Workpad — B-151

## Queue item

**B-151**: the printed `accept_suggestion()` call is ambiguous for a
measurement column with a code list.

The item file, `queue/items/B-151.yaml`, is the only home of its state and its
retirement condition, and this workpad restates neither. **This pull request
meets the metasalmon half of that condition, and cannot meet the rest.** The
condition also asks for the metasalmonpy fix, a metasalmonpy test, and the
removal of metasalmonpy's pin *in the same change*. The hub claim covers one
branch in metasalmon (`HUB.md`, the work-branch row of `writes.permitted`),
and the register allows one pull request per item, in the item's repository.
So the mirror half is prepared, tested and committed locally, and it is not
pushed. See *The mirror* below, and the first entry under *Found, not
absorbed*.

Nothing in the dispatch disagreed with `HUB.md`.

## What changed and where

### `R/review-console.R`

- **`.ms_review_match_slot_rows()`**: `code_value` now has three states rather
  than two. `NULL` leaves it unconstrained, as before. A value selects that
  code's slot, as before. **A blank value (`""`, or `NA`) now selects the slots
  with no code value.** Before, a blank value went through
  `!is.na(review$code_value) & review$code_value == ""`, which excluded every
  no-code row, so `code_value = ""` aborted with *"No review slot matches"*.
  The function now normalises its own argument with `.ms_scalar_text()`, so the
  NA-safety its comment claims holds even if a caller does not do that first.
- **`.ms_review_call_args()`**: when `(column, role, table)` still matches more
  than one slot, `code_value` is now added even for a row with no code value,
  as `code_value = ""`. It used to be added only when the row had one, which is
  the escalation gap the item names.
- **`.ms_review_resolve_slot()`, the ambiguity refusal**: a row with no code
  value that shares its table with code rows is now offered as
  `table = "t", code_value = ""`. The old option was bare `table = "t"`, the
  argument the call already carried, so following the message could not reach
  the column's own slot. A no-code row in a table with no code rows is still
  offered as bare `table = `, so the two-table case prints what it did.
- **roxygen for `code_value`**, and `man/accept_suggestion.Rd` regenerated:
  says what `""` and `NA` select and that an omitted value matches the codes'
  slots too.

**What did not change, on purpose: an omitted `code_value` is still
unconstrained.** The item offers two fixes, and the other is to read the
omission as "no code value". That fix would change what an already-printed
call resolves to. A code slot's call is printed without `code_value` whenever
`(column, role)` is unique at print time. That is the case for a column with a
single code, when the column has no slot of that role in the queue: a
categorical column never has one, and a measurement column's own slot leaves
the queue once filled. With that fix, the same pasted line would resolve to the
column's own slot if one is queued, and to nothing if not. The pasted call is
the audit trail, so this fix leaves the meaning of every earlier-printed call
alone. Earlier versions never printed `code_value = ""`, and the calls they did
print resolve exactly as they did.

**The printed spelling is `""`, not `NA`.** `code_value = NA` is accepted and
means the same thing. The printed form is `""` because it is the one literal
both implementations can print alike. Python's `None` is already the
unconstrained default, so a Python spelling of "absent" has to be `""`. If R
printed `NA`, the two consoles would print different calls, and that
difference would need a parity-register row.

### `tests/testthat/test-review-console.R`: one helper, four tests

- **`expect_printed_calls_decide_their_own_slots()`** (helper) runs every
  printed accept and reject line and checks it decides **the slot and rank it
  was printed under**. That is stricter than the file's existing load-bearing
  test, which checks that one decision was recorded and that its IRI is the
  candidate's. A call that resolves to a sibling slot passes that check and
  writes the wrong field. Each call is also checked to be a rendered line, so
  the text run is the text a user pastes.
- **`measurement_code_review()`** (fixture) builds the collision the way
  discovery builds it. A measurement column has its own `entity_iri` and
  `constraint_iri` slots. Two codes each have one `codes.csv` slot, carrying
  constraint, entity and method candidates.
- *"a measurement column with a code list prints a call that reaches its own
  slot"*: the column's call carries `code_value = ""`, and every printed call
  decides its own slot.
- *"a blank code_value selects the column's own slot, and an omitted one still
  matches every code"*: `""` and `NA` both select the column's slot, and the
  bare call still refuses. This pins the design choice above.
- *"doing what the ambiguity refusal says reaches every slot it matched"*: each
  option the refusal lists is added to the call and run, and together they
  must reach every slot the bare call matched. This reads the options from the
  condition's `body`, not from formatted text, so it does not depend on the
  bullet glyph or the locale.
- *"a measurement column with a code list round-trips from create_sdp() to
  disk"*: the real pipeline. It runs `create_sdp(semantic_code_scope = "all")`
  with a two-code sentinel list (`-9` *Not surveyed*, `-99` *Survey
  abandoned*) on a count column, with `find_terms` mocked. It checks the
  collision is real for `entity` and `constraint`, and that every printed call
  decides its own slot. Then it pastes the column's entity call and runs
  `apply_sdp_semantics()`. The chosen IRI must land in
  `column_dictionary.csv` `entity_iri`, and `codes.csv` must be unchanged.

### `NEWS.md`

One entry under *metasalmon (development version)*, *Fixed*. It covers the
defect, the fix, why an omitted `code_value` keeps its meaning, and the mirror
status.

### `knowledge/parity-deviations.md` and `knowledge/roadmap.md`: the owed port

Both files get a paragraph headed *"The development version after 0.5.0 adds to
what the port owes (2026-09-24)"*. The roadmap's own rule is that a new mirror
debt is recorded in both places in the same change. The shape follows B-176's
paragraphs, which sit just above. **No numbered register row is added or
amended.** The difference this closes, R aborting on `code_value = ""` where
Python selected the no-code slot, was in neither register. The prose names
the Python functions, the pin to delete, the tests to mirror, and why the
spelling is `""`. It cites no queue id that does not exist on `main`, so the
mirror item is "to be filed", as B-176's is.

## Evidence

### The item's measurement, reproduced through the real pipeline on `main` `7bfc3ca`

A scratch script ran `create_sdp()` with a mocked `find_terms`,
`seed_codes` for `spawner_count`, and `semantic_code_scope = "all"`. It then
evaluated every line `review_semantics()` printed. That option is needed: the
default scope, `"factor"`, gives a numeric column's codes no semantic targets.

```
column_role(spawner_count): measurement
slots for spawner_count: 5 column-level (variable, property, entity, unit,
  constraint) + 2 code slots (-9, -99), each carrying constraint/entity/method
printed calls: 33  failing: 6
  accept_suggestion(review, "spawner_count", "entity", rank = 1, table = "spawners")      ERROR
  accept_suggestion(review, "spawner_count", "entity", rank = 2, table = "spawners")      ERROR
  accept_suggestion(review, "spawner_count", "constraint", rank = 1, table = "spawners")  ERROR
  accept_suggestion(review, "spawner_count", "constraint", rank = 2, table = "spawners")  ERROR
  reject_suggestion(review, "spawner_count", "entity", table = "spawners")                ERROR
  reject_suggestion(review, "spawner_count", "constraint", table = "spawners")            ERROR
  each: That column and role match more than one review slot.
        i Add one of these arguments to say which:
        * table = "spawners"
        * table = "spawners", code_value = "-9"
        * table = "spawners", code_value = "-99"
```

Two things this adds to the item's own measurement. **`constraint` fails the
same way as `entity`**, because a measurement parent gives its codes both
roles. And **the `reject_suggestion()` lines fail too**.

The same script on this branch prints `code_value = ""` on those six lines,
and **33 of 33 run**, each deciding the slot it was printed under.

### The new tests: red on `main`, green here

The four tests were run against `main`'s `R/review-console.R`, with the new
test file in place:

```
── 1. Failure: a measurement column with a code list prints a call that reaches its own slot
   Expected `any(...)` to be TRUE.          (no `code_value = ""` in the printed line)
── 2. Error: ...prints a call that reaches its own slot
   accept_suggestion(review, "spawner_count", "entity", rank = 1, table = "spawners"):
   That column and role match more than one review slot.
── 3. Error: a blank code_value selects the column's own slot...
   accept_suggestion(review, "spawner_count", "entity", rank = 1, code_value = blank):
   No review slot matches that column and role.
── 4. Failure: doing what the ambiguity refusal says reaches every slot it matched
   Needs: NA
   Absent: "column_dictionary.csv|demo-1/spawners/spawner_count|entity_iri"
── 5. Error: a measurement column with a code list round-trips from create_sdp() to disk
   That column and role match more than one review slot.
```

On this branch the whole file passes (`test-review-console.R`, summary
reporter, no failures).

### Each change has a test that fails without it: three single-change mutants

Each mutant reverts one of the three code changes and keeps the other two.

| mutant | tests failing |
|---|---|
| matcher reverted to `!is.na() & ==` | 4 of 4: printed call, blank `code_value`, refusal options, round trip |
| call args back to "add `code_value` only when the row has one" | 2 of 4 (3 failures): the printed-call test, both its rendered-line and its run assertions, and the round trip |
| refusal back to bare `table = ` for a no-code row | 1 of 4: refusal options |

### Full suite and check

- `devtools::test()` at the fix: 903 tests, **0 failed, 2 errors, 7 skipped**,
  38 warnings, 4426 expectations passed. **Both errors are environmental and
  are B-152's**: `test-github-helpers.R:161` (*read_github_csv can read remote
  content…*) and `:264` (*read_github_csv_dir can fetch when…*). Each is an
  `HTTP 404 Not Found` from `httr2::req_perform()`, exactly the two lines that
  item's evidence names. #146's workpad measured the same two erroring
  identically on `main` in this container.
- `rcmdcheck(args = c("--no-manual", "--no-tests", "--ignore-vignettes"),
  build_args = "--no-build-vignettes")`: **Status OK, 0 errors, 0 warnings,
  0 notes**, including *checking R files for non-ASCII characters ... OK*.
  Tests ran separately above. Vignettes are skipped because two fail locally
  and CI's newer R does not run them (B-164), which is how B-176's check was
  run too. **This is R 4.3.3, not CI's R**, so per `AGENTS.md` the local check
  is evidence about this machine and CI is the check that counts.
- `git diff --check` is clean, and no non-ASCII byte was added to `R/`, `man/`
  or the test file.
- `python3 scripts/check-parity-registers.py` against the mirror tree gives
  *parity registers agree: 61 rows, 1-61 with no gaps*. `hub_queue.py lint`
  and `hub_queue.py check` are both OK.

The branch has two commits so that the failing-before can be rerun from
history. The first adds only the tests, with `R/` identical to `main`, and the
second is the fix. The red run above and the mutants swapped
`R/review-console.R` in place and restored it from a saved copy. That was
before the full suite started, so nothing else read the tree while it was
mutated.

### Commands

```sh
# every R command run as LANG=C.UTF-8 LC_ALL=C.UTF-8 (B-137: six tests need UTF-8)
Rscript <scratch>/repro.R                                   # before and after
Rscript -e 'pkgload::load_all(".", quiet = TRUE); testthat::test_file("tests/testthat/test-review-console.R", reporter = "summary")'
Rscript -e 'devtools::test(reporter = "summary", stop_on_failure = FALSE)'
Rscript -e 'rcmdcheck::rcmdcheck(".", args = c("--no-manual", "--no-tests", "--ignore-vignettes"), build_args = "--no-build-vignettes")'
git show origin/main:R/review-console.R > R/review-console.R   # red run and mutants; restored from a copy after
Rscript -e 'devtools::document()'   # then reverted DESCRIPTION and NAMESPACE: roxygen 8.1.0 here, 8.0.0 in the repo
git diff --check
```

## The mirror

**The metasalmonpy half is written, tested and committed, and it is not
pushed.** It is commit `6fc01bd` on the local branch `local/B-151-mirror-half`.
The worktree is `../hub-worktrees/salmon-data-mobilization-metasalmonpy-B-151/metasalmonpy`,
relative to this repository, and it is based on metasalmonpy `main` `3f8349a`.
The branch is named `local/…` on purpose, so it cannot pass for a work branch
this claim may push. Its changes:

- `review_console.py`: `_review_call_args()` drops its `if code_value:` guard,
  and `_resolve_slot()` offers `code_value=""` for a no-code row that shares
  its table with code rows. **`_match_slot_rows()` needed no code change**, only
  a docstring. It compares through `scalar_text()`, which maps a missing value
  to `""`. The `code_value` parameter docstring matches R's.
- `tests/test_review_console.py`: deletes
  `test_a_column_level_slot_sharing_a_role_with_its_codes_is_still_ambiguous`
  and adds the four R tests' mirrors, including the
  `create_sdp(semantic_code_scope="all")` round trip.
- `CHANGELOG.md`: an entry under `## Unreleased`.

Measured on that tree:

```
before (main 3f8349a):  tests/test_review_console.py  58 passed
after:                  tests/test_review_console.py  61 passed   (-1 pin, +4)
new tests against main's review_console.py: 3 failed, 1 passed
  the one that passes is the blank-code_value test, as expected: Python's matcher
  already read "" as "no code value", which is the parity claim below
full suite, core leg:    825 passed, 142 skipped, 190 subtests passed
full suite, extras leg:  966 passed, 1 skipped, 231 subtests passed
```

**Parity, measured rather than read.** On metasalmonpy `main` `3f8349a`, the
same fixture gives these results. `accept_suggestion(..., code_value="")`,
`pd.NA` and `NaN` each select the column's own slot. The printed call for that
slot raises the same ambiguity, and its refusal offers the same bare
`table="spawners"`. So R's matcher change moves R to behaviour Python already
had, and closes a difference neither register recorded. The printed-call and
refusal changes are the port.

## What I did not do, and why

- **Push the metasalmonpy half or open a pull request there.** The
  work-branch row covers `agent/<queue-id>/<token>` in the repository the
  item's `repo` field names, which is metasalmon. The draft row allows one pull
  request per handed-back item. Either write would be outside the register.
- **Narrow B-151's `retires_when`, or file the mirror item.** Both are queue
  changes that belong to whoever dispatches, not to the holder of this claim.
  They are described below as a candidate instead.
- **Change what an omitted `code_value` means.** See *What did not change, on
  purpose*.
- **Run metasalmonpy's `tests/test_roundtrip.py` against R.** CI's `roundtrip`
  job installs metasalmon from `main`, which does not carry this change yet.

## Found, not absorbed

1. **B-151's `retires_when` cannot be met inside the repository it names.**
   This is `queue/README.md` corollary 2, the B-161 shape. It asks for both
   implementations and the metasalmonpy pin removal "in the same change", while
   declaring `repo: metasalmon`. So when this pull request merges, the item's
   condition is still not true. **Candidate:** split it into a pair, the
   prescribed remedy. Narrow B-151 to the metasalmon half, which this pull
   request meets. File a metasalmonpy item for the mirror, which
   `6fc01bd` above already implements. Each item names the other. The split
   is worth making before B-151 is marked `done`; otherwise `done` records a
   retirement condition that is not true.
2. **Observation, not filed: a code slot's printed calls name one role for
   candidates of three.** Discovery gives a measurement parent's code the roles
   constraint, entity and method, all in one `codes.csv` slot. The console
   prints every candidate in that slot with the role of the slot's first row.
   So the entity candidate at rank 3 prints as
   `accept_suggestion(review, "spawner_count", "constraint", rank = 3, table = "spawners", code_value = "-9")`.
   It resolves and decides correctly, because the role is only an address and
   `rank` indexes the whole slot. The mocked run above shows it. I am not
   calling it a defect: nothing decides wrongly. It is recorded so that nobody
   rediscovers it and takes it for part of this fix.

## Guards added

None. No suppression, skip, exclusion or allowlist entry was added. The new
tests have no skip condition.

## Which list this pull request is in

**Delegated**: a defect fix with a reproduced failing-before and a test. I
checked each of `HUB.md`'s ten classes. These are the ones that needed a look:

- **Class 2 (public signatures, attributes, frozen contracts):** no signature,
  return value, attribute or frozen column changes. There are two behaviour
  changes. `accept_suggestion()` and `reject_suggestion()` now read a blank
  `code_value` as "no code value". Before, that call aborted on every review
  the package builds, because discovery and `.ms_read_metadata_csv()` both
  leave a no-code row's `code_value` as `NA`. And
  `review_semantics()` prints `code_value = ""` on exactly the lines that
  could not run before.
- **The user-visible question the dispatch raised:** this is a correctness fix,
  not a user-experience choice. The printed text changes only on lines that
  aborted when pasted, and no line that ran before prints differently. Two
  choices were made, and both are settled from the repository rather than by
  taste. Keeping an omitted `code_value` unconstrained keeps the meaning of
  every earlier-printed call. `""` over `NA` is the spelling that needs no
  parity row.
- **Class 5 (parity register):** no numbered row is added, removed or amended.
  The owed-port paragraphs follow the B-176 precedent and record a port, not a
  deliberate difference.
- **Class 6 (weakening a guard or test):** nothing on this branch is weakened.
  The Python pin is removed in the local mirror commit, not here, and that
  removal replaces the pin with tests of the fixed behaviour.
- **Class 1 (ontology IRIs):** none chosen. The IRIs in the new tests are
  fixtures under `example.org`, plus the `smn` fixture IRI this test file
  already used.
- **Class 10:** nothing in this pull request is unsettled. The queue split
  above is a queue change for whoever dispatches, not a judgement inside this
  diff.
