# Workpad — B-200

## Queue item

**B-200**: no check that a `NEWS.md` entry under a released heading was in the
tree the tag names.

The item file, `queue/items/B-200.yaml`, is the only home of its state and its
retirement condition (`retires_when`), and this workpad restates neither. Its
evidence is the `B-200` entry in `knowledge/backlog.md`. Its pair is `B-201`,
the metasalmonpy half.

## What changed and where

- **`scripts/check-changelog-window.py`**, new, beside
  `check-parity-registers.py`. Standard library only. For every released
  heading in `NEWS.md` it finds the version's bump commit, diffs the section
  as it stood there against the section now, and asks `git blame` which
  commit added each added line and `git merge-base --is-ancestor` whether
  that commit is an ancestor of the bump. Its docstring states the exemption,
  what it does not cover, its exit codes (0, 1, and 2 when it cannot run —
  including a shallow clone), and its retirement condition. A `metasalmonpy`
  profile (`CHANGELOG.md`, `## Unreleased`, `pyproject.toml`, reached through
  `METASALMONPY_PATH`) exists so that B-201 can reuse the script.
- **`scripts/tests/test_check_changelog_window.py`**, new: 25 tests, each
  building a throwaway repository and running the script as a subprocess. The
  RED demonstration the item names is `TestTheWindow` and `TestTheExemption`.
- **`.github/workflows/changelog-window.yaml`**, new: on every pull request and
  on pushes to `main`, no path filter, a full-history checkout, the tests, then
  the check. No existing workflow changed.
- **`AGENTS.md`**, *Releases*: the closing sentence that pointed at this item
  now names the script as the pre-tag step, where to run it, and the two
  marked forms a correction must take.

No `NEWS.md` entry: nothing in the package changed (`scripts/`, `.github/`
and `.hub/` are all in `.Rbuildignore`), and `check-parity-registers.py`
landed without one either.

## Where the retirement condition is met, part by part

| part of `retires_when` | evidence |
|---|---|
| the script, beside `check-parity-registers.py`, with that definition of the bump commit | `scripts/check-changelog-window.py`; one deviation, below |
| runs in CI on every pull request | `changelog-window.yaml`; first run read on the pull request, below |
| named as the pre-tag step in *Releases* | `AGENTS.md` |
| passes over `main` with the exemption and not without | below: exit 0 with it, exit 1 without it on exactly the three 0.4.0 hunks the item measured |
| RED fixture, and the same bullet green under the development heading | `test_bullet_added_under_released_heading_after_the_bump_is_red`, `test_same_bullet_under_the_development_heading_is_green`; hand-run below |
| an unmarked paragraph under a released heading is still a finding | `test_unmarked_paragraph_under_a_released_heading_is_red` |
| the docstring states what it does not cover | the script's *WHAT IT DOES NOT COVER*, all three limits the item names, plus the ones found while building it |

## One deviation from the written condition, and why

**The first-commit fallback, read literally for every untagged version, is red
on `main`.** The item measured only `v0.4.0`. Measured here over every released
heading at `origin/main` `372ef07`: 49 lines under two untagged headings were
added by commits that are not ancestors of the first commit whose `DESCRIPTION`
reads the version.

- **0.2.4**: bump `e739f91` (#15); lines from `041d5dd` (reached `main` in #16,
  `fbda99b`) and `8930467` ("docs: add the 0.2.4 NEWS entries…", #17,
  `fa94968`), both merged while `DESCRIPTION` still read 0.2.4.
- **0.1.6**: bump `f774673` (#5); lines from `e00bba9`, `b546417` and `0e6a142`,
  all merged in #7 (`5dfe5c9`) while `DESCRIPTION` still read 0.1.6.

Neither version was ever tagged, and `AGENTS.md` says the 0.2.x ones are
deliberately untagged history, not to be backfilled. Before the rule existed,
entries accumulated under a version's heading while it stood.

**Resolution:** tags and the open window are exactly as the item specifies. An
untagged version that a later version has superseded is measured as it stood at
the *last* first-parent commit whose `DESCRIPTION` read it. What it gained while
it stood passes. A line added after it was superseded is still a finding, so no
historical heading goes unchecked against new additions. While a version is
current, the first commit rules, so CI sees such a line when it is written. The
docstring states this, what it forgives, and what would retire it.

**Alternatives considered:** leaving superseded untagged headings unchecked
(simpler, but a line added today under 0.2.3 would pass), or an allowlist of the
five commits (literal elsewhere, but it hard-codes history, and a later typo fix
to one of those lines would turn red).

## Commands and results

Environment: Python 3.11.15 locally, plus `python3.12` (3.12.3), which is the
version CI pins. git 2.43.0. The primary checkout was a shallow clone, so it was
unshallowed with `git fetch --unshallow --tags origin`: 771 commits on
`origin/main`. No R code changed, so no local R run is evidence here. CI's
`R-CMD-check` is read on the pull request.

**RED, before the script existed** (commit `eb03c9e`):

    $ python3 -m pytest scripts/tests/test_check_changelog_window.py -q
    20 failed in 1.66s

The first run showed 18 failed and 2 passed. The two exit-2 tests passed with no
script, because Python itself exits 2 when it cannot open a file. `assertExit`
now also requires the script's own "cannot run" message, which is why the RED
commit reads 20 of 20.

**After:**

    $ python3 -m pytest scripts/tests/test_check_changelog_window.py -q
    25 passed in 4.50s
    $ python3.12 scripts/tests/test_check_changelog_window.py
    Ran 25 tests in 4.900s
    OK

**Over `main` (`origin/main` = `372ef07`), with the exemption:** exit 0.

    changelog window: NEWS.md at 372ef07, 16 released headings checked
      0.5.0    af84689  tag v0.5.0
      0.4.0    4e2bbb6  tag v0.4.0; 9 added lines exempt as marked corrections
      ...
    not checked, no bump commit on this history (27): 0.1.2 0.1.1 0.0.27 ... 0.0.1
    no findings

**Without it** (`--no-exemption`): exit 1, on exactly the item's three hunks.

    NEWS.md:1482-1485 under "metasalmon 0.4.0", added by 0909953, ... (tag v0.4.0)
    NEWS.md:1620 under "metasalmon 0.4.0", added by 5a90a7f, ... (tag v0.4.0)
    NEWS.md:1665-1668 under "metasalmon 0.4.0", added by 0909953, ... (tag v0.4.0)

The item's line numbers were measured at `e9d7dc4`; the file has grown above
them since.

**A hand-built fixture:** bump to 0.1.0, then a bullet under it. Exit 1, "1 line
under a released heading missed that release". The same bullet under the
development heading gives exit 0, "no findings".

**A reordering is not an addition.** Three shipped bullets with the third
moved to the top was a finding until `added_lines()` learned to treat an
inserted line as a move when the diff removed a line of exactly its text from
the same section. `test_reordering_shipped_bullets_is_not_a_finding` was red
before that. `test_a_line_moved_in_from_another_heading_is_red` keeps a line
moved under a release from any other heading a finding. `git blame -M` was
measured and rejected on the same fixtures. It still credits a short reordered
bullet to the reorder commit, because by default it follows only moves of at
least 20 alphanumeric characters. And it credits a long line moved across
headings to its original commit.

**Three design choices, each shown RED against its alternative** (scratch
copies of the script with one line changed, run against this test file):

- The literal first-commit fallback everywhere:
  `test_an_untagged_version_superseded_is_measured_where_it_stood` fails, and
  `main` gives exit 1 with 49 lines.
- Pairing a replaced run by difflib's `ratio` instead of characters kept:
  `test_the_edited_line_stands_for_its_original_even_beside_a_short_one` fails.
  The ratio scores `NEWS.md`'s own closing line `"  how widely it already
  held.)*"` above the edited line at 1664 (0.554 against 0.541), and so reports
  the edited line as the one added.
- No tag requirement for the versions AGENTS.md says are tagged:
  `test_a_superseded_release_the_policy_tags_needs_its_tag` fails. The hazard
  was measured on a `--no-tags` clone of `main` at `372ef07`. There, 0.4.0 read
  as untagged history, was measured where it last stood, and passed even with
  `--no-exemption` (exit 0). With the rule, that clone exits 2: "no tag for
  v0.4.0, v0.3.0 in this clone, though every release from 0.3.0 forward is
  tagged".

**`git blame` and a configured ignore list:** with `blame.ignoreRevsFile` naming
`0909953`, plain blame attributed none of its nine `NEWS.md` lines to it.
`--ignore-revs-file=` restores all nine, and the script passes it.

`python3 scripts/hub_queue.py lint` and `check`: OK. `git diff --check`: clean.

## CI on the pull request

The pull request is #173. The new job's first run was on head `ecdd5cf`, merge
ref `9921c43`, with Python 3.12.14 and git 2.55.0. It printed `Ran 23 tests ...
OK`, then `changelog window: NEWS.md at 9921c43, 16 released headings checked`.
It found all five tags, `tag v0.5.0` through `tag v0.1.7`, reported `0.4.0 ...
9 added lines exempt as marked corrections`, and ended `no findings`. The job
took 10 seconds.

On `1a74ece` all five checks passed:

- `released-headings-hold-only-what-shipped`
- `queue-is-valid`, whose `pytest scripts/tests` also collected these tests on
  its shallow checkout
- `registers-agree`
- `reference-index`
- `check`: R 4.6.1, where this machine has 4.3.3. It reported 0 errors,
  0 warnings and 2 notes, both about the metasalmonpy sibling checkout (see
  below).

## What I did not do, and why

- **The metasalmonpy replay** belongs to B-201 and is not part of this
  condition. I ran it once, read-only, to hand B-201 what it will meet; see
  below.
- **The 27 headings older than the history** (0.0.1–0.1.2) are listed as not
  checked. No commit's `DESCRIPTION` reads them, so they have no bump commit.
- **No existing workflow changed.** `hub-queue.yml` also collects the new test
  file, through its `pytest scripts/tests` step, when its path filter matches.

## Found, belonging to other items

- **B-201.** I replayed the script, `--profile metasalmonpy`, against
  a local metasalmonpy checkout:
  - At `1e9245c` it finds B-144's lines, `CHANGELOG.md:257-296` under
    `## 0.5.0`. Blame names `773f5d4`, the branch commit that `b939fd9` merged,
    not the merge itself.
  - It also finds a **second** instance, 24 lines under the **tagged**
    `## 0.2.1`, from `10d0616` (2026-08-18). That commit is contained in
    `v0.4.0` and `v0.5.0` but not in `v0.2.1`.
  - On metasalmonpy `main` `012d04b` that second finding remains, exit 1.

  B-201's card says the check reports exactly one finding at `1e9245c` and is
  green on `main` from `3f8349a` on. Both fail as measured. The superseded
  untagged rule does not reach this case, because `v0.2.1` is tagged. Moving the
  entry to the version it shipped in would itself read as an addition under that
  heading, since blame credits moved lines to whoever moved them. So B-201 needs
  its own resolution.

- **A candidate with no item.** R-CMD-check reports two NOTEs on this pull
  request: hidden files and directories, and non-portable file paths. Every
  file they list is under `.metasalmonpy-sibling`. That is the metasalmonpy
  checkout `R-CMD-check.yaml` places inside the package directory for
  `METASALMONPY_PATH`. `.Rbuildignore` does not exclude it, so `R CMD build`
  packs it into the tarball; the notes list it as
  `metasalmon/.metasalmonpy-sibling/...`. This branch touches neither file nor
  any listed path. A one-line `.Rbuildignore` entry would retire both notes. No
  queue item names this.

## Retirement conditions of what this adds

- **The script:** its docstring. It retires when the changelog stops being
  written by hand.
- **The superseded-untagged exception:** its paragraph in the docstring. In
  practice it stays while 0.1.6 and 0.2.4 keep their lines.
- **The workflow:** its header. It is deleted with the script.
- **The test file:** its header. It retires with the script.
