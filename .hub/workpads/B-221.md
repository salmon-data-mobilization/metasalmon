# Workpad — B-221

## Queue item

**B-221**: hand-picking the IRI of a lower-ranked candidate writes `term_type`
`skos_concept`, and re-applying the rebuilt review writes the candidate's own
type.

The item file, `queue/items/B-221.yaml`, is the only home of its state and its
retirement condition, and this workpad restates neither. The metasalmonpy half
is `B-222`, a separate item, and nothing here does it.

## What changed and where

- **`R/review-console.R`, `accept_suggestion()`.** With `iri =`, the decision
  went on the slot's first row whatever the IRI was. It now goes on the row of
  the first candidate in the slot whose IRI, trimmed and without the `REVIEW:`
  marker, equals the accepted IRI. That is the row `rank = <its rank>` uses,
  and it is where `review_semantics()` replays the decision after an apply. So
  `iri =` naming a shortlisted candidate returns the same review object as
  `rank =` (a test compares them with `expect_identical()`, marked and
  unmarked). An IRI no candidate in the review carries still goes on the first
  row. The `rank =` path is unchanged.
- **`R/review-console.R`, `.ms_review_decision_iri()`**, new. It gives a
  candidate's IRI as a decision records it: trimmed, with the leading
  `REVIEW:` marker removed, which is what `rank =` and a replayed decision put
  in `decision_iri`. `accept_suggestion()`'s selection above reads it.
- **`R/metadata-write.R`, `apply_sdp_semantics()`**: one comparison. Its
  `term_type` branch takes the candidate's type when the decision row carries
  the accepted IRI, and it compared the row's stored IRI, marker and all, with
  the unmarked `decision_iri`. It now reads the row's IRI through
  `.ms_review_decision_iri()`, the rendering the selection uses. Without that,
  a candidate stored as `REVIEW: <IRI>` was selected by `accept_suggestion()`
  and not recognised by the writer, and wrote `skos_concept` by `rank =` as
  much as by `iri =`. The record mask further down is unchanged: it already
  strips the marker, and it reads `semantic_suggestions.csv` through
  `readr::read_csv()`, which trims whitespace, so it cannot disagree with the
  helper on any value the file holds.
- **The `iri` parameter's documentation** says that an `iri` a shortlisted
  candidate carries is recorded as that candidate. `man/accept_suggestion.Rd`
  is regenerated for that paragraph only. This machine's roxygen2 is 8.1.0 and
  the repository was built with 8.0.0, so `devtools::document()` also rewrote
  `DESCRIPTION`'s `Config/roxygen2/version` and reflowed two `importFrom()`
  blocks in `NAMESPACE`. Those two files were reverted.
- **`tests/testthat/test-metadata-write.R`**, a new section after B-176's with
  four tests on a fixture whose `spawner_count` variable slot holds an `smn`
  candidate and then an `owl_class` one (`type_iris` `owl#Class`):
  1. The condition's pin: hand-pick the rank-2 `owl_class` candidate's IRI,
     apply, rebuild with `include_filled = TRUE`, re-apply, and compare
     `column_dictionary.csv`'s raw bytes and the four managed files' digests.
     It asserts its premise first: the IRI is at rank 2, that candidate reads
     `owl_class`, and rank 1 reads `skos_concept`.
  2. `accept_suggestion(iri = <that IRI>)`, and the same IRI with the marker,
     give the review `rank = 2` gives.
  3. The rank-2 candidate's IRI stored as `REVIEW: <IRI>` in
     `semantic_suggestions.csv`: by `iri =` and by `rank = 2`, the write is
     `owl_class`, and a rebuild and re-apply leave the managed files as they
     were.
  4. A control for B-176's case: every candidate is an `owl_class`, and an IRI
     none carries still writes `skos_concept`. It passes before and after. It
     is there so a later fix that takes the first row's type regardless fails.
- **`NEWS.md`.** A *Fixed* entry under *metasalmon (development version)*.
- **`knowledge/parity-deviations.md`** (the port section) and
  **`knowledge/roadmap.md`** (the release index). Each gains the owed port in
  the form the 2026-09-25 entries for B-56 and B-219 use, naming `B-222`. There
  is no landed record, since `B-222` is not done.
- **`queue/items/B-222.yaml`**: `blocked_by: [B-221]`, which was `[]`. #165 did
  the same for B-220, for the same reason (below).

## Evidence

### Failing before, passing after

The test commit `4b391e8` and the fix commit `745597f`, each exported with
`git archive`, run with `pkgload::load_all()` and
`testthat::test_file("tests/testthat/test-metadata-write.R")`, R 4.3.3,
`LC_ALL=C.UTF-8`:

```
tree-4b391e8: 25 tests, 2 with a failure, 0 errored, 0 skipped
FAIL  hand-picking a lower-ranked owl_class candidate's IRI writes its term_type, and a rebuild re-applies the same bytes (3 expectations failed)
FAIL  accept_suggestion(iri = ) naming a shortlisted candidate records what rank = records (2 expectations failed)
ok    an IRI no candidate carries still writes skos_concept, whatever the first candidate is (0 expectations failed)
tree-745597f: 25 tests, 0 with a failure, 0 errored, 0 skipped
(the same three lines, all ok)
```

On `4b391e8` the three failed expectations in the first test are: `term_type`
is `"skos_concept"` where `"owl_class"` is expected. `column_dictionary.csv`'s
bytes differ after the re-apply (`sko…` becomes `owl…`). The digests differ for
`column_dictionary.csv` and for `datapackage.json`, whose descriptor carries
`term_type` too. The second test's two failures show the decision on row 1 for
`iri =` and on row 2 for `rank = 2`.

The marked-candidate test and the writer's comparison are the commit pair
`d88bee0` (test) and `bb66e83` (fix), run the same way:

```
tree-d88bee0: 26 tests, 1 with a failure, 0 errored, 0 skipped
ok    hand-picking a lower-ranked owl_class candidate's IRI writes its term_type, and a rebuild re-applies the same bytes (0 expectations failed)
ok    accept_suggestion(iri = ) naming a shortlisted candidate records what rank = records (0 expectations failed)
FAIL  a candidate whose stored IRI carries the REVIEW: marker writes its own term_type, by iri = and by rank = (2 expectations failed)
ok    an IRI no candidate carries still writes skos_concept, whatever the first candidate is (0 expectations failed)
tree-bb66e83: 26 tests, 0 with a failure, 0 errored, 0 skipped
(the same four lines, all ok)
```

On `d88bee0` both failures are `term_type` `"skos_concept"` where `"owl_class"`
is expected, one for `iri =` and one for `rank =`. Its byte comparison already
passed there, because both applies were wrong the same way.

### The same run as a script, before and after

The item's scenario plus two controls. `main` `0ff9bff` is exported with
`git archive`, and the branch is loaded from the worktree.

```
== main 0ff9bff
iri = owl  decision on rank 1; rebuilt replays on rank 2; term_type skos_concept -> owl_class; column_dictionary.csv identical: FALSE
rank = 2   decision on rank 2; rebuilt replays on rank 2; term_type owl_class -> owl_class; column_dictionary.csv identical: TRUE
iri = new  decision on rank 1; rebuilt replays on rank 1; term_type skos_concept -> skos_concept; column_dictionary.csv identical: TRUE
== branch
iri = owl  decision on rank 2; rebuilt replays on rank 2; term_type owl_class -> owl_class; column_dictionary.csv identical: TRUE
rank = 2   decision on rank 2; rebuilt replays on rank 2; term_type owl_class -> owl_class; column_dictionary.csv identical: TRUE
iri = new  decision on rank 1; rebuilt replays on rank 1; term_type skos_concept -> skos_concept; column_dictionary.csv identical: TRUE
```

`iri = new` is B-176's case, an IRI no candidate carries, and it is unchanged.

### metasalmonpy has the same defect

metasalmonpy `main` `2405df2`, exported with `git archive`, Python 3.11.15,
pandas 3.0.5, on the same two candidates (`semantic_max_per_role=2`).
`accept_suggestion()` sets `target_index` to the slot's first row whenever
`iri` is given (`review_console.py:1214`). `apply_sdp_semantics()` writes
`skos_concept` when that row's `iri` differs from `decision_iri`
(`metadata_write.py:192-200`).

```
{'iri': 'https://example.org/ols/SpawnerCount'}: decision on rank [1]
  rebuilt replays on rank [2]
  term_type first apply='skos_concept', after rebuild+re-apply='owl_class', column_dictionary.csv bytes identical=False
{'rank': 2}: decision on rank [2]
  rebuilt replays on rank [2]
  term_type first apply='owl_class', after rebuild+re-apply='owl_class', column_dictionary.csv bytes identical=True
```

With the rank-2 candidate's IRI stored as `REVIEW: <IRI>` in
`semantic_suggestions.csv`, its writer keeps the marker in `_text(row["iri"])`:

```
premise rank 2: [{'iri': 'REVIEW: https://example.org/ols/SpawnerCount', 'term_type': 'owl_class'}]
{'rank': 2}: decision on rank [2], decision_iri ['https://example.org/ols/SpawnerCount'], term_type written 'skos_concept'
{'iri': 'https://example.org/ols/SpawnerCount'}: decision on rank [1], decision_iri ['https://example.org/ols/SpawnerCount'], term_type written 'skos_concept'
```

### The port record, and why `B-222` now names `B-221`

`B-202`'s port-record rule reads a metasalmonpy item as a port only when its
`blocked_by` names the metasalmon half. On a `git archive` of `bb66e83` with
`B-221` and `B-222` both set to `done` and `claimable: false`, and no landed
record:

```
== blocked_by: [B-221]
knowledge/parity-deviations.md:818: [port-landed-unrecorded] B-222 is a port (the metasalmonpy half of B-221) and its item is done, but this passage has no landed record for it, so it still reads as owed ...
knowledge/roadmap.md:610: [port-landed-unrecorded] B-222 is a port (the metasalmonpy half of B-221) and its item is done, but this passage has no landed record for it, so it still reads as owed ...
FAIL   (exit 1)
== the same copy with blocked_by: []
OK     (exit 0)
```

So with `[]` the two passages this branch adds could go on reading as owed
after `B-222` lands, and lint would pass. The field also holds `B-222` until
this merges, which is the order a port takes.

### Checks

- `python3 scripts/hub_queue.py lint`: OK. `python3 scripts/hub_queue.py
  check`: OK. `git diff --check`: clean.
- `test-review-console.R` passes under `LC_ALL=C.UTF-8`. Under this machine's
  default POSIX locale it fails one test, *"print() emits exactly the rendered
  lines"*, because the rule line prints as `<U+2500>`. B-219's workpad records
  the same failure on `main`. It is the locale, not this change.
- The full `rcmdcheck` line CI runs
  (`rcmdcheck::rcmdcheck(args = "--no-manual")`, results read for errors and
  warnings as `error_on = "warning"` would, `LC_ALL=C.UTF-8`), on the tree of
  `de51130`: **2 errors, 0 warnings, 0 notes**, with the suite at
  `[ FAIL 2 | WARN 38 | SKIP 25 | PASS 4738 ]`. Neither error comes from this
  change. The two test errors are `test-github-helpers.R`'s token-gated network
  tests (`read_github_csv can read remote content with a token`,
  `read_github_csv_dir can fetch when a token is configured`), which get HTTP
  404 here. Run alone on `main` `0ff9bff`, that file gives the same two errors
  (11 tests, 2 errored). The vignette error is the two vignettes `B-164` names
  (`migrating-to-sdp-0-3-0.Rmd`, `tidy-data-for-sdp.Rmd`), each reading a
  `metadata/tables.csv` that does not exist in the check's working directory.
  B-219's workpad records the same three. The full line was run once, on
  `de51130`. The commits after it change `accept_suggestion()`'s selection
  helper and one comparison in `apply_sdp_semantics()`, and add a test. For
  those, every test file that calls `apply_sdp_semantics()` or
  `accept_suggestion()` was run on `bb66e83` under `LC_ALL=C.UTF-8`, with none
  failing: `test-metadata-write.R` (26 tests), `test-review-console.R` (44),
  `test-package-helpers.R` (111), `test-sdp-field-setters.R` (27),
  `test-collation-guard.R` (3) and `test-create-sdp-sidecar-atomicity.R` (5).
  CI's checks on the pull request's head are the full run for those commits.
- **This machine runs R 4.3.3, and CI runs R 4.6.1**, read from the log of
  `main`'s run for `0ff9bff` (job 107967119356). A local pass is evidence
  about 4.3.3.
- Not run: `psc-okf check knowledge --tier capture`. There is no sibling
  `psc-data-systems` checkout on this machine.

## What was not done

- The metasalmonpy fix. It is `B-222`, and a hub claim covers one branch in one
  repository.
- Any change to what `apply_sdp_semantics()` writes for an IRI no shortlisted
  candidate carries. That is B-176's case, and the condition keeps it.
- The case below, which is outside this item's condition.

## Found, not absorbed

1. **Candidate, a pair: an IRI carried by a candidate outside the review's
   `max_candidates` window.** `accept_suggestion()` can only look at the review
   it is given, and `review_semantics()` drops every row past
   `max_candidates` (5 by default). So `iri =` naming a rank-6 candidate goes on
   the first row and writes `skos_concept`. `apply_sdp_semantics()` then marks
   the rank-6 row `accepted` in `semantic_suggestions.csv`, because its record
   mask matches by IRI across the whole slot. A review rebuilt at the default
   window shows no decision, which is `B-217`'s symptom. Rebuilt with
   `max_candidates = Inf`, it replays on rank 6, and re-applying writes that
   candidate's type. Measured on this branch, six candidates with an
   `owl_class` at rank 6:

   ```
   default review holds ranks 1,2,3,4,5
   iri = <rank-6 owl_class IRI>, default window: term_type skos_concept
   rebuilt at default window carries a decision: FALSE
   rebuilt at max_candidates = Inf replays on rank 6
   re-applied: term_type owl_class ; column_dictionary.csv identical: FALSE
   ```

   metasalmonpy `main` `2405df2` gives the same (`max_candidates=None` shows
   all): ranks 1-5, `skos_concept` then `owl_class`, bytes not identical. The
   same decision can therefore write two types, depending on the window the
   next review is built with. The condition names a *shortlisted* candidate, and
   `rank = 6` does not exist in a five-row review, so this is not B-221's. A fix
   would probably go in the writer, taking `term_type` from the package's own
   `semantic_suggestions.csv` rows for the slot, the same rows the record mask
   marks. That choice belongs to whichever item takes this up. It sits beside
   `B-217` and `B-218` rather than inside them: they are about what a review
   shows, and this is about what gets written.

## Guards added, and what retires them

None. The four tests are regression tests, not suppressions, skips or
workarounds. Each one's premise assertions are there so that a change to the
fixture's ranking or to type inference fails loudly, rather than leaving a test
that checks nothing. They go when the tests go.
