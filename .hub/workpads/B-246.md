# Workpad — B-246

## Queue item

**B-246**: the R review console can still record an accept whose IRI is empty
or still a `REVIEW:` marker, and reports a suggestion row with an empty IRI as a
field it cannot decide.

The item file, `queue/items/B-246.yaml`, is the only home of its state and its
retirement condition (`retires_when`), and this workpad restates neither. The
condition's four cases are referred to below as (1) to (4), in its order. The
metasalmonpy half is `B-247`, a separate item, and nothing here does it. Which
spellings count as the marker is `Q-63`'s: every case is written against the
spellings `.ms_strip_review_iri()` and `.ms_is_review_iri()` recognise today,
and neither function changed.

## What changed and where

- **`R/review-console.R`, `.ms_review_names_term()`**, new. It takes an IRI as
  a decision records it (`.ms_review_decision_iri()`: trimmed, one leading
  marker stripped) and answers whether it names a term: not missing, not empty,
  and not still read as a marker by `.ms_is_review_iri()`. The queue, the
  refusal and the console below all read it, so they cannot disagree about what
  a decision may record. That disagreement, between a check and the strip, is
  what B-219 fixed.
- **`R/review-console.R`, `review_semantics()`.** `has_iri` is that predicate,
  where it tested only that `iri` was not blank. The list printed under *"Some
  suggestions target fields this review cannot decide"* is built from the rows
  with no write-back address or no IRI field (`!decidable`), where it was built
  from every dropped row (`!keep`). Since the second Codex review, a row
  carrying a recorded reject is kept whatever its IRI, because rejecting a slot
  names no candidate, so its rejection and reason are still replayed.
- **`R/review-console.R`, `accept_suggestion()`.** The refusal calls the
  predicate instead of `nzchar()`, so a value that is still a marker once one
  is stripped is refused too. It has its own message: *"`iri` must be an IRI,
  not a `REVIEW:` marker."* The two B-219 messages are unchanged, byte for
  byte. Since the first Codex review it also runs on `rank =`, where it aborts
  with *"The candidate at that `rank` names no term."* `review_semantics()`
  queues no such candidate, so this is reached only by a review that function
  did not build as it is now: one saved by an earlier version, or edited by
  hand.
- **`R/review-console.R`, `.ms_review_render_lines()`.** Since the third Codex
  review, the console prints no accept call for a candidate whose IRI names no
  term. It prints *"(its IRI names no term, so it cannot be accepted)"* in its
  place, because every printed call has to run. Such a candidate reaches the
  console only as the carrier of a recorded reject, or in a review
  `review_semantics()` did not build.
- **`tests/testthat/test-review-console.R`**, 36 tests after B-219's:
  - (1) per spelling in `marker_only_iris` (8): a candidate that is only the
    marker is not queued alone, so `accept_suggestion(rank = 1)` finds no slot;
    ahead of a real candidate it takes no rank, dropping it prints no "cannot
    decide", and `rank = 1` records the real IRI.
  - (2) per spelling (8): a recorded `accepted` row whose `iri` is only the
    marker replays no accept with an empty `decision_iri`, and the slot is
    queued again, undecided.
  - (3) per doubled spelling (4: `REVIEW: REVIEW:`, `REVIEW:REVIEW:`,
    `review : Review:`, and `REVIEW: REVIEW: <IRI>`): `iri =` refuses it; as a
    candidate it is not queued, so `rank = 1` records the real candidate, and
    a recorded accept of it replays no marker.
  - (4) for an empty and a missing `iri` (2): the row is not reported under
    "cannot decide" and the slot's real candidate is queued; beside a
    `dataset.csv` `keywords` row, the message still names `dataset.csv` and
    no longer names `column_dictionary.csv`.
  - (1) and (3) in a review the current `review_semantics()` did not build
    (12, every spelling of both lists): with a queued candidate's `iri` edited
    to the spelling, `rank = 1` refuses it. Added for the first Codex review.
  - A recorded reject (1 test, looping over every spelling of both lists and
    an empty and a missing `iri`): a slot whose only candidate carries the
    value, recorded `rejected` with a reason, comes back from
    `include_filled = TRUE` as a reject with its reason, is not in the default
    queue, and `rank = 1` on it is refused. Added for the second Codex review.
  - The console (1 test, looping over `REVIEW:`, `REVIEW: REVIEW:`, an empty
    and a missing `iri`): beside a slot with a real candidate, a rejected slot
    whose only candidate carries the value shows `DECIDED: reject` and the "names
    no term" note. The one accept call printed runs and records the real
    candidate, and every reject call runs. Added for the third Codex review.
  - Each per-spelling test asserts its premise against the strip and detector
    first, as B-219's do, and (1) and (2) reuse B-219's `marker_only_iris`
    rather than a second list.
- **`NEWS.md`**: a *Fixed* entry under *metasalmon (development version)*,
  including that a candidate the queue drops takes no rank, so the candidates
  after it in its slot rank one place higher.
- **`knowledge/parity-deviations.md`** (the port section) and
  **`knowledge/roadmap.md`** (the release index): the owed port, in the form
  B-219's and B-221's use, naming `B-247`, with no landed record.

## Which shape, and why each fix is the smallest

- **(1): never queue, not refuse in `accept_suggestion()`.** One changed line,
  `has_iri`. It is the smallest because the same line also closes (2):
  `.ms_review_seed_recorded_decisions()` replays decisions over the queued rows
  only. Refusing in `accept_suggestion()` would have closed (1) and left the
  replay recording the empty IRI, so it needed a second change in the replay.
  It is also how a blank `iri` was already treated: never queued, taking no
  rank. **The first Codex review found the route this shape leaves open:** a
  review object the current `review_semantics()` did not build, one saved by an
  earlier version or edited by hand, can still hold such a candidate, and
  `accept_suggestion()` checked only the class of what it was given. So the
  refusal now runs on `rank =` too. That is the other shape as a second layer,
  not a replacement: the queue filter is still what closes (2) and what keeps
  the console from printing a candidate it would then refuse.
- **(2): no change of its own.** Closed by (1)'s line.
- **(3): refuse, and do not strip again.** The predicate's
  `.ms_is_review_iri()` term closes the `rank =` and replay routes inside the
  same `has_iri` line, and the existing `iri =` refusal calls the predicate,
  plus one message branch (and the `rank =` refusal above covers a review that
  still holds one). Stripping repeatedly would have been a new rule about what
  counts as the marker, which is `Q-63`'s.
- **(4): build the report from `!decidable` rather than `!keep`.** Three index
  expressions change and nothing is added. The condition allows not reporting
  such a row. The row offers nothing to accept, and the slot's real candidates
  are queued as before, so no new message was written for it.
- **No exported signature, return-value attribute or column changed.** The
  review tibble has the same columns. `review_semantics()` returns fewer rows
  where a candidate names no term, and `accept_suggestion()` refuses more
  values. The 19-column target row is untouched.

## Evidence

### Red on the code as it stands, green with the fix

Commit `ddbfa34` adds the tests alone on `390207d`, which was `main` when the
item was claimed; `7cfc7c8` adds the fix. Each commit was exported with
`git archive` and run with `pkgload::load_all(".")` then
`testthat::test_file("tests/testthat/test-review-console.R")`, tabulated per
test, with R 4.3.3 and `LANG=C.UTF-8` (`LC_COLLATE=C.UTF-8`):

```
ddbfa34 (tests, R/ as on 390207d): 66 tests, 513 expectations, 60 failed, 0 errors
  FAIL 3/5  (1) x 8 spellings   - nrow(alone) 1 not 0; accept(rank = 1) did not error;
                                  rank = 1 recorded decision_iri "" not the real IRI
  FAIL 2/4  (2) x 8 spellings   - an accept with decision_iri "" was replayed;
                                  the slot was not queued again
  FAIL 4/5  (3) x 4 doubled     - iri = did not error; rank = 1 recorded a marker
                                  (.ms_is_review_iri() TRUE); the replay was a marker
  FAIL 2/6  (4) x 2 forms       - "cannot decide ... column_dictionary.csv · term_iri
                                  ... Edit those in the metadata CSVs directly."
  the file's 44 existing tests: all pass

7cfc7c8 (the fix): 66 tests, 513 expectations, 0 failed, 0 errors
c2c0caf (the branch with main 670955f merged in): the same, 0 failed
```

**(4) was read and not run when it was filed; this red run is its first
measurement in R, and it reproduces.** With an `accepted` row whose `iri` is
empty at the head of a slot whose other candidate is queued,
`review_semantics()` printed:

```
Some suggestions target fields this review cannot decide, and are not queued.
* column_dictionary.csv · term_iri
i Edit those in the metadata CSVs directly.
```

The same probe, run before any test was written, on `390207d`: `rank = 1` on a
candidate `"REVIEW:"`, `"REVIEW: "` or `"review:"` recorded `decision_iri`
`""`; a recorded `accepted` row with `iri` `"REVIEW:"` replayed as `accept`
with `decision_iri` `""`; `iri = "REVIEW: REVIEW:"` and `rank = 1` on a
candidate carrying it each recorded `"REVIEW:"`, which `.ms_is_review_iri()`
reads as a marker.

### Each part of the fix is pinned

Each change reverted alone on top of the fix, the rest kept:

```
has_iri back to the blank test        52 failed: all of (1), (2), (3)'s rank and replay parts
report mask back to !keep             12 failed: (4), and (1)'s "no cannot-decide" check
iri = refusal back to nzchar()         4 failed: (3)'s iri = part
```

The second row is why (4)'s change is needed by (1)'s: without it, every
candidate that (1) drops would be listed as a field the review cannot decide.

### The full suite

`Rscript -e 'devtools::test()'` with R 4.3.3 and `LANG=C.UTF-8`, run twice.

On `c2c0caf`, the branch with `main` `670955f` merged in, before any Codex
round:

```
[ FAIL 0 | WARN 38 | SKIP 11 | PASS 5065 ]
```

On `934c227`, the code as handed back: after all three rounds, with `main`
`9aeb0ec` merged in and `METASALMONPY_PATH` pointed at metasalmonpy `cae3d83`'s
`PARITY.md`:

```
[ FAIL 0 | WARN 38 | SKIP 10 | PASS 5178 ]
```

Every skip comes from the environment, and none is in
`test-review-console.R`. They are:

- the CI-only optional-package check;
- two token-gated GitHub reads, answered HTTP 404 here;
- two locale-independence tests, with no contrasting collation locale;
- one with no non-English `LC_TIME`;
- four opt-in Theme A integrity tests;
- in the first run only, the parity-register guard, which found no
  metasalmonpy sibling.

So a green local run is not full coverage. The second run's register guard
compared against `cae3d83`, 63 rows. CI compares against metasalmonpy's
current `main`, which now has 64; see *CI*.

### Other checks

These were run first on `c2c0caf`, and the first, second, fourth and fifth were
run again on `934c227` with the same results.

- `python3 scripts/hub_queue.py lint`: OK. `check`: OK.
- `python3 scripts/check-changelog-window.py`: no findings.
- `python3 scripts/check-parity-registers.py` against metasalmonpy `main`
  `e089b86`'s `PARITY.md`: the registers agree, 61 rows. No row changed here.
  That was true at about 22:03Z and stopped being true minutes later, when
  metasalmonpy #55 and #60 merged rows 62 and 63 (`cae3d83`, 63 rows). CI
  says so below.
- `python3 -m pytest scripts/tests -q`: 186 passed.
- `git diff --check`: clean. No non-ASCII character in the R or test diff.
- (4) through a package on disk: a `create_sdp()` package built with the
  mocked `find_terms()` of `tests/testthat/test-metadata-write.R`, with the row
  the B-219 defect wrote (a `user` row, `accepted`, empty `iri`) put at the
  head of the `spawner_count` variable slot in `semantic_suggestions.csv`.
  `review_semantics(path)` on the tree of `ddbfa34` printed the "cannot
  decide ... column_dictionary.csv · term_iri" message; on the fix it printed
  nothing. Both queued the slot's two real candidates.

### Codex review, round 1 (on `8806320`)

One P2 finding: *"Validate ranked IRIs before recording the decision"*. A
review object the current `review_semantics()` did not build, saved by an
earlier version or edited by hand, can hold a candidate whose IRI names no
term, and `rank =` skipped the predicate. It reproduces. Twelve new tests (every
spelling of `marker_only_iris` and `doubled_marker_iris`, with a queued
candidate's `iri` edited to it) were run with `test_file()` as above:

```
8806320 (before):    78 tests, 12 failed expectations, one per new test: rank = 1 recorded the accept
with the refusal:    78 tests, 0 failed
```

The fix is the refusal on `rank =` described under *What changed*. It is
answered on the finding's thread.

### Codex review, round 2 (on `c75fcaa`)

Two P2 findings. The security review found nothing.

- **"Preserve recorded rejects when filtering malformed candidates."** It is
  real, and it came from this branch. The queue filter dropped a row with a
  recorded `rejected` before the replay could read it. A slot whose only
  candidate named no term therefore lost its rejection and reason from
  `include_filled = TRUE`. Probed with a one-candidate slot recorded
  `rejected`, reason `why`, reading `include_filled = TRUE`:

  ```
                     main 390207d        1c762fc (before)    with the fix
  "REVIEW:"          reject, "why"       no row              reject, "why"
  "review :"         reject, "why"       no row              reject, "why"
  "REVIEW: REVIEW:"  reject, "why"       no row              reject, "why"
  ""                 no row              no row              reject, "why"
  NA                 no row              no row              reject, "why"
  ```

  So the branch had lost what `main` did for a marker. For a blank `iri`,
  `main` lost it too. The fix keeps any row carrying a recorded reject,
  whatever its IRI, which covers both, and `rank =` still refuses such a row.
  The new test was red on `1c762fc`: it errored on its first value, after two
  failed expectations, because the review was empty. With the fix it is green:
  79 tests, 593 expectations, 0 failed.
- **"Validate saved accept decisions before applying them."** It is real, and
  it is not fixed here. A review object saved by an earlier version can already
  hold an `accept` whose `decision_iri` is empty or still a marker, and
  `apply_sdp_semantics()` writes it. That is a decision recorded under an
  earlier version's rules and written by a different function. This item is
  about what the console records, and the family's own condition (B-219's) put
  the fix at the source rather than downstream in the writer. `HUB.md` routes a
  problem found beside a change to the next sweep, so it is below as a
  candidate, with the probe. It is answered on its thread, and the thread is
  left open for whoever merges, because the finding was routed rather than
  fixed.

### Codex review, round 3 (on `1713c9c`), the last one the cap allows

One P2 finding: *"Omit unusable accept calls for replay-only reject rows"*. It
is real, and round 2's fix introduced it. The console printed an
`accept_suggestion(..., rank = )` call for every row, so a kept reject row
whose IRI names no term got a call that the round-1 refusal then aborts. That
breaks the console's contract that every printed call runs. The fix is the
renderer change under *What changed*. The new test was red on `1713c9c`: two
failed expectations (no note printed, and two accept calls where one belongs),
and then an error when the printed call for that row was evaluated. With the
fix it is green: 80 tests, 625 expectations, 0 failed. The security review
found nothing. No further review was asked for, as the cap in `HUB.md`
requires.

### CI

CI runs **R 4.6.1 (2026-06-24)** under `LANG`/`LC_ALL` `en_US.UTF-8`, read from
the `check` job's log for `8806320`, against 4.3.3 here. On `8806320`,
`test-review-console.R` passed on CI. The `check` and `registers-agree` jobs
each failed on one thing, and it did not come from this change:
`test-parity-register-guard.R` and `check-parity-registers.py` both report
*"PARITY.md: rows [62, 63] have no counterpart in parity-deviations.md"*.
`main` fails the same way (`c93328c`, parity-registers run 36195280179), from
the moment metasalmonpy #55 and #60 merged. metasalmon #193 carries both rows
and is green on that check, so merging `main` into this branch once #193 lands
is what turns these two green. Adding the rows here would be a register change,
which is Brett's class 5 and not this item's.

The same held on `c75fcaa`: the one CI failure was the register guard, and
`test-review-console.R` passed. #193 merged as `9aeb0ec`, and `1c762fc` merges
it in. On that tree, `check-parity-registers.py` and
`test-parity-register-guard.R`, pointed at metasalmonpy `main` `cae3d83`, agree
on 63 rows.

**It moved again.** metasalmonpy #44 (B-188, `6f1a191`) then merged its
register row as row 64. So on `1713c9c` the same two jobs failed on *"PARITY.md:
rows [64] have no counterpart"*, and again nothing else failed:
`test-review-console.R` passed on R 4.6.1. The metasalmon half of that row is
open pull request #179, which files it as row 62. Its metasalmonpy twin was
renumbered to 64 on merge, so #179 has to move to 64 as well. Until it lands,
every metasalmon pull request fails these two jobs. That is #179's and B-188's
to settle, not this item's.

## What was not done

- The metasalmonpy half. It is `B-247`, and a hub claim covers one branch in
  one repository.
- Any change to `.ms_strip_review_iri()` or `.ms_is_review_iri()`, or to which
  spellings count as the marker, which is `Q-63`'s.
- Any change to `apply_sdp_semantics()`. The console now records no such
  decision. A review saved by an earlier version can still carry one to the
  writer, and that is the candidate below.
- A local `rcmdcheck`. CI runs the check line, and the suite above is the
  local evidence.

## Found, not absorbed

This reports what was looked at, which was the review console's queue, replay,
refusal and report, and the writer's record mask. It does not claim that nothing
else is wrong.

**Candidate: `apply_sdp_semantics()` writes a recorded accept that names no
term.** This comes from the second Codex review. A review object saved before
this change can hold an `accept` whose `decision_iri` is empty or still a
marker, and the writer checks only the object's class. Probed on this branch
with a `create_sdp()` package (the mocked `find_terms()` of
`tests/testthat/test-metadata-write.R`): the `spawner_count` variable row set to
`decision = "accept"` and `decision_iri = ""`, then applied. `term_iri` went
from `REVIEW: https://w3id.org/smn/SpawnerAbundance` to empty, and
`semantic_suggestions.csv` gained a `user` row, `accepted`, with an empty `iri`,
ahead of the two real candidates marked `not_selected`. That is the damage
B-219 described, reached through a saved object. A fix has a choice to make:
the writer strips one marker as it writes (`R/metadata-write.R:451`), so an old
`decision_iri` of `REVIEW: <IRI>` still writes a real IRI, and a refusal at
that boundary has to decide whether to accept it. metasalmonpy was not measured.

Two things that are not this item's:

1. **The register drift that fails CI.** Rows 62 and 63 came in with #193,
   now merged. Row 64 belongs to #179 (B-188), which has to move to that number.
   See *CI* above.
2. **Open pull request #194 (B-326) moves `review_semantics()`'s body into a new
   `.ms_review_queue()`.** The filter lines this branch changes sit inside the
   moved body, and #194 does not edit them, so git should merge the two.
   Whichever lands second should still run `test-review-console.R`. Once both
   land, the review packet that #194 builds from that queue will also leave out
   a candidate that names no term, apart from a recorded reject. That is the
   agreement between console and packet that #194's own comment asks for.

## Guards added, and what retires them

- **The premise assertion in each per-spelling test.** (1) and (2) assert that
  the strip empties the spelling, and (3) that what the strip leaves is still
  read as a marker. Each fails naming its spelling if the strip or detector
  changes, so a changed strip cannot leave a test that checks nothing.
  *Retires when* `Q-63`'s ruling puts every detector behind one predicate: the
  lists should then come from it, as B-219's list should.
- The queue filter and the silent drop in (4) are behaviour rather than
  guards: a row that names no term offers nothing to decide, so nothing
  retires them. The same holds for the `rank =` refusal and for the console's
  note in place of an accept call. The call that note replaces could only
  fail, so leaving it out hides no signal.

## Hand-back

- Pull request #199, opened as a draft with the `agent-run` label. It was
  marked ready, because this change sits in a delegated class. It is not
  merged: the dispatch said not to merge.
- The claim was handed off with `hub done`, and the heartbeat loop was stopped
  after that. No beat fell due between the claim and the handoff.
- Codex reviewed three times, the cap for a pull request that changes code:
  once when the pull request was marked ready, then after two
  `@codex review` requests. There were four findings. Three were fixed, and
  their threads are answered and resolved. One, the saved-accept finding, was
  routed to the next sweep. It is answered on its thread and listed as a
  candidate above, and the thread is left open. The security review found
  nothing in any round.
- CI: on each head measured before this workpad's last commit (`8806320`,
  `c75fcaa` and `1713c9c`), the only failures were the two register jobs, for
  the reason given under *CI*. The final head is read in the hand-back report,
  not here, because this file is part of it.
