# Workpad: B-44

## Queue item

**B-44 — The gcdfo validation layer is inert** (P1, stream S6, repo
`dfo-salmon-ontology`). Claimed 2026-09-10T21:40Z by `a-71a6d7c9eefb16bc`
under the batch lease. Evidence:
`knowledge/plans/2026-08-10-gcdfo-validation-layer-verification.md` and
backlog `#44` in the metasalmon hub.

Retirement condition, verbatim: *The SHACL shapes and example data bind their
default prefix to the namespace gcdfo subjects actually use, each competency
query declares its prefixes and is executed separately, `robot-profile.yaml`
amends rather than replaces the default profile so the 16 ERROR-level checks
run again, and a deliberately broken fixture makes the gate fail.*

**One finding up front.** Clause 1's literal condition was already met on
`origin/main` (`26d7c38`): both `ontology/shapes/dfo-salmon-shapes.ttl` and
`ontology/examples/sample-survey-data.ttl` bind `@prefix : <https://w3id.org/gcdfo/salmon#>`,
and no file in the tree names `w3id.org/dfo/salmon#` any more. The 2026-08-10
evidence is stale on that one point. What that rebinding exposed — and what
nothing had ever run to see — is that the shapes, once live, **reject the
repo's own example data** (7 violations). So clause 1's real work turned out
to be making the shapes satisfiable and running them, not the prefix.

## What changed, and where

All inside the worktree `/home/user/hub-worktrees/dfo-pacific-science-dfo-salmon-ontology-B-44`
on branch `agent/B-44/a-71a6d7c9eefb16bc`, from `origin/main` = `26d7c38`.
No ontology term IRI, label or definition was chosen, changed or removed.

| Clause | File | Change |
|---|---|---|
| 1 SHACL | `ontology/shapes/dfo-salmon-shapes.ttl` | `SnorkelSurveyShape` and `AerialSurveyShape` both targeted every `EscapementSurveyEvent` with contradictory `sh:hasValue`s. Each now uses a SHACL-core implication: `sh:or ( [not this method] [this method's constraints] )`, so its constraints apply only to events of its method; inner constraints unchanged. `countUnitIRI` accepted only an `xsd:anyURI` literal while the data gives an IRI node and the ontology declares no range: now `sh:or ( [sh:nodeKind sh:IRI] [sh:datatype xsd:anyURI] )`, retirement condition in the file. No SHACL-AF (SPARQL target) on purpose: measured that pyshacl without `--advanced` silently ignores one and reports `Conforms: True`. |
| 1 SHACL | `scripts/shacl-validate.sh` (new), `Makefile` `shacl-validate` | pyshacl over shapes + example data + ontology (no `owl:imports` fetched, no network), fails on non-conformance, report to `release/tmp/shacl-report.txt`. |
| 1 SHACL | `scripts/requirements.txt`, `.github/workflows/ci.yml` | `pyshacl==0.31.0` — the newest that resolves against the `rdflib==7.2.1` pin CI relies on for `docs/gcdfo.jsonld` bytes (0.40.x needs a newer rdflib). CI now installs `-r scripts/requirements.txt` so the pins have one home; the `.tsv` report is added to the uploaded artifacts. |
| 2 SPARQL | `ontology/sparql/competency/cq-01-stock-status-zones.rq`, `cq-02-survey-events-with-protocols.rq`, `cq-03-management-units-and-conservation-units.rq` (new); `ontology/sparql/example-competency-questions.rq` (deleted) | One query per file, prefixes declared in each. Query bodies are byte-identical to the originals (verified by diff after stripping comments). |
| 2 SPARQL | `scripts/run-competency-questions.sh` (new), `Makefile` `competency-questions` | ROBOT `merge` (ontology + example data, import closure collapsed via the catalog) then `query` per file; a parse error, an undeclared prefix or a ROBOT failure fails the run; prints a row count per query; **zero rows is reported, not failed** (see "guards" below). |
| 3 ROBOT | `robot-profile.yaml` | Was 2 lines (`INFO missing_label`, `INFO missing_definition`). Now ROBOT 1.9.8's own `report_profile.txt` (32 rules, extracted from `tools/robot.jar`) with exactly those two rules at INFO; 16 rules at ERROR. ROBOT rejects `#` comment lines in a profile (tested: `REPORT LEVEL ERROR '# …' is not a valid reporting level`), so provenance is in `scripts/robot-quality-check.sh`'s header and `docs/ROBOT_SETUP.md`, and enforced by the self-test. |
| 3 ROBOT | `scripts/robot-quality-check.sh` | Found while running it: under `set -e`, a ROBOT exit of 1 killed the script before its own "ERROR-level violations" branch, so a failing gate printed nothing (unreachable since the day it was written; never mattered because the 2-line profile could not produce an ERROR). The call is now wrapped in `set +e`/`set -e`, the branch is reachable, and it re-runs the report as TSV to print the failing rows. The stale "expected violations" echo block (31 SKOS label errors, etc.) replaced with the current baseline. |
| 4 fixture | `tests/fixtures/shacl/invalid-survey-data.ttl`, `tests/fixtures/sparql/undeclared-prefix.rq`, `tests/fixtures/robot/duplicate-label.ttl` (new) | Deliberately broken inputs, one per gate, each header saying what it provokes and what retires it. |
| 4 fixture | `scripts/validation-selftest.sh` (new), `Makefile` `validation-selftest`, `test` | Four checks: SHACL gate fails on its fixture with every documented message present **and** the aerial control node absent (proves the conditional shapes are conditional); competency runner fails on the undeclared-prefix query; `robot report --profile robot-profile.yaml --fail-on ERROR` exits 1 on the duplicate-label fixture with `duplicate_label` as the **only** ERROR rule; `robot-profile.yaml` equals the jar's default profile modulo exactly the two documented downgrades. Wired into `make test`, hence `make ci`. |
| docs | `docs/entrypoints.md`, `docs/ROBOT_SETUP.md`, `docs/tech-debt.md` (2 new entries), `docs/adr/010-validation-gates.md` (new), `CHANGELOG.md` (Unreleased / Fixed), `README.md` (2 lines), `AGENTS.md` (agent-context: stack + `last_updated`) | Per the target repo's `AGENTS.md`: entrypoints on wiring change, tech-debt on debt introduced, ADR on a choice between approaches (pyshacl vs nothing; SHACL core vs SHACL-AF; whole-profile-plus-pin vs overrides). |

## Commands run and results

Toolchain on this machine: OpenJDK 21.0.10; ROBOT 1.9.8 fetched to
`tools/robot.jar` exactly as `make install-robot` does (git-ignored);
Python 3.11.15; pyshacl 0.31.0 + rdflib 7.2.1 in a throwaway venv matching
the CI pins (the system Python has 0.40.1/7.6.0 — every SHACL result below
was produced with the pinned pair, and the SPARQL-target probe with both);
`make prepare-import-catalog` fetched the pinned smn commit `f7205ee` from
GitHub (no sibling checkout here). WIDOCO was not fetched: nothing here
touches the ontology, so `make docs-refresh` regenerates nothing — not run,
and not claimed.

### Clause 1 — SHACL

**Failing before** (shapes as on `origin/main` over the repo's own example data;
`pyshacl -s <old shapes> -e ontology/dfo-salmon.ttl ontology/examples/sample-survey-data.ttl`, exit 1):

```
Validation Report
Conforms: False
Results (7):
Constraint Violation in DatatypeConstraintComponent (http://www.w3.org/ns/shacl#DatatypeConstraintComponent):
	Severity: sh:Violation
	Source Shape: [ sh:datatype xsd:anyURI ; sh:message Literal("Count unit must be a valid IRI") ; sh:path :countUnitIRI ]
	Focus Node: :SkeenaCount2022_001
	Value Node: qudtunit:Each
	Result Path: :countUnitIRI
	Message: Count unit must be a valid IRI
Constraint Violation in DatatypeConstraintComponent (http://www.w3.org/ns/shacl#DatatypeConstraintComponent):
	Severity: sh:Violation
	Source Shape: [ sh:datatype xsd:anyURI ; sh:message Literal("Count unit must be a valid IRI") ; sh:path :countUnitIRI ]
	Focus Node: :SkeenaCount2022_002
	Value Node: qudtunit:Each
	Result Path: :countUnitIRI
	Message: Count unit must be a valid IRI
Constraint Violation in HasValueConstraintComponent (http://www.w3.org/ns/shacl#HasValueConstraintComponent):
	Severity: sh:Violation
	Source Shape: [ sh:hasValue :AerialSurveyCount ; sh:message Literal("Aerial surveys must use AerialSurveyCount method") ; sh:path :usesEnumerationMethod ]
	Focus Node: :SkeenaSnorkel2022_001
	Result Path: :usesEnumerationMethod
	Message: Aerial surveys must use AerialSurveyCount method
Constraint Violation in HasValueConstraintComponent (http://www.w3.org/ns/shacl#HasValueConstraintComponent):
	Severity: sh:Violation
	Source Shape: [ sh:hasValue :AerialSurveyCount ; sh:message Literal("Aerial surveys must use AerialSurveyCount method") ; sh:path :usesEnumerationMethod ]
	Focus Node: :SkeenaSnorkel2022_002
	Result Path: :usesEnumerationMethod
	Message: Aerial surveys must use AerialSurveyCount method
Constraint Violation in HasValueConstraintComponent (http://www.w3.org/ns/shacl#HasValueConstraintComponent):
	Severity: sh:Violation
	Source Shape: [ sh:hasValue :AerialSurveyCount ; sh:message Literal("Aerial surveys must use AerialSurveyCount method") ; sh:path :usesEnumerationMethod ]
	Focus Node: :SkeenaSnorkel2022_003
	Result Path: :usesEnumerationMethod
	Message: Aerial surveys must use AerialSurveyCount method
Constraint Violation in HasValueConstraintComponent (http://www.w3.org/ns/shacl#HasValueConstraintComponent):
	Severity: sh:Violation
	Source Shape: [ sh:hasValue :AerialSurveyCount ; sh:message Literal("Aerial surveys must use AerialSurveyCount method") ; sh:path :usesEnumerationMethod ]
	Focus Node: :SkeenaSnorkel2022_004
	Result Path: :usesEnumerationMethod
	Message: Aerial surveys must use AerialSurveyCount method
Constraint Violation in HasValueConstraintComponent (http://www.w3.org/ns/shacl#HasValueConstraintComponent):
	Severity: sh:Violation
	Source Shape: [ sh:hasValue :VisualSnorkelCount ; sh:message Literal("Snorkel surveys must use VisualSnorkelCount method") ; sh:path :usesEnumerationMethod ]
	Focus Node: :SkeenaAerial2022_001
	Result Path: :usesEnumerationMethod
	Message: Snorkel surveys must use VisualSnorkelCount method
```

The 7: `countUnitIRI` × 2 (IRI node vs `xsd:anyURI` literal), `AerialSurveyShape`
firing on all four snorkel events, `SnorkelSurveyShape` firing on the aerial
event. Every event was required to be both.

**Passing after** (`make shacl-validate`, exit 0):

```
🔎 SHACL: ontology/shapes/dfo-salmon-shapes.ttl over ontology/examples/sample-survey-data.ttl (+ ontology/dfo-salmon.ttl)
✅ SHACL: data conforms (report: release/tmp/shacl-report.txt)
```

### Clause 2 — competency queries

**Failing before** (the combined file as on `origin/main`, through the new
runner, exit 1 — this is the state the file had shipped in):

```
📚 Using ROBOT catalog for import resolution: release/tmp/robot-catalog.xml
🔎 Running 1 competency question(s) from /tmp/claude-0/-home-user-metasalmon/c65e9c22-7ec7-5a53-8177-deda0f189418/scratchpad/B-44/evidence/old-cq
❌ example-competency-questions: query failed (see release/tmp/competency-before/example-competency-questions.log)
     QUERY PARSE ERROR query cannot be parsed:
     Encountered " "prefix" "PREFIX "" at line 15, column 1.
     Was expecting one of:
         <EOF> 
         "limit" ...
❌ 1 competency question(s) failed to execute.
```

**Passing after** (`make competency-questions`, exit 0):

```
📚 Using ROBOT catalog for import resolution: release/tmp/robot-catalog.xml
🔎 Running 3 competency question(s) from ontology/sparql/competency
✅ cq-01-stock-status-zones: executed, 0 rows -- the query parses and runs but matches nothing in the example data; see the header of this script
✅ cq-02-survey-events-with-protocols: executed, 0 rows -- the query parses and runs but matches nothing in the example data; see the header of this script
✅ cq-03-management-units-and-conservation-units: executed, 0 rows -- the query parses and runs but matches nothing in the example data; see the header of this script
✅ All competency questions executed.
```

All three execute and all three return zero rows — see "belongs elsewhere".

### Clause 3 — ROBOT profile

**Failing before** (old 2-line profile over the real ontology: the gate cannot
fail; and over the duplicate-label fixture: it does not):

```
$ robot report --profile <old robot-profile.yaml> --fail-on ERROR --input ontology/dfo-salmon.ttl
Violations: 45
-----------------
ERROR:      0
WARN:       0
INFO:       45
exit=0     (45 = 25 missing_label + 20 missing_definition; those were the only two checks run)

$ robot report --profile <old robot-profile.yaml> --fail-on ERROR --input tests/fixtures/robot/duplicate-label.ttl
No violations found.
exit=0
```

**After** (amended profile; 32 rules, 16 at ERROR):

```
$ robot report --profile robot-profile.yaml --fail-on ERROR --input tests/fixtures/robot/duplicate-label.ttl
Violations: 4
-----------------
ERROR:      2
WARN:       0
INFO:       2
2026-09-10 21:57:04,417 ERROR org.obolibrary.robot.ReportCommand - Report failed!
exit=1   (ERROR rows: duplicate_label × 2, nothing else at ERROR)

$ make quality-check        # the real ontology
🔍 Check the report for details. ERROR-level violations need attention.
❌ ERROR violations found:
ERROR:      2
2026-09-10 22:01:37,823 ERROR org.obolibrary.robot.ReportCommand - Report failed!

   Failing rows (release/artifacts/robot-quality-report.tsv):
   ERROR	duplicate_label	https://w3id.org/gcdfo/salmon#WildSalmonPolicy	rdfs:label	Wild Salmon Policy@en
   ERROR	duplicate_label	https://w3id.org/gcdfo/salmon#WildSalmonPolicyTheme	rdfs:label	Wild Salmon Policy@en
make: *** [Makefile:92: quality-check] Error 1
```

**That last result is the gate working, and it is red on `main`.** The
re-enabled `duplicate_label` check finds `gcdfo:WildSalmonPolicy` and
`gcdfo:WildSalmonPolicyTheme` both labelled `"Wild Salmon Policy"@en`. Fixing
it is a label change — a term decision — so it is not made here; see
"decisions for Brett". Until it is made, `make quality-check` and therefore
`make ci` fail on this branch. The other re-enabled rows are WARN
(`annotation_whitespace` × 2, `missing_obsolete_label` × 3) and do not fail
the build; they are recorded in `docs/ROBOT_SETUP.md`.

### Clause 4 — the broken fixtures make the gate fail

`make validation-selftest` (exit 0):

```
🧪 Validation self-test: every gate must fail on its broken fixture

── SHACL gate vs tests/fixtures/shacl/invalid-survey-data.ttl
✅ SHACL gate rejected the invalid fixture
   ✅ reported: Measurement must be about a stock
   ✅ reported: Measurement must be observed during a survey event
   ✅ reported: Count value must be a non-negative integer
   ✅ reported: Count unit must be a valid IRI
   ✅ reported: violates a snorkel-specific constraint
   ✅ reported: Survey must specify an enumeration method
   ✅ reported: Visibility must be one of: Excellent, Good, Fair, Poor
   ✅ not reported: ControlAerial (snorkel-specific constraint stayed snorkel-specific)

── Competency-question runner vs tests/fixtures/sparql/
✅ Competency runner rejected the unparseable query

── ROBOT report gate vs tests/fixtures/robot/duplicate-label.ttl
✅ ROBOT report failed the fixture, and only on duplicate_label (a rule the two-line profile never ran)

── robot-profile.yaml is ROBOT's default profile with two documented downgrades
✅ robot-profile.yaml = default profile (32 rules) with missing_label and missing_definition at INFO; nothing else differs

✅ Validation self-test passed: every gate fails on its broken fixture, for the documented reason.
```

Shown to be load-bearing before wiring in — the self-test **goes red** against
the pre-fix artefacts:

```
$ SHAPES_FILE=<old shapes> ./scripts/validation-selftest.sh
🧪 Validation self-test: every gate must fail on its broken fixture
❌ SHACL report is missing the expected violation: violates a snorkel-specific constraint (see release/tmp/selftest/shacl-invalid-report.txt)
❌ SHACL report names the ControlAerial node: the snorkel-specific constraint fired on an aerial survey, so the conditional shape is not conditional (see release/tmp/selftest/shacl-invalid-report.txt)
❌ Validation self-test found 2 problem(s): at least one gate cannot fail, or fails for the wrong reason.

$ PROFILE_FILE=<old robot-profile.yaml> ./scripts/validation-selftest.sh
🧪 Validation self-test: every gate must fail on its broken fixture
❌ ROBOT report PASSED the duplicate-label fixture with /tmp/claude-0/-home-user-metasalmon/c65e9c22-7ec7-5a53-8177-deda0f189418/scratchpad/B-44/evidence/old-robot-profile.yaml. The profile is not running duplicate_label at ERROR.
❌ /tmp/claude-0/-home-user-metasalmon/c65e9c22-7ec7-5a53-8177-deda0f189418/scratchpad/B-44/evidence/old-robot-profile.yaml differs from ROBOT's default profile in more than the two documented downgrades. Re-derive it from the default (see docs/ROBOT_SETUP.md). Diff (- default, + ours):
     +INFO	missing_definition
     +INFO	missing_label
     -ERROR	deprecated_boolean_datatype
     … (30 more dropped rules) …
❌ Validation self-test found 2 problem(s): at least one gate cannot fail, or fails for the wrong reason.
exit=1
```

### Whole bundle

`make test` (verify-mappings + theme-coverage + alpha-lint + reason +
check-modules + shacl-validate + competency-questions + validation-selftest):
**exit 0**. `make quality-check`: **exit 1** on `duplicate_label` as above.
`git diff --check`: clean.

## What I did not do, and why

- **Not pushed, no pull request opened, no `hub done`, no `hub release`.**
  `dfo-salmon-ontology` belongs to another organization and has other
  contributors, so HUB.md's standing authorization does not cover a push
  there, and this session holds no push credential for it. The work is
  committed locally on `agent/B-44/a-71a6d7c9eefb16bc`; a patch, diffstat and
  pull-request text are in the coordinator's deliverables directory for
  Brett to push and open himself. The claim stays held for him.
- **Did not resolve the `duplicate_label` finding**, for the reason above.
- **Did not retarget the shapes, example data or queries** to the terms the
  ontology now declares (`smn:EscapementSurveyEvent`, `gcdfo:StockManagementUnit`,
  `skos:inScheme gcdfo:WSPBiologicalStatusZoneScheme`, …). Every such
  retarget is a choice of term IRI; recorded as a candidate item below with
  the exact list.
- **Did not run `make docs-refresh` / WIDOCO.** No ontology bytes changed, so
  no generated artifact changes; CI's clean-tree check should be unaffected,
  but I have not demonstrated that here.
- **Did not fetch a sibling `salmon-domain-ontology` checkout**; the catalog
  fell back to the pinned smn commit, which is the CI path anyway.
- **Did not update `docs/CONVENTIONS.md` §2.5**, whose worked SHACL example
  uses the same contradictory `sh:targetClass` + `sh:hasValue` idiom and a
  `dfo:` prefix. Candidate item below.
- **Did not touch the primary clone, `/home/user/metasalmon`, or `queue/`.**
- **The worktree is left in place** — it holds unpushed commits, which fails
  the removal checks in HUB.md, and branch deletion is Brett's call.

## Decisions for Brett (term discipline)

1. **Which label changes.** `gcdfo:WildSalmonPolicy` and `gcdfo:WildSalmonPolicyTheme`
   share `rdfs:label "Wild Salmon Policy"@en`. The gate is red until one
   differs. If the answer is instead "this check should not gate the repo",
   the downgrade goes in three places in one change — `robot-profile.yaml`,
   the expected diff in `scripts/validation-selftest.sh`, and the table in
   `docs/ROBOT_SETUP.md` — and the reason goes with it. I did not pick.
2. **`countUnitIRI`: IRI node or `xsd:anyURI` literal?** The shape now
   accepts either; it narrows the day the property is declared with a range.

## Belongs to another item, or a candidate new item

- **B-0** (docs-widoco baseline): not touched. Nothing here goes near
  `docs-widoco`; I did not run it.
- **Candidate new item (P2, term decision, `claimable: false`): the shapes,
  example data and competency queries name 13 + 3 + 4 IRIs the ontology does
  not declare.** Full list in the new `docs/tech-debt.md` entry. Retargeting
  or minting is per-term (the repo's own red-list rule against bulk swaps);
  once done, flip `scripts/run-competency-questions.sh` to fail on zero rows
  (its header says where) and add a declared-IRI assertion to the self-test.
  Until then the SHACL gate is a consistency check between two files, and the
  competency gate proves parse-and-run only. Both say so in their output.
- **Candidate new item (P3): `docs/CONVENTIONS.md` §2.5** shows the
  contradictory snorkel idiom with a `dfo:` prefix; `docs/ADR.md` ADR-005
  still says SHACL is for "automated classification"; both predate this fix.
- **Candidate new item (P3): the 3 `missing_obsolete_label` and 2
  `annotation_whitespace` WARN rows** — trivial to fix, but the whitespace
  ones edit definition strings and the obsolete-label ones edit labels, so
  they are term edits and were left alone.

## Guards, skips and workarounds added — and what retires each

- **`scripts/run-competency-questions.sh` does not fail on zero rows.**
  Retires when every query under `ontology/sparql/competency/` returns at
  least one row against the example data (the term-decision item above); the
  header names the line to flip. Stated in the script, `docs/entrypoints.md`,
  `docs/tech-debt.md`, ADR-010.
- **`countUnitIRI` accepts IRI node OR `xsd:anyURI` literal.** Retires when
  `gcdfo:countUnitIRI` is declared with a range. Stated in the shapes file.
- **`missing_label` and `missing_definition` at INFO in `robot-profile.yaml`.**
  Pre-existing downgrades, kept; now the *only* two, enforced by the
  self-test. Retire when ROBOT reads `skos:prefLabel` / `iao:0000115`, or the
  ontology mirrors both into what ROBOT reads. Stated in `docs/ROBOT_SETUP.md`.
- **The profile-provenance check assumes ROBOT's default has no native amend
  option.** Retires when ROBOT can amend rather than replace the default
  profile; then the file holds only the two overrides and checks 3–4 of the
  self-test go with it. Stated in the self-test header.
- **The three fixtures.** Each header says not to fix it and what retires it
  (the gate it proves being removed or proven another way).
- **`.hub/workpad.md`** is this file; Brett may drop the commit that adds it
  before merging.
