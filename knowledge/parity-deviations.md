---
type: InformationObject
title: "Parity deviations — metasalmonpy vs metasalmon"
description: "The hub twin of metasalmonpy's PARITY.md: every deliberate difference between the R package and its Python mirror, with kind and rationale. The two registers must agree."
status: draft
tags: [parity, mirror, s10]
psc:
  id: metasalmon:parity-deviations
  contexts: [metasalmon:context:hub-coordination]
---

# Parity deviations — metasalmonpy vs metasalmon

The mirror contract requires the same behaviour and capabilities at the
version metasalmonpy claims — **not** literal API mimicry (Brett,
2026-08-15): do not force 100% parity where it would be unintuitive for
Python users, and simple language differences that do not materially change
behaviour or capability are fine. Every deliberate difference is recorded
here **and** in metasalmonpy's `PARITY.md`; the two registers must agree. An
undocumented difference is a contract violation even when the difference
itself is fine.

Kinds of entries: **Idiom** (same behaviour, Pythonic delivery), **Ahead**
(Python already has semantics R adopted later), **Inapplicable** (no Python
counterpart, with the why), and **Gap** (a capability metasalmon has and
metasalmonpy does not — never a licence to stay behind, so a Gap row names
the milestone that closes it, or says outright that none does).

An **Ahead** row is marked *converged* once metasalmon ships the semantics.
The row stays and keeps its number: metasalmonpy still claims an earlier
parity milestone, and the row is what tells a reader the difference is era
lag rather than a standing choice. Row numbers are permanent in both
registers so the two can be cross-referenced by number.

| # | Kind | Difference | Why |
|---|---|---|---|
| 1 | Idiom | Errors are Python exceptions with actionable messages, not R cli conditions | Language idiom; same conditions trigger them |
| 2 | Idiom | Optional dependencies are extras (`metasalmonpy[eml]`, `[knb]`), not Suggests | Packaging idiom; same lazy-guard behaviour and install pointers |
| 3 | Idiom | Canonical ordering uses codepoint `sorted()`, not explicit C-collation flags | Python's default sort is already locale-independent; the contract (locale-independent deterministic output) is identical, and `locale.strxfrm` is banned |
| 4 | Idiom | Archive/EML/ORE parity is contract-level (structure, manifest, ordering, fail-closed), never byte-level | R's bytes come from zip-3.0.1/libxml2 formatters Python cannot and should not reproduce |
| 5 | Ahead | `infer_value_type` is public API in Python, internal in R | Was already exported at 0.1.6; removing it would break users for no capability gain |
| 6 | Ahead | A failed or empty ontology fetch raises in Python; 0.1.6-era R returned an empty index | Failed lookup ≠ empty lookup; R adopted the same principle at 0.2.2 |
| 7 | Ahead | `is_statistical_modifier` is a real column in both Python term-index frames; R's TTL path carries it only inside `role_hints` | Saves the 0.3.0 milestone a retrofit; hint strings match R exactly |
| 8 | Inapplicable | No interactive term-request console in Python, so R 0.2.0's cancel-must-not-submit semantics have no counterpart | Submission is a single explicit function call with confirmation |
| 9 | Inapplicable (landed 0.1.8) | `write_sdp_methods` is not implemented in Python; the registry is read and validated only | **Landed, not planned** (verified 2026-08-16): the name *is* exported (`__init__.py`, `sdp_methods.py:675`) and raises `NotImplementedError` carrying the reason, so a user who goes looking for the R function gets the explanation rather than an `AttributeError` — the deliberate shape, pinned by `tests/test_sdp_methods.py::test_the_writer_is_absent_and_says_why`. The writer would exist only to be deleted at 0.3.0 in the same replay, when SDP 0.3.0 removes `metadata/methods.csv` and `migrate_sdp_methods()` replaces it; the reader and validator survive that transition because a migration needs them. Logged in the [S10 execplan](plans/2026-08-15-s10-metasalmonpy-parity-replay.md). **Retirement condition:** delete the stub and its test at the 0.3.0 rung, when the registry stops existing |
| 10 | Idiom | The SSSOM header is parsed with a restricted YAML-subset parser, not a YAML library (R uses `yaml::yaml.load`) | pandas+requests dependency policy; out-of-subset YAML raises the same "not valid YAML" report, and duplicate-key/quoting/comment behaviours are differentially tested against R |
| 11 | Idiom | The SSSOM manifest provenance block names `metasalmonpy.write_sdp_sssom` + its version; R names its own. Each validator accepts either implementation's provenance | Provenance should be honest about which implementation wrote the artifact; the TSV mapping-set bytes stay byte-identical across languages |
| 12 | Idiom | The `measurement-decompositions.json` provenance block names `metasalmonpy.write_sdp_measurement_decompositions` + its version; each validator accepts either implementation's provenance | The row-11 honest-provenance ruling applied to the 0.1.7 decomposition artifact; the artifact binding (path, sha256, row_count) and the decomposition CSV bytes stay byte-identical across languages |
| 13 | Ahead | Python's SSSOM and decomposition writers treat a dangling symlink at a managed output path as existing (blocked without `overwrite`, then refused as a symlink); R's `file.exists()` misses dangling symlinks and silently writes through them | Fail-closed writer safety; read-side symlink handling matches R exactly |
| 14 | Idiom | The EML mapping sidecar is parsed with PyYAML (in the `[eml]` extra), not the restricted YAML-subset parser used for SSSOM headers | The sidecar is genuinely nested (creators, contacts, per-table attribute configs), unlike the flat SSSOM header; R uses `yaml::read_yaml` here too. Core dependencies stay pandas+requests |
| 15 | Idiom | EML documents are built with stdlib ElementTree and validated with lxml; parity with R is structural (`ET.canonicalize`), not byte-level | R serializes through libxml2, so whitespace differs while the documents are identical. lxml IS libxml2 — the engine behind `emld::eml_validate` — so accept/reject matches R by construction, and all UUIDv5 identifiers match exactly |
| 16 | Idiom | The KNB adapter speaks the DataONE v2 REST API directly with `requests`; R's `.ms_knb_default_adapter()` delegates to `dataone`/`datapack`. Same fourteen-method boundary as `.ms_knb_required_adapter_methods()`, injected through `set_knb_adapter()` / `set_dataone_token()` rather than R's `metasalmon.knb_adapter` / `dataone_token` options | There is no maintained Python `dataone`/`datapack`, and R already bypasses them for the ORE, the anonymous probes, the format registry and the Solr catalog. Four behaviour-preserving consequences of speaking REST directly: the CN base URL is an explicit Python constant where R resolves it through `D1Client`; `update_object` uses the spec resource path `PUT /object/{oldPid}` where `dataone` PUTs to `/object` with `pid` as a form field; Python fills `authoritativeMemberNode` in its system-metadata builder because R leaves `.ms_knb_new_system_metadata()`'s `node_id` argument unused and lets `dataone::createObject()` attach it at the upload boundary; and JWT claims are decoded with stdlib base64+json, matching R's unverified local check (the real check is server-side `echoCredentials`), so no PyJWT |
| 17 | Idiom | The deterministic SDP archive carries its own Python determinism reference (`metasalmonpy-zipfile-1`: radix member order, 2000-01-01 timestamps, 0644, Unix `create_system`, no directory entries, deflate level 9) instead of R's reviewed-`zip`-version guard, and its bytes are not comparable to R's | Row 4 applied to the archive: miniz (R's `zip`) and zlib emit different deflate streams, so byte parity is impossible without vendoring a compressor. R's guard is `.ms_knb_reviewed_zip_versions` in `R/knb-sdp-archive.R` — an allowlist of reviewed versions with a runtime abort, so a compressor change stays a reviewed package change, which matters because the archive checksum is an immutable DataONE PID input. Python keeps that purpose by asserting the reference sha256 in its KNB test fixtures, but has no runtime abort because zlib ships with the interpreter and cannot be pinned per platform. **Twin correction owed:** `PARITY.md` row 17 still describes R's guard as a single `zip` 3.0.1 pin; it has been a two-version allowlist since the guard was written |
| 18 | Idiom | Because the archive (row 17), the EML document (row 15) and the ORE document (row 4) differ from R's *bytes*, an end-to-end Python plan for the same SDP yields a different archive PID, resource-map PID and `plan_sha256` than R's | Unavoidable and disclosed, not silent: every algorithm that derives those values is pinned against R by driving the Python planner from R's own member table (R-generated fixtures committed on the Python side). The recovery-manifest JSON and the fingerprint payload are compared as literal bytes; the ORE graph and the SystemMetadata documents are `ET.canonicalize`-equal, not byte-equal — `PARITY.md` row 18's blanket "byte-exact … the ORE graph" overstates that one item, and row 16's wording there is the honest one. A manifest written by one implementation is therefore reviewed and published by that same implementation, exactly as DataONE immutability already requires |
| 19 | Idiom | `publish_sdp_to_knb(confirm = ...)` defaults to `None`, not R's `confirm = interactive()` plus `missing(confirm)` pair | Verified equivalent, not merely analogous: R's `confirm_missing` short-circuit makes the `interactive()` default inert, so an omitted `confirm` aborts in an interactive session too — and both sides demand exactly `TRUE`/`True` (R's `isTRUE()`, Python's `is True`), so a truthy `1` or `"yes"` authorizes nothing on either side. A single `None` sentinel expresses in one place what R needs two halves to guarantee: live mode proceeds only when the caller passed exactly `TRUE` |
| 20 | Ahead (converged) | Canonical SSSOM ordering (curie-map prefixes, mapping-set ids) is codepoint/C in Python; era R (0.1.7/0.1.8) used bare `sort()`/`order()`, so its bytes and manifest order varied with `LC_COLLATE` | **Converged — and one release earlier than the twin claims.** metasalmon adopted C collation at **0.2.0** ("Canonical bytes and identifiers no longer depend on `LC_COLLATE`"), not at 0.3.0 as `PARITY.md` row 20 states; `R/sssom.R` now orders with `method = "radix"` at every site that reaches bytes or the manifest. Current R and Python agree. The interop hazard the row records is live only against **era R (≤ 0.1.8)**: an era-R package written in a non-C locale can fail the Python manifest-ordering check and vice versa, demonstrated both directions |
| 21 | Ahead (converged) | Every Python SDP reader preserves a literal `"NA"`; only the empty field is missing. Era R used readr's `na = c("", "NA")` | **Converged:** `.ms_csv_na_token()` (`R/package-helpers.R`) returns `""` and is the sole token for both readers and the resource writer — metasalmon **0.2.4**'s canonical missing-value contract, on the same rationale ("NA" is a real fisheries gear code). Python forward-ported it early because era behaviour combined with the already-ported reader made a literal-`"NA"` vocabulary row unsatisfiable: Python could not export a package R could read. Residual difference is representation, not the token set — an empty field parses to `NA_character_` in R and to `""` in Python |
| 22 | Interim (0.1.7 only) | Consequence of row 21: Python's `_ERA_NA_TOKENS` (era `("", "NA")`) still governs the EML raw-token audit, so that audit models era R's parse of a **data resource** while Python's own readers keep a literal `NA` as data | An internal split inherited from applying row 21 at the 0.1.7 milestone; it disappears when the replay reaches 0.2.4. **The twin's safety claim no longer holds:** `PARITY.md` row 22 says the split "cannot make an audit verdict disagree with R's", which was true of era R but is false against 0.3.0. Current R's `.ms_eml_add_missing_values()` derives missingness from `is.na(parsed_values)`, and with `na = ""` a literal `NA` cell is not missing — so R accepts a cell that Python rejects as an undeclared non-empty missing token. A live divergence, not a dormant one |
| 23 | Idiom | `trim_ws` is an approximation of readr rather than a transliteration: `skipinitialspace=True` plus a strip of readr's trim characters (`" \t\r\n"`) | Narrow enough to document rather than chase. The probed field shapes, the header cases and the beyond-ASCII non-cases (U+00A0, U+3000 left alone) are pinned in the Python reader tests. `PARITY.md` row 23 names one known mismatch — a leading **tab** before an opening quote (`\t"z"\t`), which readr unquotes and pandas does not — but **that shape appears in no test and no code comment on either side**, so nothing would catch it moving. Recorded here as an asserted boundary, not a verified one; pinning it is cheap and should happen at the next touch |
| 24 | Idiom | Atomic writes go through a Python-only `atomic_io` module so artifacts land at the umask default mode; R has no counterpart and each writer inlines `writeBin` + `file.rename` | R gets umask behaviour free from `writeBin`; Python's `tempfile.mkstemp()` hard-codes 0600, which would otherwise have shipped owner-only semantic artifacts. The module **measures** the default mode — one throwaway `os.open(..., 0o666)` then `fstat`, so the kernel has already subtracted the umask — instead of mutating and restoring the process umask, which is a per-process race. The mechanism changed at 0.1.7; the deviation (the module exists at all) did not |
| 25 | Ahead (partly converged) | Python's `read_salmon_datapackage()` returns every resource column as a string, preserving the raw token; era R type-inferred resources with `readr::read_csv(file_path)` | **The text read converged at 0.2.0:** `.ms_read_resource_csv()` reads with `col_types = list(.default = col_character())`, and the dictionary became the sole type authority. **The typing did not.** R 0.3.0 goes on to convert each dictionary-declared column in memory, keeping the raw token as ground truth and raising a structured validation issue when a conversion is not faithful; Python stops at text with no conversion step, so a caller gets `str` where R gives a typed column. That half is an open capability gap for the 0.2.0 rung rather than an idiom, and it is the direction of travel the row already anticipated. Verified consequence on both sides: a whitespace-padded resource header is trimmed, so Python's `_validate_raw_table` aborts on it exactly as `.ms_eml_validate_raw_table()` does |
| 26 | Idiom | Python's `_is_canonical_rights_url()` expresses R's `httr2::url_parse()` / `url_build()` round-trip test as explicit conditions instead of reimplementing curl's URL normalizer | R's `.ms_license_descriptor()` (`R/package-helpers.R`) accepts a custom rights URL only when it is already in curl's canonical form — unchanged at 0.3.0. Python has no curl URL parser and vendoring one for a licence check is disproportionate, so the same rule is written as the things curl would otherwise change: an `http`/`https` scheme in lowercase, non-empty netloc and hostname, non-empty path, no whitespace, no dot segments, and a `urlsplit`/`urlunsplit` round trip. Verdicts match era R on every pinned probe value — **21 of them, where `PARITY.md` row 26 says twenty**. Residual risk is confined to exotic normalization (IDN hosts, percent-encoding case) that no SDP licence field has carried |
| 27 | Idiom (era lag) | The SDP profile version is the named constant `metadata.SDP_PROFILE_VERSION` in Python; R resolves it through `.ms_sdp_profile_version()` | The single-source-of-truth property matches and one constant is better than the three scattered literals it replaced. Two things the twin's "same observable value" no longer covers. **The values differ: R 0.3.0 declares `sdp-0.3.0`, Python still emits `sdp-0.2.0`** into `dataset.csv$spec_version` and `datapackage.json`'s `sdp.specVersion` — correct for a 0.1.7-era parity claim, and era lag rather than a standing deviation, but it means the two implementations now stamp different spec versions. **The R mechanism also moved:** `.ms_sdp_profile_version()` (`R/schema-helpers.R`) no longer reads the vendored `inst/extdata/schema/sdp.rules.yaml` directly; it returns `.ms_load_sdp_schema()$version`, remote-first against the pinned spec tag with the vendored bundle as fallback. **The old retirement condition is discharged** (verified 2026-08-16): `SDP_PROFILE_VERSION` is no longer a literal — `metadata.py:103` computes it by calling `sdp_schema.sdp_profile_version()`, which reads the vendored `sdp-0.2.0` bundle 0.1.8 landed. How it reads that bundle is now its own deviation, row 30. **New retirement condition:** the era lag closes when the replay reaches the 0.3.0 rung and swaps the vendored bundle, at which point both sides stamp `sdp-0.3.0` |
| 28 | Idiom | R's TRE-resolved `[[:space:]]` / `[[:cntrl:]]` memberships are enumerated once in Python's `metadata` module (`R_SPACE_CLASS`, `R_CNTRL_CLASS`) and imported by its `eml` and `sssom` modules; R re-writes the POSIX class at each call site | The enumeration is the load-bearing part, and duplicating it is how the Python `sssom` module drifted from `eml` in the first place. **Retirement condition:** the constants stand while metasalmon resolves those classes through TRE; if `R/eml-export.R` or `R/sssom.R` ever switch those validators to `perl = TRUE`, the replacement membership must be re-enumerated against that release rather than guessed — PCRE's `[[:space:]]` is ASCII-only, which is why the measurement-decomposition validators correctly keep an ASCII class on both sides. **Noticed while verifying, and since resolved on the R side:** `.ms_sdp_extension_is_absolute_iri()` (`R/sdp-extension-helpers.R`) applied `perl = TRUE` to the same `[^[:space:]]` shape that `R/eml-export.R` runs under TRE. Fixed post-0.3.0 (backlog #85) by dropping `perl = TRUE`, which is the direction this row's retirement condition requires — the shape now has one R owner, `.ms_absolute_iri_shape()` (`R/iri-predicates.R`), called by the SDP-extension, EML and SSSOM validators, so **the "R re-writes the POSIX class at each call site" half of this row no longer holds**: both sides now define the membership once. EML and SSSOM behaviour did not move, so the enumeration stays valid and this row does not retire. **This row was wrong to say "Python mirrors no such function"** — see row 33 |
| 29 | Idiom (asymmetric) | The `reproducibility/manifest.json` provenance block names `metasalmonpy.write_sdp_reproducibility_manifest` + its version; R names its own | The row-11 honest-provenance ruling applied to the 0.1.8 reproducibility manifest, and the byte claim holds: for the same declared artifact set the two manifests differ **only** in those two provenance values — profile string, radix `path` ordering, `sha256`, `size_bytes` and the JSON encoding all match, because `jsonlite::toJSON(pretty = TRUE)` and `json.dumps(indent = 2)` agree on two-space indentation. Pinned from R fixtures, not asserted: `tests/test_reproducibility.py::test_manifest_bytes_match_r_apart_from_provenance` swaps the provenance object back and demands R's exact bytes, over a committed v0.1.8 fixture whose own sha256 is checked. **But dual acceptance is one-way here, unlike rows 11 and 12.** Python's `_ACCEPTED_PROVENANCE` takes either writer; R's `.ms_sdp_reproducibility_validate_manifest()` (`R/reproducibility-manifest.R:298`) still demands `identical(generated_by, "metasalmon::write_sdp_reproducibility_manifest")` and a `metasalmon_version`, where `R/sssom.R:1028` and `R/measurement-decompositions.R:696` accept both. So a Python-written reproducibility manifest is **rejected by metasalmon** — the honest-provenance ruling was applied to the writer without its matching read-side half, which is an R-side defect (backlog #88), not a Python deviation. **Retirement condition:** R's reproducibility validator accepts `metasalmonpy.write_sdp_reproducibility_manifest` + `metasalmonpy_version` exactly as the SSSOM and decomposition validators already do; then this row is a plain row-11 sibling |
| 30 | Idiom | `sdp_schema.sdp_profile_version()` line-scans the vendored `sdp.rules.yaml` for its one top-level `version:` scalar; R parses the document | Core dependencies stay pandas + requests, and pulling PyYAML in for one scalar is disproportionate (it stays in the `[eml]` extra, row 14). The document is machine-generated by `smn-data-pkg`'s `scripts/generate_artifacts.py` and has carried `version:` as a plain top-level scalar in every published version, and the scan **raises rather than guessing** when the key is absent, so a format change surfaces as an error and not a silently stale version. Worth stating from this side: R's full parse is not the stylistic half of the difference. `.ms_load_sdp_schema()` (`R/schema-helpers.R`) needs the whole bundle anyway, and it **cross-checks** `rules$version` against the profile's `sdp:version`, aborting when they disagree — a bundle whose two halves declare different versions is rejected. A line scan cannot make that check and Python makes it nowhere else, so the narrow reader also trades away an internal-consistency guard, not just a dependency. **Retirement condition:** replace the scan with a full parse the day the mirror needs any *other* field of `sdp.rules.yaml` — PyYAML is justified by then and the narrow reader becomes a liability |
| 31 | Idiom (stricter) | `apply_semantic_suggestions(strategy = "llm")` requires the frame to carry `llm_selected`, `llm_decision` **and** `llm_confidence`, and applies only rows with `llm_selected` truthy *and* `llm_decision == "accept"`; R requires only `llm_selected` and filters on it alone | Pre-existing and previously unregistered. Equivalent on any frame the pipeline produces, and R is the reason why: `.ms_llm_validate_assessment_response()` (`R/llm-semantic-helpers.R`) ends with `if (!identical(decision, "accept")) selected_index <- NA_integer_`, so a non-accept decision can never carry a selected index, and `llm_selected` (`R/semantic-suggestions.R:1152`) is derived from that index. R already guarantees the invariant Python re-checks. The two differ only on a **hand-built** frame that sets `llm_selected` without an accepting decision — R applies it, Python refuses rather than writing an unreviewed IRI into the dictionary — and on column presence, since R needs only the one column while Python demands all three. Both are safe directions for a mirror to err in, but neither is a behaviour metasalmon has |
| 32 | **Gap (open, no milestone owns it)** | Python's `_score_and_rank_terms` (`term_search.py:955`) has **no counterpart to R's ranking-profile system**: no `ranking_profile` argument, no `.merge_ranking_profile()`, no `.ranking_profile_defaults()`, and every tunable hardcoded. The hardcoded values also differ from R's defaults, so the same candidate set ranks differently | **Not an idiom and not settled — an unowned open gap, recorded so it stops being invisible.** It predates the 0.1.6 parity claim, was never registered, and is **not in any S10 rung's scope**; backlog #87 carries it. Found while landing the 0.1.8 `match_type` fix, which is real and does match `.match_type_score_profiled()` on every probed value — the surrounding divergence is what the fix exposed. Verified against `R/term_search.R:2040` and `:2222`, and it is wider than the source weights: **base weights** differ (`smn` 1.2 vs R's 1.0, `gcdfo` 1.0 vs 0.9) and so does the unknown-source fallback (Python 0, R 0.1); **role boosts** differ for every role but `unit` (e.g. `variable`: R `smn` 1.7/`gcdfo` 1.3/`nvs` 1.0/`ols` 0.4/`zooma` 0.4, Python `smn` 1.5/`gcdfo` 1.0/`nvs` 0.6/`ols` 0.2/`bioportal` 0.4); the **vocabulary bonuses** differ in both magnitude and gating (R applies `host_bonus` 0.8 / `slug_bonus` 0.8 / `label_pattern_bonus` 0.4 *only inside* the role-preferences branch, Python adds 1 / 1 / 0.5 unconditionally); and R's `backend_score` term has no Python counterpart at all. **The sharpest part is a live trap, not an absence:** `benchmark_term_ranking_fixtures(profiles = ...)` is exported on *both* sides, but Python's binds each profile to `_profile` and never passes it to the scorer, then echoes the profiles back in its result. Benchmarking two profiles there returns two identical summary rows under different names — the one exported surface for comparing profiles silently reports that all profiles are equivalent. **Retirement condition:** `_score_and_rank_terms` takes a profile argument merged over a Python `.ranking_profile_defaults()` whose values are read back from the R release being claimed, `benchmark_term_ranking_fixtures` actually threads `profiles` through, and a differential fixture pins the two rankers to the same order — until then, treat any Python ranking result as unpinned against R |
| 33 | Gap (open) | `metasalmonpy/sdp_methods.py:95` `_is_absolute_iri()` — whose docstring names `.ms_sdp_extension_is_absolute_iri` as the thing it mirrors — matches whitespace with Python's `\s` (`_ABSOLUTE_IRI_RE`, `sdp_methods.py:67`; `_HTTP_AUTHORITY_RE`, `:69`) instead of importing `R_SPACE_CLASS`. Python's `\s` on a `str` pattern is Unicode-aware but is **not** the same set as TRE's `[[:space:]]` | Not a deliberate deviation — the same drift row 28 says it already fixed once in `sssom.py`, in the one extension module that never imported the constants. Measured, not guessed, on R 4.5.2 by running both engines over U+0001–U+3100: **against R post-#85 the two disagree on exactly 8 codepoints — U+001C, U+001D, U+001E, U+001F, U+0085, U+00A0, U+2007, U+202F — with Python rejecting every one that R accepts.** Python is the stricter side, so the risk is a Python-written SDP-extension IRI being refused by the mirror and accepted by R, not the reverse. **This gap narrowed rather than widened at #85:** against R *before* the fix the disagreement was 23 codepoints, so aligning R on TRE cut it to 8. **Retirement condition:** `sdp_methods.py` imports `R_SPACE_CLASS` from `metadata` and builds both regexes from it, exactly as `eml.py:92` and `sssom.py:234` already do, plus a whitespace-membership test in `tests/test_sdp_methods.py` matching the ones in `tests/test_eml.py` and `tests/test_sssom.py` (which is where the absence went unnoticed). Then this row is deleted rather than converged — there is nothing here worth keeping |

Rows 16–28 were verified against metasalmon 0.3.0 and metasalmonpy 0.1.7 on
2026-08-16 by reading both implementations, not by transcribing the twin.
Rows 20, 21 and 25 had converged in whole or in part and now say so; rows 17,
18, 20, 22, 23, 26 and 27 record a point where `PARITY.md` and this register
would otherwise disagree, and the twin is the side that needs correcting.

**Row 33 was row 29 until 2026-08-17.** Two streams claimed number 29
independently: this register took it for the `sdp_methods.py` whitespace gap,
and metasalmonpy's 0.1.8 work took it for the reproducibility-manifest
provenance row, plus 30–32 behind it. Brett resolved it in metasalmonpy's
favour — four committed rows against one — so the whitespace row moved to 33
and keeps that number permanently. Rows 29–32 were verified here against
metasalmon 0.3.0 and metasalmonpy 0.1.8 on 2026-08-17 by reading both
implementations rather than mirroring the twin's wording, and that verification
moved two of them: row 29's dual-provenance acceptance turned out to be
**one-way** (R still rejects a Python-written reproducibility manifest, where
rows 11 and 12 accept both), and row 32 is wider than a weights difference —
it reaches role boosts, vocabulary bonuses, the unknown-source fallback, and
an exported benchmark argument that is accepted and discarded.

Row 33 was added post-0.3.0 while fixing backlog #85, and it corrects row 28's
claim that "Python mirrors no such function" — it does, in `sdp_methods.py`.
That claim was made from the R side without reading the Python extension
modules; the register now records the gap the claim concealed. Rows 9 and 27
were re-verified at the same time: row 9's writer decision **landed** at
metasalmonpy 0.1.8 as an exported raising stub rather than remaining planned,
and row 27's retirement condition is **discharged** — the profile version is
now read from the vendored bundle, which is what row 30 then describes.

Maintenance: a new deviation is added in the same PR that introduces it, in
both registers, in the same stream.
