---
type: InformationObject
title: "Parity deviations — metasalmonpy vs metasalmon"
description: "The hub twin of metasalmonpy's PARITY.md: every deliberate difference between the R package and its Python mirror, with kind and rationale. The two registers must agree."
status: draft
tags: [parity, mirror, s10]
psc:
  id: metasalmon:parity-deviations
  contexts: [metasalmon:context:hub-coordination]
---

# Parity deviations — metasalmonpy vs metasalmon

The mirror contract requires the same behaviour and capabilities at the
version metasalmonpy claims — **not** literal API mimicry (Brett,
2026-08-15): do not force 100% parity where it would be unintuitive for
Python users, and simple language differences that do not materially change
behaviour or capability are fine. Every deliberate difference is recorded
here **and** in metasalmonpy's `PARITY.md`; the two registers must agree. An
undocumented difference is a contract violation even when the difference
itself is fine.

Kinds of entries: **Idiom** (same behaviour, Pythonic delivery), **Ahead**
(Python already has semantics R adopted later), **Inapplicable** (no Python
counterpart, with the why), and **Gap** (a capability metasalmon has and
metasalmonpy does not — never a licence to stay behind, so a Gap row names
the milestone that closes it, or says outright that none does).

All four are written **one-directionally**, from R towards Python, and row 43
is the first difference that points the other way: public surface metasalmonpy
has and metasalmon never had. Whether the taxonomy gains a fifth Kind or
*Inapplicable* is read symmetrically with the direction stated in the row is
**open**; row 43's label is a placeholder saying which way it points, not a
ruling.

An **Ahead** row is marked *converged* once metasalmon ships the semantics.
The row stays and keeps its number, and the reason has changed with the
mirror's state. It used to be that metasalmonpy claimed an earlier parity
milestone, so the row told a reader the difference was era lag rather than a
standing choice. **Since 2026-08-24 both packages claim 0.4.0 and there is no
era lag** — so a converged row is now kept for the *history*: it records that
the two sides once differed here, which side was right, and which one moved.
Delete it and the next divergence on the same surface looks like a first
occurrence. Row numbers are permanent in both registers so the two can be
cross-referenced by number.

| # | Kind | Difference | Why |
|---|---|---|---|
| 1 | Idiom | Errors are Python exceptions with actionable messages, not R cli conditions | Language idiom; same conditions trigger them |
| 2 | Idiom | Optional dependencies are extras (`metasalmonpy[eml]`, `[knb]`), not Suggests | Packaging idiom; same lazy-guard behaviour and install pointers |
| 3 | Idiom | Canonical ordering uses codepoint `sorted()`, not explicit C-collation flags | Python's default sort is already locale-independent; the contract (locale-independent deterministic output) is identical, and `locale.strxfrm` is banned |
| 4 | Idiom | Archive/EML/ORE parity is contract-level (structure, manifest, ordering, fail-closed), never byte-level | R's bytes come from zip-3.0.1/libxml2 formatters Python cannot and should not reproduce |
| 5 | Ahead | `infer_value_type` is public API in Python, internal in R | Was already exported at 0.1.6; removing it would break users for no capability gain |
| 6 | Ahead | A failed or empty ontology fetch raises in Python; 0.1.6-era R returned an empty index | Failed lookup ≠ empty lookup; R adopted the same principle at 0.2.2 |
| 7 | Ahead | `is_statistical_modifier` is a real column in both Python term-index frames; R's TTL path carries it only inside `role_hints` | Saves the 0.3.0 milestone a retrofit; hint strings match R exactly |
| 8 | Inapplicable | No interactive term-request console in Python, so R 0.2.0's cancel-must-not-submit semantics have no counterpart | Submission is a single explicit function call with confirmation |
| 9 | Ahead — reversed at chunk A (metasalmonpy keeps registry *read* surface metasalmon removed) | metasalmon 0.3.0 removed `write_sdp_methods()`, `read_sdp_methods()` and `validate_sdp_methods()` outright with the registry. In metasalmonpy **the reader and validator survive** as legacy read support, and the writer stub survives with its message moved to the past tense ("SDP 0.3.0 removed the registry", pointing at `migrate_sdp_methods()`) | The original row (written 2026-08-17 as a writer-only skip at 0.1.8 parity) predicted exactly this shape, and S10 chunk A landed it (metasalmonpy PR #14, 2026-08-22). metasalmonpy receives packages written by metasalmon 0.2.x that carry a registry, and 0.1.8-era EML documents quote procedures out of one, so the read surface is a real data-compatibility capability — its `SDP_METHODS_COLUMNS` is now a frozen legacy contract rather than a read of the vendored bundle, which no longer defines a `methods` table. Every *current-package* surface treats a lingering `metadata/methods.csv` exactly as R does: an error pointing at the migration (`validate_salmon_datapackage`, EML export, KNB publication). The migration itself is mirrored 1:1 against metasalmon `main` (`e02111a`) — nine-case differential, identical stop taxonomy, byte-identical rewrites — with row-1-family idioms only: the report is a dict of DataFrames instead of an invisible list, informational messages print to stdout instead of cli conditions, stops raise `SdpExtensionError`, and `dry_run` must be a real `bool` (mirroring R's `is.logical()` check). **Retirement condition:** the reader, validator and stub go together at a rung permitted to break metasalmonpy's own callers, once legacy 0.2.x packages stop circulating; the stub goes first if the ecosystem ever reinstates per-package registries, because it is the place the writer would return to |
| 10 | Idiom | The SSSOM header is parsed with a restricted YAML-subset parser, not a YAML library (R uses `yaml::yaml.load`) | pandas+requests dependency policy; out-of-subset YAML raises the same "not valid YAML" report, and duplicate-key/quoting/comment behaviours are differentially tested against R |
| 11 | Idiom | The SSSOM manifest provenance block names `metasalmonpy.write_sdp_sssom` + `metasalmonpy_version`; R names its own. Each validator accepts either implementation's provenance, and metasalmon has accepted metasalmonpy's since PR #43 | Provenance should be honest about which implementation wrote the artifact; the TSV mapping-set bytes stay byte-identical across languages, as do the manifest's entry fields, its sha256 binding and its radix `mapping_set_id` ordering. **Since metasalmonpy's 0.4.0 port the accepted writer set has one owner on each side** — `R/provenance.R` here, `provenance.py` in the mirror — rather than the same pair of literals re-typed once per validator; row 29 records what that re-typing cost. **One weakness is shared deliberately, and is not an oversight:** both SSSOM validators ask only that the version field be *present*, where their four siblings demand one non-blank string. `.ms_sssom_validate_manifest()` therefore does **not** call `.ms_manifest_provenance_version_ok()`, because metasalmonpy's `sssom.py` asks exactly `provenance.get(version_key) is None` and the two readers of one artifact must accept the same manifests — a unilateral tightening on either side would reject a manifest the other still writes. *Retires when:* both SSSOM validators tighten to the non-blank shape, in the same stream |
| 12 | Idiom | The `measurement-decompositions.json` provenance block names `metasalmonpy.write_sdp_measurement_decompositions` + `metasalmonpy_version`; each validator accepts either implementation's provenance | The row-11 honest-provenance ruling applied to the 0.1.7 decomposition artifact (metasalmon's half landed in PR #44); the artifact binding (path, sha256, row_count) and the decomposition CSV bytes stay byte-identical across languages — **pinned** in the mirror against R-generated fixtures in `tests/data/decompositions/` rather than asserted. **Both sides resolve their accepted writers through one owner since metasalmonpy's 0.4.0 port** (`R/provenance.R` / `provenance.py`), and both demand one non-blank string for the version — the tighter of the two shapes row 11 describes, and the shape this artifact has always had on the R side |
| 13 | Ahead | Python's SSSOM and decomposition writers treat a dangling symlink at a managed output path as existing (blocked without `overwrite`, then refused as a symlink); R's `file.exists()` misses dangling symlinks and silently writes through them | Fail-closed writer safety; read-side symlink handling matches R exactly |
| 14 | Idiom | The EML mapping sidecar is parsed with PyYAML (in the `[eml]` extra), not the restricted YAML-subset parser used for SSSOM headers | The sidecar is genuinely nested (creators, contacts, per-table attribute configs), unlike the flat SSSOM header; R uses `yaml::read_yaml` here too. Core dependencies stay pandas+requests |
| 15 | Idiom | EML documents are built with stdlib ElementTree and validated with lxml; parity with R is structural (`ET.canonicalize`), not byte-level | R serializes through libxml2, so whitespace differs while the documents are identical. lxml IS libxml2 — the engine behind `emld::eml_validate` — so accept/reject matches R by construction, and all UUIDv5 identifiers match exactly |
| 16 | Idiom | The KNB adapter speaks the DataONE v2 REST API directly with `requests`; R's `.ms_knb_default_adapter()` delegates to `dataone`/`datapack`. Same fourteen-method boundary as `.ms_knb_required_adapter_methods()`, injected through `set_knb_adapter()` rather than R's `metasalmon.knb_adapter` option, and each environment's JWT is supplied through its own setter — `set_dataone_token()` / `set_dataone_test_token()` — where R reads the `dataone_token` / `dataone_test_token` options | There is no maintained Python `dataone`/`datapack`, and R already bypasses them for the ORE, the anonymous probes, the format registry and the Solr catalog. Four behaviour-preserving consequences of speaking REST directly: the CN base URL is an explicit Python value where R resolves it through `D1Client`; `update_object` uses the spec resource path `PUT /object/{oldPid}` where `dataone` PUTs to `/object` with `pid` as a form field; Python fills `authoritativeMemberNode` in its system-metadata builder because R leaves `.ms_knb_new_system_metadata()`'s `node_id` argument unused and lets `dataone::createObject()` attach it at the upload boundary; and JWT claims are decoded with stdlib base64+json, matching R's unverified local check (the real check is server-side `echoCredentials`), so no PyJWT. **Two additions from the S3 environment port** (metasalmonpy 0.4.0), both consequences of the same REST choice rather than new divergences. (a) `knb_environments` carries two fields `.ms_knb_environment_fields()` does not: `cn_endpoint`, *derived* in `knb_environments._environment_record()` from that environment's own `cn_base_url` rather than declared — so it cannot be switched independently of the resolver and Solr endpoints, which is the property R gets by deriving all three — and `token_setter`, which names the Python setter where R's `token_option` names an option. Both environments carry both, so the "a field added to one environment and forgotten in the other" guard reads the same authority on both sides. (b) R announces a non-durable target with `cli::cli_alert_info()` (`R/knb-publication.R`); metasalmonpy raises a `UserWarning`, because a Python library has no cli message stream and a warning is the idiom a caller can capture, filter or escalate. The text and the trigger are identical. *Retires when:* a maintained Python `dataone`/`datapack` appears (the first four), or metasalmon's own environment registry grows the same two fields (the registry half) |
| 17 | Idiom | The deterministic SDP archive carries its own Python determinism reference (`metasalmonpy-zipfile-1`: radix member order, 2000-01-01 timestamps, 0644, Unix `create_system`, no directory entries, deflate level 9) instead of R's reviewed-`zip`-version guard, and its bytes are not comparable to R's | Row 4 applied to the archive: miniz (R's `zip`) and zlib emit different deflate streams, so byte parity is impossible without vendoring a compressor. R's guard is `.ms_knb_reviewed_zip_versions` in `R/knb-sdp-archive.R` — an allowlist of reviewed versions with a runtime abort, so a compressor change stays a reviewed package change, which matters because the archive checksum is an immutable DataONE PID input. Python keeps that purpose by asserting the reference sha256 in its KNB test fixtures, but has no runtime abort because zlib ships with the interpreter and cannot be pinned per platform. **Twin corrected 2026-08-16:** `PARITY.md` row 17 had described R's guard as a single `zip` 3.0.1 pin; it has been a two-version allowlist since the guard was written |
| 18 | Idiom | Because the archive (row 17), the EML document (row 15) and the ORE document (row 4) differ from R's *bytes*, an end-to-end Python plan for the same SDP yields a different archive PID, resource-map PID and `plan_sha256` than R's | Unavoidable and disclosed, not silent: every algorithm that derives those values is pinned against R by driving the Python planner from R's own member table (R-generated fixtures committed on the Python side). The recovery-manifest JSON and the fingerprint payload are compared as literal bytes; the ORE graph and the SystemMetadata documents are `ET.canonicalize`-equal, not byte-equal — `PARITY.md` row 18's blanket "byte-exact … the ORE graph" overstates that one item, and row 16's wording there is the honest one. A manifest written by one implementation is therefore reviewed and published by that same implementation, exactly as DataONE immutability already requires |
| 19 | Idiom | `publish_sdp_to_knb(confirm = ...)` defaults to `None`, not R's `confirm = interactive()` plus `missing(confirm)` pair | Verified equivalent, not merely analogous: R's `confirm_missing` short-circuit makes the `interactive()` default inert, so an omitted `confirm` aborts in an interactive session too — and both sides demand exactly `TRUE`/`True` (R's `isTRUE()`, Python's `is True`), so a truthy `1` or `"yes"` authorizes nothing on either side. A single `None` sentinel expresses in one place what R needs two halves to guarantee: live mode proceeds only when the caller passed exactly `TRUE` |
| 20 | Ahead (converged) | Canonical SSSOM ordering (curie-map prefixes, mapping-set ids) is codepoint/C in Python; era R (0.1.7/0.1.8) used bare `sort()`/`order()`, so its bytes and manifest order varied with `LC_COLLATE` | **Converged — and one release earlier than the twin claims.** metasalmon adopted C collation at **0.2.0** ("Canonical bytes and identifiers no longer depend on `LC_COLLATE`"), not at 0.3.0 as `PARITY.md` row 20 states; `R/sssom.R` now orders with `method = "radix"` at every site that reaches bytes or the manifest. Current R and Python agree. The interop hazard the row records is live only against **era R (≤ 0.1.8)**: an era-R package written in a non-C locale can fail the Python manifest-ordering check and vice versa, demonstrated both directions |
| 21 | Ahead (converged) | Every Python SDP reader preserves a literal `"NA"`; only the empty field is missing. Era R used readr's `na = c("", "NA")` | **Converged:** `.ms_csv_na_token()` (`R/package-helpers.R`) returns `""` and is the sole token for both readers and the resource writer — metasalmon **0.2.4**'s canonical missing-value contract, on the same rationale ("NA" is a real fisheries gear code). Python forward-ported it early because era behaviour combined with the already-ported reader made a literal-`"NA"` vocabulary row unsatisfiable: Python could not export a package R could read. Residual difference is representation, not the token set — an empty field parses to `NA_character_` in R and to `""` in Python. **S10 chunk C (2026-08-22) gave the token its single authority**: `metadata.csv_na_token()` mirrors `.ms_csv_na_token()`, every canonical writer passes it explicitly (guarded by `tests/test_missing_value_contract.py`), and the EML raw-token audit joined the same contract, closing row 22. The representation half of this row is unchanged: `read_sdp_csv` keeps `na_values=[]` because mapping the token to `NaN` would change the in-memory spelling this row records, not the bytes |
| 22 | Interim — **converged at S10 chunk C (2026-08-22)** | Consequence of row 21: until chunk C, Python's `_ERA_NA_TOKENS` (era `("", "NA")`) governed the EML raw-token audit, so that audit modelled era R's parse of a **data resource** while Python's own readers kept a literal `NA` as data. Against current R that split was a **live divergence**, not a dormant one: R's `.ms_eml_add_missing_values()` derives missingness from `is.na(parsed_values)`, and with `na = ""` a literal `NA` cell is not missing — so R accepted a cell that Python rejected as an undeclared non-empty missing token | The retirement condition this row carried since 0.1.7 was executed at chunk C (metasalmonpy PR #15): `_ERA_NA_TOKENS` is deleted and audit missingness is "raw token trims to `csv_na_token()`" — the same contract as every other reader. Every verdict was **measured against metasalmon `main` @ `39818ce`** (2026-08-22), not read from this row, and all five match: a literal `NA` cell with no declared code is **accepted** (data), a padded `" NA "` cell is **accepted**, a whitespace-only cell is **rejected** (undeclared non-empty missing token), a declared code the bytes never contain is **rejected**, and a declared `"NA"` code over literal-`NA` data is **rejected** as "declares missing-value code where the parsed value is not missing" — which is exactly why the era EML/KNB fixtures were unregenerable under the old inputs and had to move with the convergence (both era corpora declared `missingValueCode = "NA"` over era-written bytes; the regenerated `tests/data/eml/` set was cut against R `main` @ `39818ce` via R's own `make_eml_test_sdp()`, and the `tests/data/knb/` pair under the same era v0.1.8 extraction and toolchain with **only** the NA contract moved, its generator first proven byte-for-byte against all 22 unmoved era artifacts — see the fixture `generated_by` notes). The interop hazard this row warned about — packages exchanged with metasalmon ≥ 0.2.4 — is closed. The row stays at its number, per this register's rule, as the record that the era behaviour was era lag rather than a standing choice |
| 23 | Idiom | `trim_ws` is an approximation of readr rather than a transliteration: `skipinitialspace=True` plus a strip of readr's trim characters (`" \t\r\n"`) | Narrow enough to document rather than chase. The probed field shapes, the header cases and the beyond-ASCII non-cases (U+00A0, U+3000 left alone) are pinned in the Python reader tests. `PARITY.md` row 23 names one known mismatch — a leading **tab** before an opening quote (`\t"z"\t`), which readr unquotes and pandas does not — but that shape once appeared in no test on either side, so nothing would have caught it moving. **Now pinned** — `tests/test_sdp_csv_reader.py::test_a_tab_before_an_opening_quote_is_the_one_known_trim_mismatch` — so it is a verified boundary rather than an asserted one |
| 24 | Idiom | Atomic writes go through a Python-only `atomic_io` module so artifacts land at the umask default mode; R has no counterpart and each writer inlines `writeBin` + `file.rename` | R gets umask behaviour free from `writeBin`; Python's `tempfile.mkstemp()` hard-codes 0600, which would otherwise have shipped owner-only semantic artifacts. The module **measures** the default mode — one throwaway `os.open(..., 0o666)` then `fstat`, so the kernel has already subtracted the umask — instead of mutating and restoring the process umask, which is a per-process race. The mechanism changed at 0.1.7; the deviation (the module exists at all) did not |
| 25 | Gap (closed at the metasalmonpy 0.2.0 rung, 2026-08-17) | Python's `read_salmon_datapackage()` returned every resource column as a string, preserving the raw token; era R type-inferred resources with `readr::read_csv(file_path)` | **Both halves are now closed.** The text read converged at metasalmon **0.2.0**: `.ms_read_resource_csv()` reads with `col_types = list(.default = col_character())`, and the dictionary became the sole type authority. The **typing** half — which this register carried as an open capability gap through 2026-08-18 — closed on the Python side at its 0.2.0 rung: `read_salmon_datapackage()` now converts every dictionary-declared column, keeps the raw token whenever the conversion would not be faithful, and reports the mismatch as a structured validation issue, which is the shape R 0.3.0 has. **Measured against R rather than read off it:** `tests/data/resource_types/r-token-verdicts.json` is the output of driving six R token functions over a 166-token corpus under R 4.5.2, at a read-only extraction of the commit that made 0.2.1 current — **524 non-null verdicts** across the six declared types (integer 227, number 177, datetime 54, boolean 25, date 31, string 10), each asserted in `tests/test_resource_types.py`. **The Kind disagreement is discharged with it.** This row read *Ahead (partly converged)* against `PARITY.md`'s *Gap*, and the hub is the side that moved: with both halves closed the two labels describe a spent difference rather than a live claim, and the twin's is the committed, released text. The storage type Python chose for `integer` is a separate logged decision — row 35. Verified consequence on both sides: a whitespace-padded resource header is trimmed, so Python's `_validate_raw_table` aborts on it exactly as `.ms_eml_validate_raw_table()` does |
| 26 | Idiom | Python's `_is_canonical_rights_url()` expresses R's `httr2::url_parse()` / `url_build()` round-trip test as explicit conditions instead of reimplementing curl's URL normalizer | R's `.ms_license_descriptor()` (`R/package-helpers.R`) accepts a custom rights URL only when it is already in curl's canonical form — unchanged at 0.3.0. Python has no curl URL parser and vendoring one for a licence check is disproportionate, so the same rule is written as the things curl would otherwise change: an `http`/`https` scheme in lowercase, non-empty netloc and hostname, non-empty path, no whitespace, no dot segments, and a `urlsplit`/`urlunsplit` round trip. Verdicts match era R on **21** pinned probe values — 13 accepted, 8 rejected, counted in `tests/test_package_io.py::test_licence_descriptors_match_era_r` (the twin's earlier count of twenty is corrected on both sides; do not re-file it). **Percent-encoding is a measured divergence, not residual risk** (corrected here 2026-08-21): this row used to file it under "exotic normalization … that no SDP licence field has carried", which named the exposure but never said the two sides actually disagree. They do. curl decodes an encoded separator and uppercases hex digits before comparing, so R **rejects** `%2F` and lowercase `%c3%a9` where the Python conditions **accept** both; `%20` and uppercase `%C3%A9` agree. Pinned by `tests/test_package_io.py::test_percent_encoded_rights_urls_are_a_documented_divergence`, so it is a verified boundary with a named test — the same upgrade row 23's tab-before-quote shape got, and for the same reason: a residual-risk sentence with no test decays into an assertion nobody can check. Python is the **laxer** side, so the failure mode is a rights URL Python accepts and R rejects. The other half of the old phrase is still true and still unmeasured: **no probe on either side carries an IDN host**, so say unprobed rather than low-risk |
| 27 | Idiom (era lag; converged at S10 chunk A) | The SDP profile version is resolved through `sdp_schema.sdp_profile_version()` in metasalmonpy (re-exported as `metadata.SDP_PROFILE_VERSION`); R resolves it through `.ms_sdp_profile_version()` | The single-source-of-truth property matches on both sides, and each earlier retirement condition on this row was discharged in turn: the 0.1.7 constant became a read of the vendored bundle at 0.1.8 (how that bundle is read is row 30), and the mechanism gap closed at metasalmonpy's 0.2.0 rung when `sdp_schema.load_sdp_schema()` matched R's remote-first loader (registered as row 38). What remained was one number stamped from two different spec eras by the same mechanism: R 0.3.0 declared `sdp-0.3.0` while metasalmonpy still emitted `sdp-0.2.0` into `dataset.csv$spec_version` and `datapackage.json`'s `sdp.specVersion` — correct for its parity claim, era lag rather than a standing choice. **Converged at S10 chunk A (2026-08-22):** the vendored Python bundle was swapped to a verbatim copy of the upstream `sdp-0.3.0` tag — byte-identical to metasalmon's vendored copy — in the same change that moved `SDP_SPEC_TAG`, so both implementations now stamp `sdp-0.3.0` from the same bundle through the same mechanism. The row stays at its number as the converged record of the era lag |
| 28 | Idiom | R's TRE-resolved `[[:space:]]` / `[[:cntrl:]]` memberships are enumerated once in metasalmonpy's `metadata` module (`R_SPACE_CLASS`, `R_CNTRL_CLASS`) and imported by its `eml`, `sssom` **and `sdp_methods`** modules; **metasalmon consolidated the other way** — one shared *predicate*, `.ms_absolute_iri_shape()` in `R/iri-predicates.R`, called by the SDP-extension, EML and SSSOM validators, rather than one shared character class. Three regex literals over one class there; one predicate here | The enumeration is the load-bearing part, and duplicating it is how the Python `sssom` module drifted from `eml` in the first place. **This row's Difference cell said "R re-writes the POSIX class at each call site" until 2026-08-24; that described R before the post-0.3.0 consolidation**, which is exactly the drift a register exists to prevent — its own Why cell had contradicted it since the fix landed, and metasalmonpy's 0.4.0 audit is what noticed the two halves disagreeing. The consolidation was backlog #85: `.ms_sdp_extension_is_absolute_iri()` applied `perl = TRUE` to the same `[^[:space:]]` shape `R/eml-export.R` ran under TRE, which made it **stricter** — PCRE's `[[:space:]]` is ASCII-only, so it had been accepting U+3000 and U+1680 where EML and SSSOM rejected them — and dropping `perl = TRUE` closed that. **Behaviourally the two sides now agree**, verified with those exact codepoints: `sdp_methods._is_absolute_iri`, `eml._ABSOLUTE_URI_RE` and `sssom`'s two patterns all reject them, and `measurement_decompositions._ABSOLUTE_IRI_RE` deliberately accepts them, mirroring R's narrower ASCII-classed decomposition validator character-for-character. **Retirement condition:** the constants stand while metasalmon resolves those classes through TRE; if `R/iri-predicates.R`, `R/eml-export.R` or `R/sssom.R` ever switch those validators to `perl = TRUE`, the replacement membership must be re-enumerated against that release rather than guessed — PCRE's `[[:space:]]` is ASCII-only, which is why the measurement-decomposition validators correctly keep an ASCII class on both sides. **This row was also wrong to say "Python mirrors no such function"** — see row 33 |
| 29 | Idiom | The `reproducibility/manifest.json` provenance block names `metasalmonpy.write_sdp_reproducibility_manifest` + its version; R names its own. Each validator accepts either implementation's provenance | The row-11 honest-provenance ruling applied to the 0.1.8 reproducibility manifest, and the byte claim holds: for the same declared artifact set the two manifests differ **only** in those two provenance values — profile string, radix `path` ordering, `sha256`, `size_bytes` and the JSON encoding all match, because `jsonlite::toJSON(pretty = TRUE)` and `json.dumps(indent = 2)` agree on two-space indentation. Pinned from R fixtures, not asserted: `tests/test_reproducibility.py::test_manifest_bytes_match_r_apart_from_provenance` swaps the provenance object back and demands R's exact bytes, over a committed v0.1.8 fixture whose own sha256 is checked. **The one-way half of this row is discharged** (post-0.3.0, backlog #88): R's validator had the writer half of the ruling and not the read half, so a Python-written manifest was rejected — and via `R/knb-publication.R` that blocked KNB publication of any Python-written SDP, not only direct validation. R now accepts both writers, verified by driving metasalmonpy 0.1.8's writer over a tree R had written a manifest for and validating it in R. **Both sides now have one owner for the accepted set, and the claim that only R did is stale as of 2026-08-24**: this row said "Python still keeps a `_ACCEPTED_PROVENANCE` dict per module", which was true when written and false after metasalmonpy's 0.4.0 port hoisted the table into `provenance.py` — its `tests/test_provenance.py` now fails if any of the three modules regrows the dict, the mirror of the structural guard `tests/testthat/test-provenance.R` runs here. The same port fixed the *other* half of R's 0.4.0 version-typing entry: metasalmonpy's reproducibility reader coerced the version with `str()`, so a JSON number, boolean or array passed as a version, and it now demands one non-blank string through the shared `provenance.version_ok()`. metasalmon's 0.4.0 NEWS said "both Python readers already" rejected those, which was true of `measurement_decompositions.py` and not of `reproducibility.py`; that entry now carries a dated correction. A plain row-11 sibling now |
| 30 | Idiom | `sdp_schema.sdp_profile_version()` line-scans the vendored `sdp.rules.yaml` for its one top-level `version:` scalar; R parses the document | Core dependencies stay pandas + requests, and pulling PyYAML in for one scalar is disproportionate (it stays in the `[eml]` extra, row 14). The document is machine-generated by `smn-data-pkg`'s `scripts/generate_artifacts.py` and has carried `version:` as a plain top-level scalar in every published version, and the scan **raises rather than guessing** when the key is absent, so a format change surfaces as an error and not a silently stale version. Worth stating from this side: R's full parse is not the stylistic half of the difference. `.ms_load_sdp_schema()` (`R/schema-helpers.R`) needs the whole bundle anyway, and it **cross-checks** `rules$version` against the profile's `sdp:version`, aborting when they disagree — a bundle whose two halves declare different versions is rejected. A line scan cannot make that check and Python makes it nowhere else, so the narrow reader also trades away an internal-consistency guard, not just a dependency. **The core-deps property is now enforced rather than asserted** (amended 2026-08-17): this row claimed a property with nothing checking it, and the 0.1.8 milestone broke it in the same stream that wrote the row — see row 34. Two enforcement points, and both are on the Python side only, because `yaml` is a hard `Imports` dependency in metasalmon's `DESCRIPTION` and there is no property here to protect. `tests/test_knb_publication.py::KnbCoreDependencyTests` blocks `yaml` from `sys.meta_path` so the property fails in a *developer's* environment, where the extra is normally present; it also evicts cached `yaml*` entries from `sys.modules`, without which a `sys.meta_path` finder is inert against an already-imported module, and `test_the_blocker_actually_blocks` guards the guard. **The CI half of this row's complaint is discharged** (2026-08-21). This register objected that the twin named a `core-deps` job that did not exist — there was one `python` job in `.github/workflows/parity.yml` installing `[test]`, so the core-deps run existed only by accident and the broad run the wording implied existed nowhere, leaving 94 extras-gated tests with no CI at all (backlog **#92**, fixed by metasalmonpy PR #12). That job is now a **two-leg matrix** — *core dependencies only* (`.[test]`) and *with `[eml]` and `[context]` extras* (`.[test,eml,context]`) — running an identical step list, each leg asserting its own dependency configuration before it runs anything (`yaml lxml openpyxl pypdf xlrd` absent on one, present on the other). The verification step is the load-bearing part: without it a typo in the extras list turns the second leg into a second core-deps run and the job stays green under a name describing coverage it had stopped providing. `PARITY.md` row 30 now describes what CI does and keeps the old overstatement marked as such; **do not re-file the job-name correction.** **Retirement condition, replaced at the 0.2.0 rung because the old one was *wrong* rather than merely reached:** "the day the mirror needs any *other* field of `sdp.rules.yaml`" was met almost immediately — the remote loader needs the document's `profile` as well as its `version`, and it is reached from `write_salmon_datapackage()`, which must stay core-deps-safe, so obeying the old condition would have broken the very property this row claims (row 34's failure mode exactly). The scan was generalised from one key to any top-level scalar instead. The honest condition is: **replace it with a full parse when metasalmonpy needs a nested or structured field of `sdp.rules.yaml`, or when PyYAML becomes a core dependency for an unrelated reason** — until then a full parse costs the core-deps property and buys nothing |
| 31 | Idiom (stricter) | `apply_semantic_suggestions(strategy = "llm")` requires the frame to carry `llm_selected`, `llm_decision` **and** `llm_confidence`, and applies only rows with `llm_selected` truthy *and* `llm_decision == "accept"`; R requires only `llm_selected` and filters on it alone | Pre-existing and previously unregistered. Equivalent on any frame the pipeline produces, and R is the reason why: `.ms_llm_validate_assessment_response()` (`R/llm-semantic-helpers.R`) ends with `if (!identical(decision, "accept")) selected_index <- NA_integer_`, so a non-accept decision can never carry a selected index, and `llm_selected` (`R/semantic-suggestions.R:1152`) is derived from that index. R already guarantees the invariant Python re-checks. The two differ only on a **hand-built** frame that sets `llm_selected` without an accepting decision — R applies it, Python refuses rather than writing an unreviewed IRI into the dictionary — and on column presence, since R needs only the one column while Python demands all three. Both are safe directions for a mirror to err in, but neither is a behaviour metasalmon has |
| 32 | **Gap (open, no milestone owns it) — the ordering half closed 2026-08-17, in R** | Two separable claims lived in this row and only one of them was ever Python's. **(a) Still open, Python-side:** `_score_and_rank_terms` (`term_search.py:955`) has **no counterpart to R's ranking-profile system** — no `ranking_profile` argument, no `.merge_ranking_profile()`, no `.ranking_profile_defaults()`, every tunable hardcoded. A **separate Python-side defect** sits on the same surface: `benchmark_term_ranking_fixtures(profiles = ...)` is exported on *both* sides, but Python binds each profile to `_profile` and never passes it to the scorer, then echoes the profiles back in its result — benchmarking two profiles returns two identical summary rows under different names, so the one exported surface for comparing profiles silently reports every profile as equivalent. That defect **stays Python-side**. **(b) Closed, and it was R's defect, not a Python gap:** the two implementations ranked the same candidates in different orders — R put `gcdfo` above `smn`, Python the reverse. Brett ruled 2026-08-17 that **metasalmon should rank `smn` above `gcdfo`, that metasalmonpy is right, and that R is the side that changes** — the first application of the amended mirror contract, *"if the Python implementation got it right, update metasalmon"*. Fixed here post-0.3.0: `role_boost` had given `gcdfo` 1.3 against `smn`'s 1.7 for `variable`, `entity` and `method`, leaving a 0.5 preference margin that the ordinary per-candidate bonus stack (0.2 label overlap plus 0.4 cross-source label agreement) overturned; `gcdfo` is now a flat 1.0 in every role and the base weights are `smn` 1.2 / `gcdfo` 1.0 | **Not an idiom, and the two halves have different owners.** The gap half predates the 0.1.6 parity claim, was never registered, is **not in any S10 rung's scope**, and backlog #87 carries it. The ordering half was confirmed by **measurement rather than by reading** on 2026-08-17: driving `.score_and_rank_terms()` over six tie-heavy candidates under four input permutations returned one fixed order on each side (so 0.2.1's determinism property holds on both) but **not the same order**. After the R fix that same differential returns the same order *and the same scores* on both sides (`smn` 2.75, `gcdfo` 2.65), and R's `base_source_weight` now matches Python's map value-for-value. Pinned here by `tests/testthat/test-smn-outranks-gcdfo.R`. **(c) The guard split closed here 2026-08-24, and this is the second time the port ran R-wards.** metasalmonpy's 0.4.0 work made its ranking tables reachable — `term_search.ROLE_BOOST` / `.BASE_SOURCE_WEIGHT` hoisted to module constants — so `tests/test_role_contract_guard.py` checks all seven surfaces of the role contract in one file, and `PARITY.md` row 32 records that its guard "is no longer six-of-seven". metasalmon's was still split: `tests/testthat/test-role-contract-guard.R` claimed six, `role_boost` was checked over in `tests/testthat/test-smn-outranks-gcdfo.R`, and `AGENTS.md` had carried a dated correction saying so since 2026-08-18. R has now consolidated on the same behaviour. **Convergence, not a new divergence — with one deliberate structural difference, recorded because the mechanism genuinely differs and an undocumented difference is a contract violation even when the difference is fine.** Python *hoisted* because its table was an inlined dict literal inside the scorer with nothing enumerable to assert against; R *reads in place*, because `.ranking_profile_defaults()` already returns the table as a named list and is the merge base for the `ranking_profile` override system this row's half (a) says Python lacks. Hoisting in R would have created a second copy of the authority, which is exactly the drift Python then needed `test_the_role_boost_table_is_the_one_ranking_uses` to rule out — and *that* test did port, as an R pin that `.score_and_rank_terms()` still merges `.ranking_profile_defaults()`. Two consequences for whoever closes half (a). Python's `test_the_ranking_profile_system_is_still_absent` tripwire fires when the profile system lands, and its instruction is to re-point the seventh-surface check at the profile table — at which point Python's guard reads the same shape R's reads today, and the hoisted constants become the second copy. And R's consolidation found **two** gaps rather than one: surface 5, the deterministic validators, had never been checked on the R side at all (Python's `test_the_deterministic_validators_cover_the_evidence_gated_roles` had it), and `sources_for_role()` had no fall-through check where Python's `test_sources_for_role_serves_every_bundle_role` did. Both ported R-wards in the same change, so the "first six" figure `AGENTS.md` used was itself an overstatement by one. The remaining hardcoded-value differences are real but no longer known to change order: the unknown-source fallback still differs (Python 0, R 0.1); **role boosts** still differ for the non-salmon sources (e.g. `variable`: R `nvs` 1.0 / `ols` 0.4 / `zooma` 0.4, Python `nvs` 0.6 / `ols` 0.2 / `bioportal` 0.4) and R alone carries a `statistical_modifier` entry; the **vocabulary bonuses** differ in both magnitude and gating (R applies `host_bonus` 0.8 / `slug_bonus` 0.8 / `label_pattern_bonus` 0.4 *only inside* the role-preferences branch, Python adds 1 / 1 / 0.5 unconditionally); and R's `backend_score` term has no Python counterpart. **Retirement condition:** `_score_and_rank_terms` takes a profile argument merged over a Python `.ranking_profile_defaults()` whose values are read back from the R release being claimed, `benchmark_term_ranking_fixtures` actually threads `profiles` through, and the differential fixture stays pinned to a shared order — until then, treat any Python ranking result beyond the pinned `smn`/`gcdfo` order as unpinned against R |
| 33 | Gap — **discharged at S10 chunk A (2026-08-22)** | `_is_absolute_iri()` in metasalmonpy's `sdp_methods.py` — whose docstring names `.ms_sdp_extension_is_absolute_iri` as the thing it mirrors — matched whitespace with Python's `\s` instead of importing `metadata.R_SPACE_CLASS`: row 28's drift a second time, in the one extension module that never imported the constants. Measured on R 4.5.2: against R post-#85 the two engines disagreed on exactly 8 codepoints (U+001C–U+001F, U+0085, U+00A0, U+2007, U+202F), Python rejecting every one where R accepts | **The retirement condition is met**: `sdp_methods.py` builds `_ABSOLUTE_IRI_RE` and `_HTTP_AUTHORITY_RE` from `metadata.R_SPACE_CLASS` exactly as `eml.py` and `sssom.py` do, and `tests/test_sdp_methods.py::test_absolute_iri_shape_uses_r_tre_whitespace` pins the 12-codepoint membership — re-verified by driving `.ms_sdp_extension_is_absolute_iri` on metasalmon `main` (`e02111a`) over the same probes, 12/12 agreeing (was 8 disagreements). This row's own text said it is **deleted rather than converged** once discharged; deletion has to happen in both registers in one coordinated change, because `check-parity-registers.py` fails on a number present in one register only and the two registers live in different repositories — so both registers carry this discharge text until a coordinated deletion lands. Twin: `PARITY.md` row 33, which records the same state |
| 34 | Idiom | The two reviewed-ledger **binding** assertions live in the publication preflight (`_require_review_ledger_binding`, called once from `_build_plan`), not in the artifact-inventory helper. R keeps them inside `.ms_knb_sdp_artifact_paths()` (`R/knb-publication.R:311-315`, `:328-332`) next to the inventory that produced them | **R's layout encodes a constraint R does not have.** `yaml` is a hard `Imports` dependency here, so reading the EML mapping sidecar from inside the inventory costs metasalmon nothing; in Python PyYAML is in the `[eml]` extra, and mirroring the layout literally made the deterministic SDP archive — otherwise pure pandas + `zipfile` — require that extra to build. Eight of `KnbArchiveTests`' eleven methods traverse the sidecar read. Parity is behavioural, not literal (Brett, 2026-08-15), and nothing about the inventory ever needed the mapping: which ledger is published is decided by what is on disk, identically on both sides. Narrower than it sounds — only the *mapping*-binding pair moved. The manifest-declares-the-canonical-ledger assertion (R `:306-310`) and the neither-ledger error stayed in the inventory helper on both sides, deliberately, so a package with no ledger at all still reports R's "requires a reviewed semantic-selection ledger" message rather than a binding complaint about a file that does not exist. **Three claims in `PARITY.md` row 34 need trimming from this side.** (a) "Behaviour is unchanged for every reachable caller" holds for accept/reject but **not for error precedence**: the binding check now runs before the inventory's required-artifact check, so a package that is both missing `metadata/codes.csv` and carrying a wrong `semantic_review.path` reports the binding message in Python where R reports `KNB publication requires canonical SDP artifact(s)`. No test on either side covers that ordering. (b) "Earlier than R fires them" is true but not the distinguishing fact — R also fires before a byte is staged (inventory at `R/knb-sdp-archive.R:326`, staging not until `:393`). The real difference is program position: Python fires before branch dispatch, R inside the archive writer or inside `.ms_knb_sdp_artifact_objects()`. (c) The eight archive tests were not "removed from core-deps coverage" — they would have **failed red**, which is a better outcome than the phrase suggests. **Do not move the assertions back into the inventory helper**; `KnbCoreDependencyTests` exists to fail when someone does |
| 35 | Idiom | A dictionary-declared `integer` column reads as pandas `float64` in metasalmonpy, not as nullable `Int64`; `number` reads as `float64` too | metasalmon reads both with `readr::col_double()` and says why at `R/dictionary-helpers.R:650`: `col_integer()` silently `NA`s values past 2^31, and `.ms_canonical_value_tokens()` renders integrally either way. `Int64` would not have *that* defect, and **that is the problem** — it is exact past 2^53 where a double is not, so an `Int64` column would accept `9007199254740993` while metasalmon reports it as beyond exact numeric precision. R's own reader makes the same trade explicitly: `.ms_convert_declared_tokens()` (`R/package-helpers.R:2450`) parses with `readr::parse_double` and keeps the raw token as ground truth *because* every collector is lossy in some direction. Choosing the better numeric type would have bought the two implementations disagreeing about which packages are valid. `float64` keeps every mismatch verdict identical — measured token-for-token over the 524-verdict corpus row 25 describes — and the raw token survives either way, so nothing is lost that the dictionary did not already declare. Consequence a caller sees: re-inferring a declared `integer` column with `infer_value_type()` answers `"number"`; every other declared type round-trips exactly. **This number is metasalmonpy's, committed and released at its 0.2.0 rung — it is why the hub's validator row moved to 41**, on the row-29 precedent. **Retirement condition:** revisit only if metasalmon stops using `col_double()` for `integer`; the two choices must agree, because the fidelity check is defined against the storage type |
| 36 | Idiom | Typed resource columns are written with the shortest round-trip decimal in **plain** notation (`resource_types.render_resource_frame`); metasalmon writes resources with `readr::write_csv()` (`R/package-helpers.R:185`), whose double formatter sometimes chooses a scientific form | Row 4 applied to a data resource. readr's writer is vroom's C++ shortest-representation formatter with a `%.17g` fallback: `1e15` stays `1e15`, `1.5e15` becomes `15e14`, `0.00015` becomes `1.5e-4`. Reproducing those bytes is the same class of impossibility as the `zip` and libxml2 formatters. What metasalmonpy emits instead is **metasalmon's own canonical rendering** — `.ms_format_number_token()` (`R/dictionary-helpers.R:992`): plain notation, the shortest of 15/16/17 significant digits that round-trips, `decimal.mark = "."` because the token is a comparison key and must not follow `getOption("OutDec")`. That is the token R's own validator compares code values with, so the *values* are identical, both readers parse both forms, and the bytes agree wherever readr also picks plain notation. Measured over a package covering `100000`, `0.1`, `1234567890123456`, logicals, dates and datetimes: every byte matches (`tests/test_typed_roundtrip.py`). **The retirement condition is a CI experiment, not "none foreseen"** (changed 2026-08-21, in both registers). `PARITY.md` row 36 read *"none foreseen; it would take metasalmon adopting a documented, platform-independent CSV number format"* — a condition that assumes away the thing the row itself doubts. The `%.17g` fallback is platform-dependent, and **metasalmon's own source records a disagreement in that neighbourhood**: `R/dictionary-helpers.R:899` notes that near the subnormal boundary macOS cannot round-trip `1e-300` through any of 15..17 significant digits while Linux can, and that `readr::parse_double()` disagrees with `as.numeric()` there — which is why the R normalizer treats any token whose absolute exponent exceeds 290 as unrepresentable everywhere rather than asking the platform. That is row 40's shape exactly: a claim that reads as correct on the platform everyone develops on. **Retirement condition:** regenerate `tests/data/resource_types/r-package/` — the committed R-written fixture the byte comparison runs against — with the same metasalmon commit on **Linux**, and diff it against the committed copy. If the resource bytes are identical, this is a permanent Idiom *with evidence* and says so; if they differ, it stops being a formatting idiom and becomes an R-side determinism defect on the row-40 pattern, owned by the hub. Until that run exists, read "every byte matches" as a **single-platform** result |
| 37 | Inapplicable (escape half) — redaction half **converged at S10 chunk F, 2026-08-22** | `.ms_cli_escape()`, `.ms_cli_bullets()` and `.ms_abort_external()` (`R/cli-safety.R`) have no Python counterpart; `.ms_redact_secrets()` is mirrored as `text_safety.redact_secrets()`, which since chunk F is metasalmonpy's **only** redactor, exactly as `.ms_redact_secrets()` has been R's only redactor since 0.2.5 | The escape half exists because cli interpolates every element of a condition message as a glue template — the 0.2.0 P0-6 defect, where a provider error containing `{Sys.getenv("OPENAI_API_KEY")}` printed the key and an unbalanced brace replaced the message with a parse error. Python has no such layer: an exception message is a finished string and `str.format`/f-strings reach only templates the package itself writes, so escaping external text there would mangle it rather than protect it. `tests/testthat/test-cli-safety-guard.R` enforces the R half and has nothing to enforce on the other side. The **redaction** half is mirrored exactly, including its placement rule — apply where external text is *captured*, not where it is displayed, because text stored on a returned tibble or written to a CSV outlives any message. **The two era-lag facts this row stated from this side on 2026-08-21 are both discharged** (S10 chunk F, metasalmonpy PR #19, merged 2026-08-22), and they closed on exactly the terms stated: (a) metasalmon has had **one** redactor since **0.2.5**, and metasalmonpy now does too — `knb_publication._redact` is **deleted**, its three call sites (`_abort_safe`, the live-adapter warning wrapper, the DataONE REST error path) route through the shared function, and `tests/test_text_safety.py::test_exactly_one_redactor_exists` is the standing guard against a second implementation reappearing; (b) `_CREDENTIAL_NAME` adopted 0.2.5's **structural** rule — `[a-z0-9]+[_-]token(?![A-Za-z0-9_])`, any qualified name whose final segment is `token` — closing the split where `dataone_token` was redacted while `dataone_test_token` and `knb_staging_token` leaked **at rest**, which was the one place in this row where the lag had a security shape. `max_token_count`/`total_tokens` still survive; `dataone_token_v2` is now deliberately **unmatched**, adopting R's recorded trade (that verdict flipped on the Python side from redacted to left alone); the separator whitespace class was aligned to R's PCRE `[[:space:]]`. **Convergence was measured, not asserted:** a 31-string adversarial battery — credential headers, cookie jars, serialized JSON credentials, qualified staging and production tokens, token-count diagnostics, JWTs, `sk-` and `AIza` keys, URLs with query-string keys, prose decoys — is **byte-identical** between `redact_secrets()` and `.ms_redact_secrets()` on metasalmon `main` @ `794647a`, against exactly the three divergences named above before the chunk. One observable output changed with the consolidation, exactly as in R at 0.2.5: an `Authorization: Bearer …` line is redacted as a whole credential-header line rather than only its Bearer payload. The era `expected.json` `redact_*` helper values retired with the deleted function they pinned. **Retirement condition:** the *Inapplicable* half **never retires** — Python is not going to grow a glue-template layer, so there is nothing to converge on and the row stays as the record of why. The redaction half's condition (delete `knb_publication._redact`, route its call sites through the shared function, adopt the structural final-segment rule) **is met**, and survives as the one-redactor guard test |
| 38 | Idiom (converged at S10 chunk A) | metasalmonpy's remote schema loader defaulted its base URL to the upstream **`sdp-0.2.0`** tag; metasalmon's `.ms_default_sdp_schema_base_url()` (`R/schema-helpers.R:87`) defaults to the **`sdp-0.3.0`** tag | Same mechanism, different ref, because the two packages claimed different spec eras — and **both sides pin a tag; neither follows a branch** (corrected here 2026-08-21: R advanced from tracking `main` to a tag pin in commit `e6a7209`, with the reason in the source — tracking `main` meant every upstream spec release broke networked loads, because `sdp-0.3.0` deleted `methods.schema.json` and the remote fetch 404ed). The two implementations reached the pin independently and from opposite directions. Nothing is locked away on either side: `METASALMONPY_SDP_SCHEMA_BASE_URL` / `set_sdp_schema_base_url()` and R's `metasalmon.sdp_schema_base_url` option name any ref or host, and Python reads both the source and the base URL at **call** time rather than at import. **Converged at S10 chunk A (2026-08-22):** the Python pin moved to `sdp-0.3.0` in the same change that swapped the vendored bundle — the two must never name different spec eras — and the defaults are now identical: `.ms_default_sdp_schema_base_url()` and `DEFAULT_SDP_SCHEMA_BASE_URL` both name the upstream `sdp-0.3.0` tag. This is row 27's cross-reference discharged rather than open. **Still open, and not decided here:** R's pin, Python's pin and Python's vendored bundle are three copies of one spec-era fact across two repositories, each advanced by hand; whether that stays three hand edits or becomes one derived value is a question nobody has ruled on |
| 39 | Gap (open — folded into row 32, no milestone owns it) | metasalmon **0.2.1**'s tie-break fix touched nine ordering sites; **two have no Python counterpart** — `.apply_embedding_rerank()` (`R/term_search.R:1963`, opt-in through `METASALMON_EMBEDDING_RERANK`, adds an `embedding_score` column) and `.ms_merge_semantic_target_candidates()` (`R/semantics-helpers.R:187`, which merges a retry retrieval pass into the first). metasalmonpy has neither the rerank stage nor a retry pass | Recorded so that "0.2.1's collation fix is delivered" is not an overstatement. Seven of the nine sites are mirrored and carry the identical key set; the remaining two are **absent surfaces, not un-fixed ones** — nothing there orders wrongly, because nothing is there. Both belong to the ranking system row 32 already registers as pre-0.1.6 debt that no milestone owns (backlog **#87**), so this row opens nothing new: it names the two functions so that whichever rung closes row 32 knows the 0.2.1 tie-break has to arrive with them. The property 0.2.1 added **is** delivered wherever a counterpart exists, and it was measured rather than assumed — `.score_and_rank_terms()` over six tie-heavy candidates under four input permutations returns one fixed order on both sides (`tests/test_ranking_determinism.py`). That same differential is what turned row 32's ordering half from an inference into a measurement, and then into an R-side fix. *(S10 chunk E, 2026-08-22 — a **cache-key consequence** of this same gap, recorded here because it is the first place the gap costs something outside ranking itself.* metasalmon **0.2.2** folds `.ms_ranking_identity()` — the rerank switch and `METASALMON_EMBEDDING_WEIGHT` — into the `find_terms()` cache key, so a user who flips a ranking knob mid-session is not served the previous ordering out of cache. metasalmonpy's cache key **deliberately omits** any ranking-identity component, because it has no ranking knobs to key on; that omission is correct **exactly as long as this row's gap stands**. So whoever closes row 32 / backlog **#87** with a profile system must **extend the cache key in the same change**, or they reintroduce on the Python side the stale-ranking bug R fixed at 0.2.2 — a defect that would arrive as a side effect of adding a feature, which is the hardest kind to attribute.*) **Retirement condition:** delete this row when row 32 closes, with the two functions and their tie-break keys — on its own it has nothing worth keeping |
| 40 | Ahead (converged 2026-08-21, in R) | Every canonical calendar string metasalmonpy renders zero-pads the year on every platform: `resource_types._iso_date` / `._iso_seconds` build the text by explicit padding instead of `strftime`. metasalmon rendered the same keys with `format(parsed, "%Y-%m-%d")`, whose year padding was its build's rather than its own | **The divergence was real and R was the side that was wrong.** `%Y` is the one strftime field whose width C leaves unspecified; glibc does not pad it, and R delegates `%Y` to the platform strftime unless built with `--with-internal-tzcode` (default macOS, not generally Linux). metasalmonpy hit this in 0.2.0 — green on every macOS run, red on Linux CI only — and chose to pad unconditionally rather than mirror a platform dependency into a canonical key, registering the possible R-side defect here rather than leaving it silent. **That registration is the reason this row is short-lived rather than a standing deviation, and it is worth noticing why it worked:** the row recorded a *question* it could not answer, with the exact experiment that would answer it, so the next person did not have to rediscover either. **Measured 2026-08-21:** the R call returns the padded form on macOS R 4.5.2 and the unpadded `1-01-01` on this repo's Linux CI runner — settled by writing the assertion as a test and reading the check result, because every call site reads as correct and the platform where it is wrong is not the one anyone develops on. Four metasalmon sites carried it, including a latent one in the EML export that aborted a valid `dateTime` value on Linux, and one *implicit* site (`as.character()` of a Date) that contains no `%Y` to grep for. **Under Brett's 2026-08-17 ruling — "if the Python implementation got it right, then update metasalmon" — R moved:** `R/platform-time.R` renders the year explicitly, guarded by `tests/testthat/test-year-padding-guard.R`. Nothing changed on the Python side. **Hub backlog #94** — *not* #91, which this row cited until 2026-08-21: two unrelated defects were filed under #91, the `validate_salmon_datapackage()` issue-system gap kept it (row 41), and the year-padding defect was renumbered #94 so the three registers citing "#91" for year padding have an unambiguous target. `tests/data/resource_types/r-token-verdicts.json` needs no regeneration: it recorded `0001-01-01`, which was a macOS-only truth when captured and is now true on both platforms. **Retirement condition:** none — the row stays at its permanent number as the record that this was era lag rather than a standing choice, per the *Ahead* convention above. The **guard** retires when R guarantees a zero-padded `%Y` on every platform it builds on, which is the platform's contract and not this package's to change |
| 41 | Gap (closed at S10 chunk D, 2026-08-22) | `validate_salmon_datapackage()` reports validation findings through a different mechanism, not a smaller one. R's `.ms_collect_package_validation_issues()` (`R/package-helpers.R:1995-2381`) tags every finding with one of **eight** `issue_type` values — `dataset`, `tables`, `dictionary`, `codes`, `resource`, `columns`, `primary_key`, `composite_intent` — each with `table_id`, `column_name`, `value`, and collects them all before one abort names the total and previews ten. Python (`package_io.py:1593-1668`) has four unconditional `raise ValueError` sites and no collector: it stops at the **first** structural problem with an untyped string, so a package with three bad tables reports one. On `main` (0.1.8) the returned `issues` frame is `pd.DataFrame(columns=["message"])` — a column set that does not even match R's five — and the single `columns` category plus R's columns arrive only with **unmerged rung 3** (PR #10) | **Not an idiom, and not covered by row 1.** Row 1 licenses Python *exceptions* in place of R *cli conditions* — a delivery difference over the same set of conditions. This is a difference in **which conditions are reported at all**, and in how many of them one call surfaces: R's single abort is a complete typed list, Python's is the first item, untyped. Predates the 0.1.6 claim (`package_io.py` added 2026-02-06, six months before the 0.1.6 alignment) and survived revision at 0.1.6, 0.1.8 and rung 3 unreconciled. Self-documented at `package_io.py:1555-1562` — honest, and exactly the thing this register exists to convert from a source comment into a tracked deviation, since a comment is invisible to anyone reading the mirror contract. Backlog **#91** carries it. **This row was number 35 here until 2026-08-21, and it is the second collision this register has had to break.** metasalmonpy's 0.2.0 rung had already committed and released its own row 35 — the `integer`-reads-as-`float64` storage decision — while this row took 35 independently for the validator gap, in a register whose entire stated purpose is cross-reference by permanent number. Resolved on the **row-29 precedent**: the committed, released row keeps the number and the hub's row moves to the next free one, 41. The cause is the same one named under the maintenance notes below — a divergence found while reading R has no Python PR to ride, so it is numbered on this side without seeing what the other side has spent. Backlog #91's own text already cites **41** — a clause here claiming it still said "row 35" was itself stale and is struck (2026-08-22). **Sequencing hazard, logged when this row was open:** a milestone check of the form "both sides report N issues" passed *vacuously* against 0.1.8's empty frame and compared one category against eight after rung 3 — so no rung's verification could depend on issue counts. **Retirement condition:** Python collects rather than raises, emits R's eight categories with R's five columns, and a differential fixture pins both sides to the same issue set for the same deliberately broken package. **Met at S10 chunk D (metasalmonpy PR #20, merged 2026-08-22).** `_collect_package_validation_issues()` accumulates all eight typed categories with R's five columns and the validator aborts once carrying the total, a ten-message preview and the full frame; `codes` and `composite_intent` were ported whole, code values canonicalized through their declared type on both sides and the WSP composite-intent check reading route hints from metadata *and* the descriptor. Measured, not asserted: seventeen single-defect corruptions of the shipped example plus one stacked five-issue package, run through both implementations against metasalmon `main` @ `9d8f125`, and **every issue row matched field-for-field across all five columns, message bytes included** (`tests/test_validation_hardening.py` pins the transcribed rows, so a wording drift on either side fails there rather than dissolving into "roughly the same report"). **What the same differential showed about the state this row was describing is worse than the row said:** before chunk D, Python **passed clean on 13 of those 18 corrupted packages** — duplicate `table_id`s, ghost table references, non-unique primary keys, primary-key NAs, unlisted code values, composite-intent violations and a two-row `dataset.csv` all validated with **zero issues** — and aborted with one untyped `ValueError` on the other five. "Reports the first problem instead of all of them" understated it: for most categories it reported none. The raised `ValueError` carrying the typed frame as `.issues` is a delivery affordance R's cli abort has no equivalent of, covered by row 1's conditions-to-exceptions licence, not a behavioural difference. **The sequencing hazard is lifted:** both implementations now report the same issue set for the same broken package, so later milestones may compare issue counts and categories across them |
| 42 | Ahead (open — which side moves is undecided) | `infer_column_role(col_name, series)` is public API in metasalmonpy (`dictionary.py:232`, named in `__init__.py`'s `__all__`); in metasalmon it is an ordinary internal function — defined at `R/dictionary-helpers.R:1120` and absent from `NAMESPACE` | **Previously unregistered, and identical in shape to row 5.** `infer_value_type` is the same difference for the sibling helper, and the two are called from adjacent lines of the same R loop (`R/dictionary-helpers.R:214-215`), so a reader who found row 5 and stopped would reasonably assume this one was covered. It was not. The *behaviour* is mirrored — the Python function documents itself as a node-for-node port of metasalmon 0.1.7's and is pinned against R's verdicts in `tests/test_dictionary.py:153-168` — so only visibility differs. **Row 5's rationale is available but has not been applied to this name, and saying so is the point of the row.** Row 5 records a decision about `infer_value_type` ("already exported at 0.1.6; removing it would break users for no capability gain"), not a general rule that every dictionary helper is public in Python. **Open, and a real question rather than a formality:** metasalmon could export `infer_column_role()` — and `infer_value_type()` with it — so the two agree upward, or metasalmonpy keeps both public under row 5's reasoning and this register simply records the asymmetry. The amended mirror contract makes the first direction available, and nobody has ruled. **Retirement condition:** a ruling either way, recorded in both registers; if R exports, both rows converge and keep their numbers |
| 43 | Inapplicable — reversed (metasalmon has no counterpart to a *Python* surface) | metasalmonpy exports `create_salmon_datapackage()` and `create_salmon_datapackage_from_data()` (`package_io.py:802`, `:1454`) as `DeprecationWarning`-raising aliases for `write_salmon_datapackage()` and `create_sdp()`. metasalmon has neither name in any release, and no deprecation mechanism at all — no `lifecycle`, no `.Deprecated()` | **Previously unregistered.** They are metaSmnPy-era names kept for Python callers who predate the 0.1.6 alignment. Verified rather than assumed absent on the R side: `git log -S` over `R/` returns no commit that ever introduced either name, so this is not a removed R function whose alias outlived it — it is **public API surface metasalmon never had**. Harmless in behaviour (each warns and delegates immediately, with the legacy call contract preserved on `_from_data`), and precisely the kind of difference this register exists to convert from folklore into a row: someone diffing the two export lists finds two extra Python functions and nothing telling them why. **The Kind taxonomy has no slot for this, which is worth naming rather than papering over.** All four Kinds are written one-directionally — *Inapplicable* means "the R behaviour has no Python counterpart", *Gap* means "a capability metasalmon has and metasalmonpy does not". This row is the mirror image of both and the first to need it. **Open:** whether the taxonomy gains a fifth Kind for Python-only surface, or *Inapplicable* is read symmetrically with the direction stated in the row as it is here. Not settled — the label above is a placeholder that says which way it points, not a ruling. **Retirement condition:** metasalmonpy deletes both aliases at a rung permitted to break its own callers. That is a decision about metasalmonpy's compatibility promise rather than a parity question, and it has not been made |
| 44 | Gap (open, no milestone owns it) | `chat_decomposition()` takes the same `round_size` argument on both sides (R default `3L`, Python default `3`) and Python **discards it** — `chat_decomposition.py:382` is `del round_size  # Reserved for grouped decomposition questions in later slices.` metasalmon builds a real question queue and serves it in grouped rounds (`.ms_chat_decomposition_question_queue()`, `_question_groups()`, `_next_round(state, round_size = 3L)`, `R/chat-decomposition.R:180-295`); metasalmonpy asks a flat prompt sequence over the whole-variable `term_iri` only | **Documented only in metasalmonpy's `docs/tech-debt.md` (entry dated 2026-07-28) and in neither register** — which is the defect this row fixes. `docs/tech-debt.md` is a Python-repo document; the mirror contract is checked from here, so a capability difference recorded only there is invisible to the contract. What *is* mirrored is the curation outcome and the safety boundary: persisted state and transcript kept separately, candidate or new-term preview, explicit approval, nothing auto-submitted. What differs is how much of the I-ADOPT decomposition a user is asked about in one pass. **The accepted-and-discarded argument is what makes it silent**, and it is the second instance of that shape in this mirror — row 32's `benchmark_term_ranking_fixtures(profiles = ...)` is the first. A caller who tunes `round_size` gets no error, no warning and no change in behaviour, and the signature is the only place a user would look. Two occurrences of one anti-pattern is a pattern: when a rung touches either function, sweep the rest of the exported signatures for arguments that are accepted and dropped. **Retirement condition:** either metasalmonpy implements grouped rounds and honours `round_size`, or it removes the parameter from its signature so the difference is visible at the call site. **Which of the two — and whether grouped rounds are wanted in Python at all — is open.** `docs/tech-debt.md` defers it on "Python user research", which is not a scheduled event, and no rung owns it |
| 45 | Gap (open — deferred by design on the Python side, unratified here) | The versioned cryptographic capture and cohort-publication harness for LLM-provider evidence is metasalmon-only: `scripts/theme-a-benchmark.R`, `tests/testthat/test-theme-a-benchmark.R`, `tests/testthat/fixtures/theme-a/`, `notes/evidence/theme-a/`, and the `.github/workflows/theme-a-integrity.yaml` job that runs on dispatch, on release publication, and on any PR touching those paths. metasalmonpy has no counterpart; its provider coverage is deterministic injected responses, offline contract tests, and R/Python package round trips | **Documented only in metasalmonpy's `docs/tech-debt.md` (2026-07-28) and in neither register**, the same invisibility as row 44. The stated rationale is that metasalmon is the normative ontology-evidence publisher and duplicating the release-attestation machinery would test the same provider evidence twice at real maintenance and CI cost. That is a defensible standing choice rather than era lag — which is exactly *why* it needs a row. The mirror contract's default is that a capability metasalmon has, metasalmonpy gets; "deferred by design" is only legible as a decision if it is written where the contract is checked, and a tech-debt entry in the mirror's own repo is not that place. Note what the absence does and does not mean: metasalmonpy's LLM-review *behaviour* is mirrored and tested, so nothing a user runs is weaker. What is missing is the ability to **publish a benchmark capture independently of metasalmon**. **Retirement condition:** metasalmonpy needs to publish evidence independently — at which point the remediation `docs/tech-debt.md` names is language-neutral promoted fixtures shared by the two, not a second harness. **Open:** whether that ever becomes a goal. "Deferred by design" is a Python-side note; it has never been ratified as a hub decision, and the two are not the same thing |
| 46 | Gap (open) + era lag — **the era-lag half retired at S10 chunk A (2026-08-22)** | metasalmon ships two Fraser coho examples — the 30-row `inst/extdata/nuseds-fraser-coho-sample.csv` and the 173-row official slice `nuseds-fraser-coho-2023-2024.csv` with its own starter dictionary — plus `inst/extdata/example-data-README.md` and a `data-raw/` derivation script. metasalmonpy ships **only** the 30-row sample (`data/nuseds-fraser-coho-sample.csv`). Its bundled demo metadata and sample data are now **byte-copies of metasalmon `main`'s (`e02111a`)** — sixteenth dictionary column `statistical_modifier_iri`, the demo method placement on `tables.csv`, the recon-wave resolving IRIs, ISO dates and crosswalked codes — which retires the era-lag half exactly as rows 27 and 38 said it would | **The swap also fixed two things neither register had recorded.** metasalmonpy's bundled `column_dictionary.csv` demo was **corrupt as shipped** — two unquoted description commas shifted the RUN_TYPE and ESTIMATE_STAGE rows into schema-invalid `column_role`/`value_type` values, the same defect metasalmon fixed at 0.3.0 and the hub's 2026-08-21 recon found independently (backlog #98's Python half) — and its demo `dataset.csv` carried a source-citation drift. The **missing 173-row example is not era lag and stays open.** It landed in metasalmon as a deliberate second example — the tiny sample for the fastest walkthrough, the official slice for something closer to real Fraser coho data — and it carries provenance metasalmonpy has nowhere: the Open Government Canada record, the upstream workbook and its licence, and a seven-step reproducible derivation. A user following the mirror's documentation therefore has smaller examples than the documentation describes. Two costs worth stating rather than assuming away: carrying it roughly doubles the mirror's shipped data for one example, and its dictionary is a second artifact to keep in step with whatever spec era metasalmonpy claims. **Q4 was ruled 2026-08-24 (Brett): *"Promote the 173 row one."*** The 173-row slice is THE gold standard and the 30-row file is demoted to a named speed fixture — so this row's *which artifact* half is settled, and what it now records is that **the mirror is missing the ecosystem's exemplar**, not that it is missing a second example. The mirror rule attaches to the gold standard: metasalmonpy carries it, or a row here says why not. Two costs the ruling does not remove — carrying it roughly doubles the mirror's shipped data for one example, and its dictionary is a second artifact to keep in step with whatever spec era metasalmonpy claims. **Still open, and it is the narrower question:** one derivation script shared across the two repositories, or one each. Sharing keeps the 173 rows provably identical on both sides and needs a home neither repo currently has; duplicating is cheap now and is exactly how the two copies of the 30-row example drifted. **Retirement condition:** metasalmonpy ships the 173-row example, and the derivation-script question is answered in both registers |
| 47 | Gap (closed at S10 chunk B, 2026-08-22) | Until chunk B, **no NuSEDS crosswalk was wired into metasalmonpy's create path at all**: `nuseds_enumeration_method_crosswalk()` and `nuseds_estimate_method_crosswalk()` existed as data-only exports referenced nowhere else, while metasalmon has run `.ms_prefill_legacy_estimate_method_code_terms()` inside its artifact-inference context since its **initial commit** (2026-05-26, so in every era the mirror ever claimed — 0.1.6 through 0.2.1), filling `codes.csv$term_iri` for estimate-method code values during `create_sdp()`/`infer_salmon_datapackage_artifacts()` | **Previously unregistered, and broader than the R defect that exposed it.** The 2026-08-21 recon found R's *enumeration* crosswalk unwired (backlog #102) and, measuring the mirror, found metasalmonpy wired **nothing** — an undocumented divergence and therefore a mirror-contract violation until this row. Chunk B closed it by porting the shared prefill engine (`package_io._prefill_legacy_code_terms`, mirror of `.ms_prefill_legacy_code_terms`) with all three wrappers — estimate method (the pre-existing gap), estimate classification (`nuseds_estimate_classification_crosswalk()`, new on both sides; backlog #101), and enumeration methods, whose word rule is `"enumeration"` alone because NuSEDS names the column ENUMERATION_METHODS and a `\bmethod\b` test can never match the plural — wired at the same point in the artifact path as R (after codes are settled, before semantic seeding, explicit caller IRIs never overwritten). Differential-verified against metasalmon `main` (`9d8f125`): identical `term_iri` fills across the classification, enumeration, and estimate cases including the recorded non-mappings (`NO SURVEY THIS YEAR` stays blank). **Retirement condition:** none — the row stays as the permanent record that the wiring gap existed unregistered across four parity claims; the tests pinning the wiring are metasalmonpy `tests/test_package_io.py::LegacyCodePrefillTests` |
| 48 | Gap (converged at S10 chunk D, 2026-08-22) | Metadata review placeholders diverge in coverage and prose. Driving both writers over the same minimal input (S10 chunk C's byte differential, metasalmon `main` @ `39818ce`, 2026-08-22): R fills `creator`, `contact_name`, `contact_email` and `license` with `MISSING METADATA:` guidance where metasalmonpy leaves them empty, and where both fill a field the prose differs — R writes `MISSING DESCRIPTION: describe the contents and purpose of dataset 'demo'.` and `MISSING METADATA: describe the observation unit for table 'obs'.` against metasalmonpy's `MISSING METADATA: dataset description` / `MISSING METADATA: table observation unit` | Found in passing by chunk C's byte differential of `write_salmon_datapackage()` output — every other metadata byte, including the whole of `column_dictionary.csv` and the data resource, came out identical. Registered rather than fixed because placeholder surfacing is chunk **D**'s named scope and chunk C is contractually undiluted (missing-value bytes only). Both spellings share the `MISSING ` prefix metasalmonpy's `_is_review_value()` matches, so review-blocking behaviour agrees; the *bytes* and the fill coverage do not. **Closes at:** S10 chunk D (validation hardening / placeholder surfacing), which should converge coverage and prose by measuring current R rather than porting this row's examples. **Closed exactly that way (metasalmonpy PR #20, 2026-08-22):** the fill helpers were rewritten against R-measured output at metasalmon `main` @ `9d8f125` — `creator`, `contact_name`, `contact_email` and `license` gained R's `MISSING METADATA:` guidance, a blank `title`/`table_label` is titleized from its identifier through a verbatim `tools::toTitleCase` port (23 probes, zero mismatches), and the dataset/table/column prose carries R's exact wording; `infer_*` now returns placeholder-filled frames as R's do. Verified at the byte level rather than by inspection: `write_salmon_datapackage()` over the same blank input now writes a package whose every file — three metadata CSVs, the data resource and `datapackage.json` — is byte-identical to R's, and the same holds for the shipped 30-row example (11 files across the two probes). **The byte comparison also exposed two descriptor defects nobody had scoped**, which is the evidence for the method as much as for the fix: a blank `required` wrote `constraints: {"required": true}` — the *inverted* claim — because `iterrows()` hands a boolean-dtype NA back as a truthy float `nan`, and it was firing on the shipped example's own `RUN_TYPE` and `ESTIMATE_STAGE` rows; and a blank `column_label` omitted `title` where R emits an explicit `null`. Twin: `PARITY.md` row 48 |
| 49 | Idiom | The tidy-shape warning points at the reader's own tool: metasalmon 0.2.6's value-like-column-names warning ends `Consider tidyr::pivot_longer() before packaging.` and metasalmonpy's ends `Consider pandas.melt() before packaging.` Everything else about the check is exact — the two detection shapes (bare year-like names; a shared stem with numeric suffixes), the three-column threshold, the C-collation sort of the flagged names, the head-6 preview, and warning-never-issue in both validation modes — verified by a 20-probe differential of `.ms_detect_wide_columns()` vs `_detect_wide_columns()` against metasalmon `main` @ `9d8f125` (2026-08-22), pinned in metasalmonpy's `tests/test_validation_hardening.py` | The S10 execplan's chunk D row names this difference as intended scope ("the message points at `melt`"): recommending `tidyr::pivot_longer()` to a pandas user would be a pointer at a tool they are not holding. Same class as row 1's conditions-to-exceptions licence — advice text localized to the language, behaviour identical. **Retires when:** either implementation stops naming a reshaping tool in the warning, at which point the texts can converge fully |
| 50 | Idiom (transitional alias, Python-only) | Environment switches are named `METASALMONPY_*` in metasalmonpy against R's `METASALMON_*` (`METASALMONPY_CACHE` ↔ `METASALMON_CACHE`; `METASALMONPY_DEBUG_FETCH`, which has no R counterpart; joining the rung-3 `METASALMONPY_SDP_SCHEMA_*` pair), read **at call time** through one helper (`term_search._env_flag`) rather than at import; the pre-rename `SALMONPY_*` spellings keep working as deprecated aliases that warn **once per process** and are consulted only when the current spelling is unset or empty | S10 chunk E (metasalmonpy PR #17, merged 2026-08-22) executed the prefix rename that the [S10 card](sequences/s10-metasalmonpy-parity.md) had carried as *undecided* since 2026-08-13 and the execplan had claimed as "decided and logged here" with no decision record behind it — a two-document disagreement struck as an error on 2026-08-21 (execplan open decision 1b). The package was renamed from `salmonpy` on 2026-08-13 and the env prefix lagged the package name, partially pre-empted when rung 3 landed the two schema vars as `METASALMONPY_*`. The renamed switches mirror R's, so the prefix itself is ordinary idiom; **the alias window is the Python-only behaviour this row registers.** The window, decided at chunk E: the aliases survive through the S10 parity release — whatever number [Q7](questions.md) assigns it — and are **removed in the first tagged release after it**. **That number is now known and the window is now concrete: the S10 parity release is metasalmonpy `v0.4.0` (2026-08-24), so the aliases die in the first metasalmonpy tag after `v0.4.0`.** Recorded here because "the S10 parity release" was an unresolved reference for as long as Q7 was open, and an unresolved reference is exactly how a stated window becomes an open-ended one. Rationale, recorded so the window is not silently extended: no known external consumer ever existed at any parity level metasalmonpy has claimed, so one release of overlap is already generous, and keeping two live spellings of a cache switch invites exactly the silent divergence this register exists to prevent. Test-suite-only gates (`METASALMONPY_RUN_QUALARK_TEST`, `METASALMONPY_QUALARK_TEST_*`) were renamed **cleanly, with no alias** — developer knobs, not package API. Truthiness (`1`/`true`/`yes`, case-insensitive; unset and empty are off) was verified value-for-value against metasalmon `main` @ `794647a`. **Retirement condition:** in the first metasalmonpy tag after `v0.4.0`, delete the `SALMONPY_*` alias branch in `_env_flag`, its once-per-process warning set, **and this row** — the renamed variables then need no row at all, being the plain mirror of R's |
**Verification provenance.** Rows 16–28 were read against metasalmon 0.3.0 and
metasalmonpy 0.1.7 on 2026-08-16, rows 29–32 and 34 against 0.3.0 / 0.1.8 on
2026-08-17, and rows 25–27, 30 and 35–46 against 0.3.0 / 0.2.1 on 2026-08-21, and rows
**54–58 against 0.4.0 / 0.4.0 on 2026-08-24** — the first reading taken with
both sides claiming the same number, and the first taken against a pristine
`git archive` of the R tag rather than a working checkout — in every case by
reading both implementations, never by transcribing the twin.
That is the only reason this register has ever caught the twin being wrong, and
also the only reason the twin has caught this one.
| 51 | Idiom (unregistered until S10 chunk H) | The **package ownership sentinel** differs in name and content: metasalmon writes `.metasalmon-package` holding `metasalmon-owned\n`, metasalmonpy writes `.metasalmonpy-package` holding `metasalmonpy-owned\n`, and each side's managed-path inventory (`.ms_package_managed_paths()` / `_package_managed_paths()`) names **only its own** | **Previously unregistered in both registers**, and it arrived silently: the file was renamed with the package on 2026-08-13, exactly the class of change that looks cosmetic and is not. Found by S10 chunk H's byte differential against this repo's `main` @ `3b620ae`, where the example package came out byte-identical on all six shared files and differed **only** in the sentinel. The naming itself is right — each package should mark what it owns, and the ownership test falls back to the SDP-CSV check, so neither side refuses a package the other wrote. **The measured consequence is that a cross-written package accumulates both files and neither writer removes the other's** (measured 2026-08-22). Low harm, but undeclared package content. **Deliberately not fixed at chunk H**: changing either managed-path set makes one implementation delete a file the other owns, which is a cross-implementation ruling rather than a drive-by — raised as [Q14](questions.md). **Q14 was ruled 2026-08-24 (Brett): *"I want one share sentinel name. Nobody uses this yet so dont worry about breaking changes."*** — one **shared** sentinel name written and recognised by both implementations, with the compatibility break **explicitly accepted**, so the read-both/write-shared transition the recommendation proposed is optional rather than owed and no migration is due to existing packages. The rejected option is worth naming because it is the one a passing implementer reaches for: **neither side removes the other's file.** Choosing the name and landing it in both repositories is follow-up work — [backlog #113](backlog.md) here, and its twin in metasalmonpy. *Retires when:* both implementations write and recognise the one shared name and neither writes a per-language sentinel |
| 52 | Idiom | The structural guard keeping direct filesystem mutations out of the package writer reads the function differently on the two sides: metasalmon's `test-write-datapackage-abort-safety.R` scans `deparse(body(write_salmon_datapackage))`, which has already discarded comments, while metasalmonpy's `tests/test_write_datapackage_abort_safety.py` scans `inspect.getsource(...)`, which retains the docstring and every comment. The token lists differ accordingly — R's mutating vocabulary against Python's | Python has no cheap deparse of a live function body; `inspect.getsource` is the idiomatic reach. **The direction of the difference is what makes it safe to keep**: the Python guard scans a *superset* of R's text, so it can produce a false **positive** — a comment merely mentioning a mutating call fails the guard — and never a false negative. A guard noisier than its twin but never weaker is the acceptable direction, and stating which way it errs is the point of this row, per `AGENTS.md`'s rule that a guard whose claimed scope exceeds its real scope is worse than a missing guard. Both share the same stated limitation: literal token scans, defeated by one level of aliasing. *Retires when:* either guard stops being a token scan |
| 53 | Gap (open — a shared defect the mirror has *more* of) | `create_sdp()`'s three create-owned sidecars are unlink-then-rewrite on both sides (backlog **#111**), but metasalmonpy's widest window is wider. For `metadata/metadata-edh-hnap.xml`, R calls `.ms_replace_create_output()` then builds from the **in-memory** `artifacts$dataset_meta`; metasalmonpy calls `_replace_create_output()` then runs a **full `read_salmon_datapackage()` from disk** before building, so every read and parse failure of the whole package sits inside the destroyed-file window too | Measured, not read, during S10 chunk H (2026-08-22): after a clean `create_sdp(include_edh_xml=True)`, an abort injected into the EDH builder leaves 5479 bytes of previously written XML **deleted with nothing in its place**. The other two sidecars share the shape at narrower windows. Recorded rather than fixed because it is genuinely separate from chunk H — single-file blast radius against chunk H's whole-package one, and these files are `create_sdp()`-owned rather than part of the writer's managed-path inventory. **It will not retire when #111's R half closes**: #111 is the R shape, and closing it leaves metasalmonpy's wider read-from-disk window standing. *Retires when:* #111 closes **and** metasalmonpy's EDH path either builds in memory or becomes transactional |
| 54 | Gap (open, pre-0.1.6 debt — **direction not yet ruled**) | `write_salmon_datapackage()` **refuses an existing empty directory** without `overwrite = TRUE`; metasalmonpy writes into it. `.ms_check_package_write_dir()` (`R/package-helpers.R`) orders its guards *not a directory → create and return; `!overwrite` → abort; empty → return*, so an existing empty directory hits the `overwrite` abort first ("Directory ... already exists. Set `overwrite = TRUE` to replace."). metasalmonpy's `package_io._check_package_write_dir()` orders them *not a directory → mkdir and return; empty → return; `not overwrite` → raise*, so the empty directory returns before the gate. Confirmed empirically on both sides | **Not new, and not S10 chunk H's doing.** The reversed order arrived in the mirror with commit `e33526d` ("align core workflows with metasalmon 0.1.6") in the function chunk H later replaced, and metasalmon 0.1.6's `.ms_prepare_package_write_dir()` already had R's order — so the difference has been carried forward faithfully and silently since **before** the 0.1.6 parity claim was made. **Neither side's tests pin it**, which is why a whole-suite parity run stayed green over it: R's `tests/testthat/test-package-helpers.R` exercises a **non-empty** directory, and the mirror exercises the case not at all. Found by metasalmonpy's 0.4.0 audit, not by a failure, and deliberately not changed in that port. **Direction not yet ruled**, and this row deliberately does not rule it: no 0.4.0 NEWS entry on either side calls for the change, it is user-visible in both directions (refusing a write that used to succeed, or permitting one that used to abort), and under the 2026-08-17 amendment a divergence opens the question of which side is right rather than settling it. **A ruling turns on which failure is worse**: R's order is the safer default — an existing directory is a signal the caller may not have meant this path, and `overwrite` is how they say they did — while metasalmonpy's is friendlier to the common `mkdir -p && write` shape, where an empty directory carries no data to destroy and demanding `overwrite` for it trains callers to pass `overwrite` habitually, which is the flag's whole value gone. Raised as [Q15](questions.md). **Retirement condition:** Brett rules which order is correct; the losing side changes, both sides gain a test that pins an existing-empty-directory write with `overwrite = FALSE`, and this row records the ruling rather than the divergence |
| 55 | Ahead (converged 2026-08-17 in Python, 2026-08-24 in R) — **unregistered until metasalmonpy's 0.4.0 audit** | A `datetime`-typed observation dimension validates. metasalmonpy's `observation_structures._typed_character()` formats a temporal value into the canonical ISO-8601 lexical form the validators expect and leaves character input untouched; metasalmon 0.4.0 fixed the same defect on this side, seven days later | **Recorded late, which is the point of recording it.** Both implementations had the identical bug — the normalizer took a string coercion of a typed instant, yielding `"2024-01-31 10:00:00"`, and tested it against a strict ISO-8601 pattern that string can never match, so **every** package with a datetime-typed dimension was rejected and `write_sdp_observation_structures()` refused to write one. metasalmonpy fixed it in commit `739f9fd` (2026-08-17), and its commit message states it had previously "mirrored the rejection rather than diverging silently, reporting it to the hub" — the **correct** behaviour under the mirror contract, and it left **no register row** for the window in which the two sides deliberately differed. **So R was the follower here**, and metasalmon's own 0.4.0 NEWS entry described the fix without naming the mirror; that entry now carries a dated correction (2026-08-24) saying so and citing this row. An undocumented difference is a contract violation even when the difference itself was right, and a deliberately-mirrored defect is exactly the kind that evaporates: once both sides are green, the next auditor sees a matching suite and no trace of the window. **Retirement condition:** none — the divergence is closed and this row is its record. It stays as the precedent for how a deliberately-mirrored defect gets registered *while* it is being mirrored, not after |
| 56 | Gap (open — a shared residual, and a cross-implementation difference) | A `POSIXct`/`Timestamp` metadata scalar is rendered differently by the descriptor and by the CSV writer, on **both** sides, in opposite directions. metasalmon 0.4.0 routed the descriptor's temporal values through `.ms_iso_character()` so "the JSON and the CSV cannot disagree about the same field" — true for `Date`, because `.ms_align_cols()` has already converted it to character, and **false for `POSIXct`**: measured, `.ms_iso_character()` yields `2024-01-31 10:00:00` while `readr::write_csv()` renders the same column `2024-01-31T10:00:00Z`. metasalmonpy has the mirror image: `package_io._clean()` emits the `T` form into `datapackage.json` and `to_csv` emits the space form into `metadata/dataset.csv`. So the two descriptors also disagree **with each other** for the same input | Found by metasalmonpy's 0.4.0 audit by driving both writers over the same instant, not by reading either; re-measured here 2026-08-24 (R 4.5.2, readr 2.x) before this row was written. Deliberately not fixed on either side: a unilateral change moves one descriptor's bytes to match neither its own CSV nor the other implementation, and **the correct lexical form for a metadata instant is a decision the SDP profile should make once for both sides**. `Date` already has such a ruling — [backlog #93](backlog.md) item 1, "Date only, per the measured rule", which is why `.ms_iso_date_columns()` deliberately does not touch `POSIXct`: readr's instant path was never broken and coercing it would change bytes twice over. Instants have no such ruling. The 0.4.0 port did fix the *adjacent* half in the mirror: every descriptor scalar presence test now renders and **trims** before deciding (`package_io._meta_scalar_present()`, mirroring `.ms_meta_scalar_present()`), which removes a whitespace-only `primary_key` writing `"primaryKey": []` — a key R cannot produce. **Retirement condition:** the SDP profile rules on the canonical lexical form for a metadata instant (backlog #93, alongside items 3 and 5 under [Q12](questions.md)); both sides adopt it in the same stream and this row records the ruling |
| 57 | Gap (open — **direction not yet ruled**) | `create_sdp()`'s deterministic prefill is **wider here**. metasalmon calls `apply_semantic_suggestions()` with no role restriction (`roles = NULL`), so a constraint or statistical modifier is also applied when the column's own name, label or description carries the evidence — gated by `.ms_measurement_supports_constraint_slot()` / `..._statistical_modifier_slot()` in `R/package-helpers.R`. metasalmonpy's `package_io` restricts it to `auto_roles = ["variable", "property", "entity", "unit"]` | Surfaced by metasalmonpy's 0.4.0 **documentation** audit rather than by a test, and that is the striking part: metasalmon 0.4.0 corrected its quickstart's "never auto-filled" claim as **wrong for R**, and the identical sentence in the mirror's `guides/faq.qmd` and `guides/parity.qmd` is **true for Python**. The prose matched the code on each side while the code diverged, so no reader of either was in a position to catch it. Not ported blind in either direction: auto-filling a constraint changes what `create_sdp()` writes into a dictionary without review, and the narrower default is defensible on exactly the ground R's quickstart used to assert. Read from code on both sides rather than measured end to end. **Direction not yet ruled.** **A ruling turns on how much a `REVIEW:`-marked prefill is worth against an unreviewed IRI in a slot the user did not ask about**: R's slot gates already demand the evidence come from the column's own text and the deterministic path marks what it filled, so the wider behaviour is arguably review-visible rather than silent — but constraint and statistical modifier are the two roles that change what a variable *is* (a mean weight and a maximum weight are different variables, which is why 0.3.0 moved the modifier into the dictionary), and a wrong one is harder for a reviewer to notice than a missing one. Raised as [Q16](questions.md). **Retirement condition:** Brett rules whether deterministic constraint and statistical-modifier prefill belongs on the `create_sdp()` path; the losing side changes `auto_roles` or R's slot gates, both sides pin the resulting dictionary in a test, and the docs on both sides are corrected in the same change |
| 58 | Gap (open) — documentation | metasalmon 0.4.0 added two vignettes (roadmap S11 slice 2) with no counterpart in metasalmonpy: **Migrating to SDP 0.3.0** (`vignettes/migrating-to-sdp-0-3-0.Rmd`) and **Tidy Data for Salmon Data Packages** (`vignettes/tidy-data-for-sdp.Rmd`). `migrate_sdp_methods()` is exported there and has no `reference/` page at all | The *behaviour* both vignettes document exists in the mirror — `migrate_sdp_methods()` with its stop-and-report cases, declared-primary-key uniqueness, the value-like column-name warning, the `pivot_longer` reshape (row 49) — so this is a **prose gap, not a capability gap**, and the 0.4.0 parity claim rests on behaviour. What the 0.4.0 port **did** fix there is the docs that were factually *wrong* rather than merely absent: `guides/semantic-review.qmd`, `guides/glossary.qmd`, `README.md` and `guides/parity.qmd` all still described a dictionary `method` slot that sdp-0.3.0 removed, the glossary named four I-ADOPT components instead of five, and `guides/parity.qmd` claimed 0.1.6 parity while listing EML export, KNB publication, SSSOM, decompositions and observation structures as "not yet in Python" — every one of which ships. **A register that points at a stale guide is worse than one that admits a missing guide**, which is why the wrong docs were the priority and this row is what keeps the missing ones from being forgotten. The checked-in generated `reference/*.qmd` remain stale (they need quartodoc and a `quarto` binary to regenerate); the source docstrings they were generated from are already correct. **Retirement condition:** the two guides are written in metasalmonpy — **or** the ecosystem decides that narrative guides live once, on the R side, and the mirror's docs link to them instead of duplicating them. Either answer retires the row; leaving it unanswered is what this row exists to prevent. The `reference/` half retires when a docs build runs in metasalmonpy's CI |

**Rows 54–58 were added 2026-08-24 by a hub-side pass, closing a one-sided
window the checker had been reporting since metasalmonpy's 0.4.0 parity audit.**
The audit had write access to metasalmonpy and not to this repository, so it
spent five numbers in `PARITY.md` and handed the twin text over in its report —
the expected transient state for a difference discovered on the mirror side, and
indistinguishable from drift to a check that reads numbers. `PARITY.md` says so
in a note that **should now be deleted by the next metasalmonpy change**: it
names its own retirement condition as "the moment the hub lands its twins", and
that moment is this commit. The same pass updated rows 11, 12, 16, 28 and 29,
each of which described metasalmonpy or metasalmon as it was before 0.4.0 — row
28's Difference cell had been contradicted by its own Why cell since the backlog
#85 consolidation landed, and row 29 asserted a per-module `_ACCEPTED_PROVENANCE`
the mirror no longer has. Row 32 needed nothing: PR #89 had already carried the
seven-surface guard convergence into it. **Rows 54 and 57 are recorded as
`direction not yet ruled`** and raised as [Q15 and Q16](questions.md); under the
2026-08-17 amendment an honest undecided is the correct state for a divergence
nobody has ruled on, and inventing a ruling to make a row look finished is the
failure this register is for.

**Rows 46 and 51 were updated 2026-08-24 by a hub-side rulings pass, and their
twins have not moved yet.** Brett ruled [Q4](questions.md) (the 173-row Fraser
coho example is the gold standard) and [Q14](questions.md) (one shared ownership
sentinel, breaking change accepted). Both rows now record a **ruling** rather
than a code change, so there is no metasalmonpy PR for them to ride — the same
structural cause as the numbering collisions below, in its content form. The
twin text for `PARITY.md` rows 46 and 51 was written out in that pass's pull
request for a metasalmonpy agent to apply. **Until it lands, the twin is a
version behind on content, and the checker below cannot see it** — it reads
numbers, and no number was spent. This is the one live disagreement; the
paragraph beneath is otherwise still true.

**No other disagreement with the twin is live** (2026-08-21). Three were,
and all three are discharged rather than carried forward: row 25's Kind (this
side moved to *Gap*, both halves now closed), row 32's text being a version
behind on the twin (rewritten there), and the hub-only validator row (now row
41, mirrored there). Earlier corrections the twin owed — rows 17, 18, 20, 22,
23, 26 and 27 — were all made on 2026-08-17. **Do not re-file any of them.**
The corrections travelling the other way this pass are rows 37 and 38, where
the twin described R as it was rather than as it is: one redactor since 0.2.5,
and a schema pin on the `sdp-0.3.0` tag rather than a branch.

**Rows 9, 27, 33, 38 and 46 were updated at S10 chunk A (2026-08-22)**,
against metasalmon `main` at `e02111a` and metasalmonpy at PR #14's merge: 9
rewritten for the landed registry removal, 27 and 38 marked converged by the
bundle-and-pin flip, 33 discharged by measurement, and 46's era-lag half
retired. No new number was spent. The twin carried the same five updates
first — they merged inside PR #14 itself — and this register followed in a
hub-side pass. No disagreement went live in the gap, but the gap existed, for
the same structural reason as the collisions below: a difference retired by a
Python-side PR has no hub PR to ride until someone opens one.

**Rows 21 and 22 were updated, and rows 47 and 48 added, at S10 chunks B and C
(2026-08-22).** Chunk C (metasalmonpy PR #15) converged row 22 and gave row
21's token its single authority, measured against metasalmon `main` @
`39818ce`; chunk B (PR #16) closed the crosswalk wiring gap as row 47,
measured against `main` @ `9d8f125`; row 48 records the metadata-placeholder
divergence chunk C found in passing, which closes at chunk D. As at chunk A,
the twins landed first — inside the two PRs — and this register followed in a
hub-side pass. The numbering held this time because the near-collision was
caught **before** merge rather than after: PR #15 originally claimed 47, ran
the checker pre-push, found chunk B had already committed its own 47 on an
unmerged branch, and moved to 48 on the committed-first precedent — the first
collision prevented rather than repaired, and the first evidence the checker
below earns its keep as a pre-commit step.

**Rows 37, 39, 41 and 48 were updated, and rows 49 and 50 added, at S10
chunks D, E and F (2026-08-22)** — metasalmonpy PRs #20 (D), #17 (E) and #19
(F). Chunk D closed row 41 and converged row 48, and claimed **49** for the
`melt`/`pivot_longer` pointer; chunk E claimed **50** for the env-prefix alias
window and appended row 39's cache-key consequence; chunk F rewrote row 37 in
place and claimed no number. As at chunks A–C, the twins landed first — inside
the three PRs — and this register followed in a hub-side pass.

**The numbering held again, and this time under genuine concurrency**, which is
the first evidence that the pre-commit checker plus an announced claim is enough
for parallel agents. D and E were in flight simultaneously and both ran
`scripts/check-parity-registers.py` before pushing: E claimed 50 and stated in
its PR body that *49 is deliberately left for the chunk-D agent, which announced
claiming from 49*; D verified the current highest before claiming rather than
assuming, found 49 free in both registers, and took it. F, stacked on E, claimed
nothing. Two rows, two PRs, no collision — against two collisions repaired and
one prevented in the preceding weeks.

**The lag this pass exposes is content, not numbers.** The checker reports a
one-sided *number*, which is how rows 49 and 50 surfaced; it cannot report that
rows 37, 41 and 48 had gone stale here while their twins were rewritten, because
it does not read content. Row 41 is the sharper case: it carried a clause
asserting that backlog #91 *"still says row 35"* when #91 had already been
corrected to 41, so this register was wrong about a third document while
correctly describing itself. Read that as the standing limit of the check below.

**Both numbering collisions had the same cause, and it now has a check.** Row
33 was row 29 until 2026-08-17: this register took 29 for the `sdp_methods.py`
whitespace gap while metasalmonpy's 0.1.8 work took it for the
reproducibility-manifest provenance row and 30–32 behind it. Brett resolved it
in metasalmonpy's favour — four committed rows against one — and the whitespace
row moved. Row 41 was row 35 until 2026-08-21, colliding the same way with
metasalmonpy's released `integer`-storage row, and was resolved on that
precedent: the committed, released row keeps the number. The cause is
structural rather than careless. Both registers are updated "in the same PR
that introduces the difference" — a rule written when differences were
introduced on the Python side. A divergence *ruled on in metasalmon* (row 32)
or *found while reading R* (rows 41–46) has no Python PR to ride, so it gets
numbered here without seeing what the mirror has already spent, and the twin
silently keeps stale text. Now that R is allowed to be the side that changes,
an R-side PR must carry the twin update too.

Run the number check before adding a row, and again before committing:

```sh
python3 scripts/check-parity-registers.py
```

It reads both registers, and fails on a number used twice in one file, a number
present in one register and not the other, or a row whose number cannot be
parsed. **What it does not check is content** — two rows can share a number and
describe different things, which is what a collision looks like the moment
before it is noticed, so it catches the shape of both collisions above but not
the wording drift rows 32, 37 and 38 carried. **It needs both checkouts, so it
cannot run in either repository's CI as things stand** (`knowledge/` is excluded
from `R CMD build`, and neither repository checks the other out). Where it runs
automatically — a hub workflow that checks out metasalmonpy, a metasalmonpy
workflow that checks out the hub, or neither — is **open**; until it is
decided, this is a local pre-commit step and `tests/testthat/test-parity-register-guard.R`
skips loudly rather than passing quietly. *Retires when:* the two registers
live in one repository, at which point a single test can read both directly.

Maintenance: a new deviation is added in the same PR that introduces it, in
both registers, in the same stream, at the same number.
