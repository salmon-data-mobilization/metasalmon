% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/review-console.R
\name{accept_suggestion}
\alias{accept_suggestion}
\alias{reject_suggestion}
\title{Decide a semantic review slot}
\usage{
accept_suggestion(
  review,
  column = NULL,
  role,
  rank = 1L,
  table = NULL,
  code_value = NULL,
  iri = NULL
)

reject_suggestion(
  review,
  column = NULL,
  role,
  table = NULL,
  code_value = NULL,
  reason = NULL
)
}
\arguments{
\item{review}{An \code{ms_semantic_review} object from \code{\link[=review_semantics]{review_semantics()}}.}

\item{column}{Column name of the slot. Omit it for a table-level slot
(\code{tables.csv} - \code{observation_unit_iri}), which has no column; pass \code{table}
instead. \code{review_semantics()} prints the right spelling either way.}

\item{role}{Semantic role of the slot (\code{"variable"}, \code{"property"},
\code{"entity"}, \code{"unit"}, \code{"constraint"}, \code{"statistical_modifier"}).}

\item{rank}{Rank of the candidate to accept, as printed in the shortlist.}

\item{table}{Table identifier; needed only when the column name appears in
more than one table.}

\item{code_value}{Code value; needed only for code-level slots.}

\item{iri}{Optional IRI to accept instead of a shortlisted candidate -- for
the case where the right term exists but retrieval did not surface it.}

\item{reason}{Optional free-text reason recorded with a rejection.}
}
\value{
The review, with the decision recorded.
}
\description{
\code{accept_suggestion()} records that a candidate is the right term for a slot;
\code{reject_suggestion()} records that none is, and clears the field. Both are
pipe-friendly -- they take a review and return it -- so a whole review is an
ordinary, re-runnable R script. Nothing is written until
\code{\link[=apply_sdp_semantics]{apply_sdp_semantics()}} is called.
}
\details{
These are the calls \code{\link[=review_semantics]{review_semantics()}} prints. Pasting the printed line is
the intended workflow, and it is what makes the decision reproducible: the
script is the audit trail.
}
\examples{
dict <- tibble::tibble(
  dataset_id = "demo-1", table_id = "spawners",
  column_name = "spawner_count", term_iri = NA_character_
)
attr(dict, "semantic_suggestions") <- tibble::tibble(
  dataset_id = "demo-1", table_id = "spawners",
  column_name = "spawner_count", code_value = NA_character_,
  dictionary_role = "variable", target_scope = "column",
  target_sdp_file = "column_dictionary.csv", target_sdp_field = "term_iri",
  target_row_key = "demo-1/spawners/spawner_count",
  label = "Spawner Abundance", iri = "https://w3id.org/smn/SpawnerAbundance",
  source = "smn", ontology = "smn",
  definition = "Mature salmon returning to spawn.", score = 4.9
)

review <- review_semantics(dict)
review <- accept_suggestion(review, "spawner_count", "variable", rank = 1)
review$decision
}
\seealso{
\code{\link[=review_semantics]{review_semantics()}}, \code{\link[=apply_sdp_semantics]{apply_sdp_semantics()}}
}
