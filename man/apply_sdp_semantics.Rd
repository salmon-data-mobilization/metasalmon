% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metadata-write.R
\name{apply_sdp_semantics}
\alias{apply_sdp_semantics}
\title{Write semantic review decisions into a package}
\usage{
apply_sdp_semantics(path, review, quiet = FALSE)
}
\arguments{
\item{path}{Path to the package directory.}

\item{review}{An \code{ms_semantic_review} object carrying decisions.}

\item{quiet}{Logical; suppress the summary message.}
}
\value{
The package path, invisibly.
}
\description{
Applies the decisions recorded by \code{\link[=accept_suggestion]{accept_suggestion()}} and
\code{\link[=reject_suggestion]{reject_suggestion()}} to a written Salmon Data Package. Accepted IRIs are
written with the \verb{REVIEW:} prefix stripped; rejected slots are cleared;
every other field, and every data CSV byte, is left untouched.
}
\details{
Safe to re-run: applying the same review twice produces identical bytes.
Only fields carrying a decision are written, so undecided slots keep their
\verb{REVIEW:} markers.

The metadata CSVs, \code{semantic_suggestions.csv} and \code{datapackage.json} are
installed as one transactional set -- the descriptor duplicates the
dictionary's IRI fields, and a half-applied edit would leave the package
quietly self-inconsistent.
}
\examples{
\dontrun{
pkg <- create_sdp(resources, dataset_id = "demo-1")
review <- review_semantics(pkg)
review <- accept_suggestion(review, "spawner_count", "variable", rank = 1)
apply_sdp_semantics(pkg, review)
validate_salmon_datapackage(pkg, require_iris = TRUE)
}
}
\seealso{
\code{\link[=review_semantics]{review_semantics()}}, \code{\link[=accept_suggestion]{accept_suggestion()}}
}
