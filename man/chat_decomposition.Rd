% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chat-decomposition.R
\name{chat_decomposition}
\alias{chat_decomposition}
\title{Interactive decomposition review for measurement variables}
\usage{
chat_decomposition(
  dict,
  column_name,
  df = NULL,
  table_id = NULL,
  dataset_id = NULL,
  suggestions = NULL,
  sources = c("smn", "gcdfo", "ols", "nvs"),
  search_fn = find_terms,
  max_per_role = 5L,
  session_id = NULL,
  session_root = NULL,
  round_size = 3L,
  chat_provider = NULL,
  chat_model = NULL,
  chat_api_key = NULL,
  chat_base_url = NULL,
  chat_timeout_seconds = 60,
  chat_request_fn = NULL,
  commands = NULL,
  input_fn = readline,
  output_fn = NULL
)
}
\arguments{
\item{dict}{A dictionary tibble containing the target measurement column.}

\item{column_name}{Column name to review.}

\item{df}{Optional data frame or named list of data frames, forwarded to
\code{\link[=suggest_semantics]{suggest_semantics()}} when \code{suggestions} are not supplied.}

\item{table_id, dataset_id}{Optional keys used to disambiguate \code{column_name}
when \code{dict} contains multiple matching rows.}

\item{suggestions}{Optional \code{semantic_suggestions}-like tibble. When omitted,
\code{\link[=suggest_semantics]{suggest_semantics()}} is called and the results are filtered down to the
selected measurement column's \code{term_iri} variable shortlist.}

\item{sources, search_fn, max_per_role}{Retrieval controls used only when
\code{suggestions} are not supplied.}

\item{session_id}{Optional existing session id to resume.}

\item{session_root}{Optional directory for persisted sessions. Defaults to
a package state directory under \code{\link[tools:userdir]{tools::R_user_dir()}}.}

\item{round_size}{Maximum number of grouped questions to ask in each round.
Default is \code{3}.}

\item{chat_provider, chat_model, chat_api_key, chat_base_url}{Optional chat
adapter settings used for shortlist review. When omitted, the function uses
a deterministic fallback over the retrieved shortlist.}

\item{chat_timeout_seconds}{Timeout for the optional chat adapter call.}

\item{chat_request_fn}{Advanced/test hook overriding the package-local chat
adapter request function.}

\item{commands}{Optional scripted replies/actions, mainly for testing.
When supplied, they take precedence over \code{input_fn}.}

\item{input_fn}{Function used to read console input. Defaults to
\code{\link[base:readline]{base::readline()}}.}

\item{output_fn}{Function used to print console output. Defaults to a simple
\code{cat()} wrapper.}
}
\value{
A list with \code{session_id}, \code{session_dir}, \code{approval_status},
\code{proposed_patch}, \code{approved_patch}, \code{state}, and \code{transcript}.
}
\description{
Starts or resumes a lightweight R-console decomposition session for one
measurement column. The session keeps structured state separately from the
raw transcript, asks grouped decomposition questions in small rounds, and
ends in an explicit preview/approve or new-term decision.
}
\details{
The current first slice is intentionally narrow: it focuses on measurement
\code{term_iri} review and reuses existing \code{\link[=suggest_semantics]{suggest_semantics()}} retrieval
machinery when a shortlist is not supplied directly.
}
\examples{
\dontrun{
dict <- tibble::tibble(
  dataset_id = "demo",
  table_id = "main",
  column_name = "spawner_count",
  column_label = "Spawner count",
  column_description = "Estimated natural-origin spawner abundance",
  column_role = "measurement",
  value_type = "integer",
  unit_label = "count",
  unit_iri = NA_character_,
  term_iri = NA_character_,
  property_iri = NA_character_,
  entity_iri = NA_character_,
  constraint_iri = NA_character_,
  method_iri = NA_character_
)

chat_decomposition(dict, column_name = "spawner_count")
}
}
