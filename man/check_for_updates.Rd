% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/version-check.R
\name{check_for_updates}
\alias{check_for_updates}
\title{Check whether a newer metasalmon release is available}
\usage{
check_for_updates(
  repo = "dfo-pacific-science/metasalmon",
  current = utils::packageVersion("metasalmon"),
  timeout = 2,
  quiet = FALSE
)
}
\arguments{
\item{repo}{GitHub repository in \code{"owner/name"} form. Defaults to the
canonical \code{metasalmon} repository.}

\item{current}{Installed version to compare. Defaults to
\code{utils::packageVersion("metasalmon")}.}

\item{timeout}{Number of seconds to wait for GitHub before giving up.
Defaults to \code{2}.}

\item{quiet}{Logical; if \code{TRUE}, suppresses cli messages and only returns the
result object.}
}
\value{
Invisibly returns a list with class \code{"metasalmon_update_check"}.
Elements include \code{status}, \code{current_version}, \code{latest_version},
\code{update_available}, \code{repo}, \code{release_tag}, \code{release_url},
\code{install_command}, and \code{message}.
}
\description{
Compares the installed package version with the latest GitHub release for
\code{dfo-pacific-science/metasalmon}.
}
\details{
This function performs a network request only when you call it. \code{metasalmon}
does not check for updates automatically when the package is attached.
\code{create_sdp()} can call it optionally when \code{check_updates = TRUE}.
}
\examples{
\dontrun{
check_for_updates()
}
}
