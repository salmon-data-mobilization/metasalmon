% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/package-helpers.R
\name{create_sdp}
\alias{create_sdp}
\title{Create a Salmon Data Package directly from raw tables}
\usage{
create_sdp(
  resources,
  path = NULL,
  dataset_id = "dataset-1",
  table_id = "table_1",
  guess_types = TRUE,
  seed_semantics = TRUE,
  semantic_sources = c("smn", "gcdfo", "ols", "nvs"),
  semantic_max_per_role = 1,
  seed_verbose = TRUE,
  seed_codes = NULL,
  seed_table_meta = TRUE,
  seed_dataset_meta = TRUE,
  semantic_code_scope = c("factor", "all", "none"),
  llm_assess = FALSE,
  llm_provider = c("openai", "openrouter", "openai_compatible", "chapi"),
  llm_model = NULL,
  llm_api_key = NULL,
  llm_base_url = NULL,
  llm_reasoning_effort = NULL,
  llm_top_n = 5L,
  llm_context_files = NULL,
  llm_context_text = NULL,
  llm_timeout_seconds = 60,
  llm_request_fn = NULL,
  check_updates = interactive(),
  format = "csv",
  overwrite = FALSE,
  include_edh_xml = FALSE,
  ...
)
}
\arguments{
\item{resources}{Either a named list of data frames (one per resource table)
or a single data frame (converted internally to a one-table list).}

\item{path}{Character; directory path where package will be written. If
omitted, defaults to \verb{file.path(getwd(), paste0(<dataset_id>-sdp))} using
a filesystem-safe dataset id slug.}

\item{dataset_id}{Dataset identifier applied to all inferred metadata rows.}

\item{table_id}{Fallback table identifier when \code{resources} is a single data frame.}

\item{guess_types}{Logical; if \code{TRUE} (default), infer \code{value_type} for each
dictionary column.}

\item{seed_semantics}{Logical; if \code{TRUE} (default), seed semantic suggestions
during inference.}

\item{semantic_sources}{Vector of vocabulary sources passed to
\code{suggest_semantics()}.}

\item{semantic_max_per_role}{Maximum number of suggestions retained per
I-ADOPT role.}

\item{seed_verbose}{Logical; if TRUE, emit progress messages while seeding
semantic suggestions.}

\item{seed_codes}{Optional \code{codes.csv}-style seed metadata.}

\item{seed_table_meta}{Optional \code{tables.csv}-style seed metadata. Use
\code{TRUE} (default) to infer starter table metadata from \code{resources}.}

\item{seed_dataset_meta}{Optional \code{dataset.csv}-style seed metadata. Use
\code{TRUE} (default) to infer starter dataset metadata from \code{resources}.}

\item{semantic_code_scope}{Character string controlling which \code{codes.csv}
rows are sent through \code{suggest_semantics()} during one-shot seeding.
\code{"factor"} (default) analyzes codes sourced from factor columns and
low-cardinality character columns in the original data frame(s); \code{"all"}
analyzes all inferred or supplied code rows; \code{"none"} skips code-level
semantic suggestions.}

\item{llm_assess}{Logical; if \code{TRUE}, run the optional LLM shortlist
assessment inside \code{suggest_semantics()}.}

\item{llm_provider}{LLM provider preset forwarded to \code{suggest_semantics()}.}

\item{llm_model}{Optional LLM model identifier forwarded to
\code{suggest_semantics()}.}

\item{llm_api_key}{Optional API key override forwarded to
\code{suggest_semantics()}.}

\item{llm_base_url}{Optional OpenAI-compatible base URL forwarded to
\code{suggest_semantics()}.}

\item{llm_reasoning_effort}{Optional reasoning-effort hint forwarded to
\code{suggest_semantics()} when using the OpenAI provider.}

\item{llm_top_n}{Maximum number of retrieved candidates sent to the LLM per
target.}

\item{llm_context_files}{Optional character vector of local context file
paths forwarded to \code{suggest_semantics()} when \code{llm_assess = TRUE}. Pass
file paths, not parsed data frames, XML documents, or R Markdown objects.
See \code{suggest_semantics()} for supported file types, including HTML, DOCX,
\code{.R}, \code{.Rmd}, \code{.qmd}, PDF, and Excel context files.}

\item{llm_context_text}{Optional inline context snippets forwarded to
\code{suggest_semantics()}.}

\item{llm_timeout_seconds}{Timeout for each LLM request in seconds.}

\item{llm_request_fn}{Advanced/test hook overriding the low-level
OpenAI-compatible request function.}

\item{check_updates}{Logical; if \code{TRUE}, run a short, non-fatal
\code{\link[=check_for_updates]{check_for_updates()}} call after writing the package and mention newer
releases only when one is available. Defaults to \code{interactive()}.}

\item{format}{Character; resource format: \code{"csv"} (default, only format supported)}

\item{overwrite}{Logical; if \code{FALSE} (default), errors if path exists. If
\code{TRUE}, replacement is only allowed for empty directories or directories
previously written by \code{metasalmon}.}

\item{include_edh_xml}{Logical; when \code{TRUE}, writes an HNAP-aware EDH XML
metadata file to \code{metadata/metadata-edh-hnap.xml} using
\code{edh_build_hnap_xml()}. The default is \code{FALSE}.}

\item{...}{Deprecated legacy EDH arguments accepted for backwards
compatibility: \code{edh_profile}, \code{EDH_Profile}, and \code{EDH_profile} all enable
EDH XML export and must be \code{"dfo_edh_hnap"} when supplied.
\code{edh_xml_path} is ignored with a warning because XML now always writes to
the default metadata path. Any other extra arguments error.}
}
\value{
Invisibly returns the package path.
}
\description{
Primary one-shot wrapper: infer dictionary/table metadata/codes/dataset
metadata from raw data tables and immediately write a review-ready Salmon
Data Package.
}
\details{
This one-shot helper creates a review-ready package by default:
semantic suggestions are seeded and the top-ranked column-level suggestions
are auto-applied only into missing dictionary IRI fields. Table-level
observation-unit suggestions stay enabled, and \code{create_sdp()} can
auto-apply them into missing \code{tables.csv$observation_unit_iri} values when
the suggestion still looks lexically compatible with the available table
context (prefer \code{observation_unit}/\code{description}, otherwise fall back to
\code{table_label}/\code{table_id}); compatible suggestions can also backfill
\code{tables.csv$observation_unit} labels when missing. To reduce review
noise conservatively, code-level suggestions default to factor and
low-cardinality character source columns only; set
\code{semantic_code_scope = "all"} to broaden that or \code{"none"} to disable it.
The package root contains \code{README-review.txt},
\code{semantic_suggestions.csv} (when available), \code{datapackage.json},
\verb{metadata/}, and \verb{data/}. Review the prefilled values already written into
\code{metadata/tables.csv} and \code{metadata/column_dictionary.csv} first; use
\code{semantic_suggestions.csv} as a fallback shortlist when you want more
context or a better match. To keep that review file usable,
\code{semantic_suggestions.csv} trims code-level suggestions that do not have
enough human-readable context to review safely. When \code{llm_assess = TRUE},
the same review file also carries \verb{llm_*} columns so the shortlisted LLM
judgment stays explicit and reviewable. Any auto-applied column/table IRI
draft is written back into the metadata CSVs as a \verb{REVIEW:}-prefixed value
for manual confirmation there. Required-field review placeholders are also
inserted into the inferred metadata files. In
interactive use, \code{create_sdp()} can also mention an available package update;
set \code{check_updates = FALSE} to skip that network check. The package bundles
two Fraser coho examples: \code{nuseds-fraser-coho-sample.csv} (30 rows across
1996-2024) for the quickest demo, and \verb{nuseds-fraser-coho-2023-2024.csv}
(173 rows from the official Open Government Canada Fraser and BC Interior
workbook) for a fuller multi-year example. The bundled
\code{system.file("extdata", "example-data-README.md", package = "metasalmon")}
note points to the upstream record/resource URLs, licensing, and the
repository \verb{data-raw/} script used to derive the fuller example.
}
\examples{
\dontrun{
data_path <- system.file("extdata", "nuseds-fraser-coho-sample.csv", package = "metasalmon")
fraser_coho <- readr::read_csv(data_path, show_col_types = FALSE)

pkg <- create_sdp(
  fraser_coho,
  dataset_id = "fraser-coho-2024",
  table_id = "escapement",
  overwrite = FALSE
)
}
}
