% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/term-deduplication.R
\name{deduplicate_proposed_terms}
\alias{deduplicate_proposed_terms}
\title{Deduplicate proposed ontology terms}
\usage{
deduplicate_proposed_terms(proposed_terms, warn_threshold = 30L)
}
\arguments{
\item{proposed_terms}{A data frame with columns: term_label, term_definition,
term_type, suggested_parent_iri. Typically loaded from a proposed-terms CSV.}

\item{warn_threshold}{Integer. If the input has more than this many rows,
issue a warning about potential over-engineering. Default is 30.}
}
\value{
A tibble with deduplicated terms and additional columns:
\itemize{
\item \code{is_base_term}: TRUE if this is the canonical base term for a pattern
\item \code{needs_age_facet}: TRUE if age variants should use constraint_iri
\item \code{needs_phase_facet}: TRUE if phase variants should use constraint_iri
\item \code{collapsed_from}: Count of how many variants were collapsed into this term
\item \code{dedup_notes}: Explanation of deduplication applied
}
}
\description{
Applies I-ADOPT compositional deduplication to a proposed-terms dataframe.
This prevents term proliferation by:
\enumerate{
\item Removing duplicates across tables (same term_label)
\item Collapsing age-stratified variants (X Age 1..7) into one base term
\item Collapsing phase-stratified variants (Ocean/Terminal/Mainstem X) into one base term
\item Identifying terms that should use constraint_iri instead of new term_iri
}
}
\details{
The function returns a deduplicated dataframe with added columns for facet handling.

\strong{Target ratio}: For a dictionary with N measurement columns, expect ~N/10 to N/5
distinct base terms, NOT N terms. If the output still has >30 rows, consider
further manual review.

\strong{Anti-patterns detected}:
\itemize{
\item "Spawners Age 1", "Spawners Age 2", ... patterns -> collapsed to "SpawnerCount"
\item Duplicate term_labels across different tables -> deduplicated
\item Phase-stratified variants (Ocean X, Terminal X) -> collapsed to base term
}
}
\examples{
\dontrun{
# Load raw proposed terms
proposed <- readr::read_csv("work/semantics/proposed_terms.csv")

# Deduplicate
deduped <- deduplicate_proposed_terms(proposed)

# Review collapsed terms
deduped |> dplyr::filter(collapsed_from > 1)

# Write cleaned output
readr::write_csv(deduped, "work/semantics/proposed_terms_deduped.csv")
}
}
