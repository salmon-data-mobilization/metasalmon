% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/term-request-helpers.R
\name{detect_semantic_term_gaps}
\alias{detect_semantic_term_gaps}
\title{Detect missing semantic terms that are not covered by SMN}
\usage{
detect_semantic_term_gaps(
  dict = NULL,
  suggestions = NULL,
  include_target_scopes = c("column", "code", "table", "dataset"),
  include_dictionary_roles = NULL,
  min_score = NA_real_
)
}
\arguments{
\item{dict}{A dictionary tibble. Used only when \code{suggestions} is \code{NULL}.}

\item{suggestions}{Optional semantic suggestion table. If omitted, this function
uses \code{attr(dict, "semantic_suggestions")}.}

\item{include_target_scopes}{Target scopes to inspect. Defaults to all supported
scopes.}

\item{include_dictionary_roles}{Optional vector of dictionary roles to restrict
the gap scan (for example \code{c("variable", "property", "entity")}).}

\item{min_score}{Optional minimum score filter. Rows with score below this value
are ignored when score is available.}
}
\value{
A tibble with one row per target that has no SMN match.
Key columns:
\itemize{
\item \code{dataset_id}, \code{table_id}, \code{column_name}, \code{target_scope},
\code{target_sdp_file}, \code{target_sdp_field}, \code{target_row_key}, \code{dictionary_role};
\item \code{search_query} text used for lookup;
\item \code{top_non_smn_source}, \code{top_non_smn_label}, \code{top_non_smn_iri},
\code{top_non_smn_score};
\item \code{non_smn_sources}, \code{candidate_count}, \code{placement_recommendation},
\code{placement_confidence}, \code{placement_rationale}.
}
}
\description{
Given semantic suggestions (typically attached to a dictionary as
\code{semantic_suggestions}), this function summarizes candidate fields that appear to
need ontology support but do not have a direct \code{smn} match.
}
\details{
It is designed to support a practical workflow:
\enumerate{
\item generate semantic suggestions with \code{suggest_semantics()};
\item detect unresolved gaps with \code{detect_semantic_term_gaps()};
\item render request payloads with \code{render_ontology_term_request()};
\item optionally submit issues with \code{submit_term_request_issues()}.
}
}
\examples{
suggestions <- tibble::tibble(
  dataset_id = c("d1", "d1"),
  table_id = c("t1", "t1"),
  column_name = c("run_id", "run_id"),
  code_value = NA_character_,
  column_label = c("Run ID", "Run ID"),
  column_description = "Run identifier from local monitoring pipeline",
  dictionary_role = c("variable", "variable"),
  target_scope = c("column", "column"),
  target_sdp_file = c("column_dictionary.csv", "column_dictionary.csv"),
  target_sdp_field = c("term_iri", "term_iri"),
  target_row_key = c("run_id", "run_id"),
  search_query = c("run_id", "run_id"),
  label = c("Run ID", "Run ID"),
  iri = c(NA_character_, NA_character_),
  source = c("gbif", "worms"),
  ontology = c("gbif", "worms"),
  match_type = c("label", "label"),
  definition = NA_character_,
  score = c(0.9, 0.85)
)
gaps <- detect_semantic_term_gaps(
  suggestions = suggestions,
  include_dictionary_roles = "variable"
)
gaps


}
\seealso{
\code{\link[=render_ontology_term_request]{render_ontology_term_request()}}, \code{\link[=submit_term_request_issues]{submit_term_request_issues()}},
\code{\link[=suggest_semantics]{suggest_semantics()}}
}
