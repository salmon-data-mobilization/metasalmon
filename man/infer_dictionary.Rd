% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dictionary-helpers.R
\name{infer_dictionary}
\alias{infer_dictionary}
\title{Infer a starter dictionary from a data frame}
\usage{
infer_dictionary(
  df,
  guess_types = TRUE,
  dataset_id = "dataset-1",
  table_id = "table_1",
  seed_semantics = FALSE,
  semantic_sources = c("smn", "gcdfo", "ols", "nvs"),
  semantic_max_per_role = 1,
  seed_verbose = TRUE,
  seed_codes = NULL,
  seed_table_meta = NULL,
  seed_dataset_meta = NULL,
  llm_assess = FALSE,
  llm_provider = c("openai", "openrouter", "openai_compatible", "chapi"),
  llm_model = NULL,
  llm_api_key = NULL,
  llm_base_url = NULL,
  llm_reasoning_effort = NULL,
  llm_top_n = 5L,
  llm_context_files = NULL,
  llm_context_text = NULL,
  llm_timeout_seconds = 60,
  llm_request_fn = NULL
)
}
\arguments{
\item{df}{A data frame or tibble to analyze. Or, when provided as a named list of data frames,
\code{infer_dictionary()} infers each table and returns a combined dictionary.}

\item{guess_types}{Logical; if \code{TRUE} (default), infer value types from data.}

\item{dataset_id}{Character; dataset identifier (default: "dataset-1").}

\item{table_id}{Character; table identifier (default: "table_1").}

\item{seed_semantics}{Logical; if \code{TRUE}, run \code{suggest_semantics()} and attach
the resulting \code{semantic_suggestions} attribute to the returned dictionary.}

\item{semantic_sources}{Character vector of vocabulary sources passed to
\code{suggest_semantics()} when \code{seed_semantics = TRUE}. Default: \code{c("smn", "gcdfo", "ols", "nvs")}.}

\item{semantic_max_per_role}{Maximum number of suggestions retained per I-ADOPT
role when seeding suggestions. Default: \code{1}.}

\item{seed_verbose}{Logical; if TRUE, print a short progress message while
seeding semantic suggestions.}

\item{seed_codes}{Optional \code{codes.csv}-style tibble forwarded to
\code{suggest_semantics()} when \code{seed_semantics = TRUE}.}

\item{seed_table_meta}{Optional \code{tables.csv}-style tibble forwarded to
\code{suggest_semantics()} when \code{seed_semantics = TRUE}.}

\item{seed_dataset_meta}{Optional \code{dataset.csv}-style tibble forwarded to
\code{suggest_semantics()} when \code{seed_semantics = TRUE}.}

\item{llm_assess}{Logical; if \code{TRUE}, run the optional LLM shortlist
assessment inside \code{suggest_semantics()}.}

\item{llm_provider}{LLM provider preset forwarded to \code{suggest_semantics()}.}

\item{llm_model}{Optional LLM model identifier forwarded to
\code{suggest_semantics()}.}

\item{llm_api_key}{Optional API key override forwarded to
\code{suggest_semantics()}.}

\item{llm_base_url}{Optional OpenAI-compatible base URL forwarded to
\code{suggest_semantics()}.}

\item{llm_reasoning_effort}{Optional reasoning-effort hint forwarded to
\code{suggest_semantics()} when using the OpenAI provider.}

\item{llm_top_n}{Maximum number of retrieved candidates sent to the LLM per
target.}

\item{llm_context_files}{Optional local context files forwarded to
\code{suggest_semantics()}. See that function for supported file types,
including HTML, DOCX, \code{.R}, \code{.Rmd}, \code{.qmd}, PDF, and Excel context files.}

\item{llm_context_text}{Optional inline context snippets forwarded to
\code{suggest_semantics()}.}

\item{llm_timeout_seconds}{Timeout for each LLM request in seconds.}

\item{llm_request_fn}{Advanced/test hook overriding the low-level
OpenAI-compatible request function.}
}
\value{
A tibble with dictionary schema columns in canonical Salmon Data
Package order: \code{dataset_id}, \code{table_id}, \code{column_name}, \code{column_label},
\code{column_description}, \code{term_iri}, \code{property_iri}, \code{entity_iri},
\code{constraint_iri}, \code{method_iri}, \code{unit_label}, \code{unit_iri}, \code{term_type},
\code{value_type}, \code{column_role}, \code{required}.
}
\description{
Proposes a starter dictionary (column dictionary schema) from raw data by
guessing column types, roles, and basic metadata.
}
\examples{
\dontrun{
df <- data.frame(
  species = c("Coho", "Chinook"),
  count = c(100, 200),
  date = as.Date(c("2024-01-01", "2024-01-02"))
)
dict <- infer_dictionary(df)

# Optional: seed semantic suggestions from vocabulary services
# (SMN is queried first; GCDFO is a distinct DFO-specific source)
dict <- infer_dictionary(
  df,
  seed_semantics = TRUE,
  semantic_sources = c("smn", "gcdfo", "ols", "nvs")
)
suggestions <- attr(dict, "semantic_suggestions")
}
}
