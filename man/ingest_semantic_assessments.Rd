% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/semantic-review-ingest.R
\name{ingest_semantic_assessments}
\alias{ingest_semantic_assessments}
\title{Ingest a harness's semantic assessments}
\usage{
ingest_semantic_assessments(
  x,
  assessments = NULL,
  packet = NULL,
  packet_id = NULL,
  provider = NULL,
  model = NULL,
  search_fn = find_terms,
  review_dir = NULL,
  quiet = FALSE
)
}
\arguments{
\item{x}{The package directory or the in-memory dictionary the packet was
built from.}

\item{assessments}{The harness's file: a CSV path or a data frame. Defaults
to \verb{review/semantic-assessments-pass-<n>.csv} for the packet's pass.}

\item{packet}{The packet the file answers: a path or a parsed packet.
Defaults to the latest pass in \verb{review/}.}

\item{packet_id}{Optional. The \code{packet_id} this ingest expects; a
different packet is refused with code \code{packet_mismatch}.}

\item{provider, model}{Optional overrides for the \code{llm_provider} and
\code{llm_model} columns of every row.}

\item{search_fn}{The search function used for a retry. Defaults to
\code{\link[=find_terms]{find_terms()}}.}

\item{review_dir}{Where the session lives. Defaults to \verb{review/} under the
package directory; required for in-memory input.}

\item{quiet}{Logical. Suppress the summary message.}
}
\value{
Invisibly, a list: \code{status} (\code{"complete"} or \code{"awaiting_pass_2"}),
\code{pass}, \code{packet_id}, \code{next_packet} (the continuation packet's path, or
\code{NULL}), \code{assessments} (the record, with the validator findings as its
\code{semantic_validator_findings} attribute), \code{findings}, \code{suggestions},
\code{targets}, \code{dictionary} (with \code{semantic_suggestions}, \code{semantic_targets}
and \code{semantic_llm_assessments} attached, so
\code{\link[=detect_semantic_term_gaps]{detect_semantic_term_gaps()}} and the accessors work unchanged), and
\code{summary} (counts of decisions, errors, downgrades, escalations and
retries).
}
\description{
The second half of the review-packet contract: reads the CSV a harness
wrote against a packet from \code{\link[=write_semantic_review_packet]{write_semantic_review_packet()}}, in the
frozen 30-column assessment row, and turns it into the package's record.
It refuses a selection whose IRI the packet did not offer (recorded as an
error row, never applied), runs the retry bookkeeping, escalates a final
\code{reject_shortlist} to \code{request_new_term} so the ontology gap is surfaced,
runs the existing bundle validators, merges the result into the
suggestions and persists it under \verb{review/}, so that
\code{\link[=semantic_llm_assessments]{semantic_llm_assessments()}} returns it for a package path.
}
\details{
\strong{A retry is a second harness pass.} A \code{retry_search} with a usable query
is retrieved here through \code{search_fn}, and when the search widens the
shortlist a continuation packet (\verb{review/semantic-review-packet-pass-2.json})
is written holding the widened shortlist; nothing from that target is
merged or escalated until the harness has answered it. Calling this again
after the harness answers the second packet completes the session. There
is no third pass.

\strong{This never calls a model}, and it reaches the network only through
\code{search_fn}, for a retry. It never touches the metadata CSVs: applying a
choice stays \code{\link[=review_semantics]{review_semantics()}} -> \code{\link[=accept_suggestion]{accept_suggestion()}} ->
\code{\link[=apply_sdp_semantics]{apply_sdp_semantics()}}.

A file-level problem aborts the ingest and writes nothing, with a stable
code carried as the condition's \code{code} field and class
\verb{metasalmon_semantic_review_<code>}: \code{packet_version}, \code{packet_integrity}
(the packet no longer matches its \code{packet_id}), \code{packet_mismatch},
\code{header}, \code{unknown_target}, \code{duplicate_target}, \code{provenance} and
\code{no_pass_2}. A row-level problem makes that row an error row or downgrades
it with a note, and processing continues.
}
\seealso{
\code{\link[=write_semantic_review_packet]{write_semantic_review_packet()}}, \code{\link[=semantic_llm_assessments]{semantic_llm_assessments()}}
}
