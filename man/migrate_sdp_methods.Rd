% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sdp-methods.R
\name{migrate_sdp_methods}
\alias{migrate_sdp_methods}
\title{Migrate an sdp-0.2.0 package's method metadata to sdp-0.3.0}
\usage{
migrate_sdp_methods(path, dry_run = FALSE)
}
\arguments{
\item{path}{Existing Salmon Data Package directory.}

\item{dry_run}{Logical; when \code{TRUE}, report what would change without
touching any file.}
}
\value{
Invisibly, a list report: \code{tables} (the table-level method
placements applied), \code{dropped_review} (unresolved \verb{REVIEW:} bindings
dropped), and \code{registry} (the legacy registry rows, for relocating
labels/descriptions to the shared vocabulary and citations to
\code{protocol_citation}).
}
\description{
sdp-0.3.0 removed the \code{metadata/methods.csv} registry and the
column-dictionary \code{method_iri} field. This tool relocates what can be
relocated mechanically and \strong{stops and reports} on anything that needs a
judgement call, rather than guessing:
}
\details{
\itemize{
\item A \code{method_iri} shared by every bound measurement column of a table
becomes that table's \code{tables.csv$method_iri}.
\item Columns of one table bound to \emph{different} methods stop the migration:
you decide whether to split the table, cite a protocol, or move the
method into the data as a code column (see the methods section of the
SDP specification).
\item \verb{REVIEW:}-marked values are dropped, not migrated, and reported.
\item Registry labels and descriptions are reported, not relocated — they
belong in the shared vocabulary. A registry \code{method_version} or
\code{citation} is offered in the report as \code{protocol_citation} material.
}

The rewrite is atomic: either every affected metadata file is updated and
\code{metadata/methods.csv} removed, or nothing changes.
}
