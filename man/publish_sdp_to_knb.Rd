% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/knb-publication.R
\name{publish_sdp_to_knb}
\alias{publish_sdp_to_knb}
\title{Publish a reviewed Salmon Data Package to KNB}
\usage{
publish_sdp_to_knb(
  path,
  eml_path = NULL,
  public = NULL,
  manifest_path = NULL,
  dry_run = TRUE,
  confirm = interactive(),
  revision_manifest = NULL,
  representation = c("archive", "expanded"),
  overwrite = FALSE,
  knb_environment = NULL
)
}
\arguments{
\item{path}{Directory containing the reviewed Salmon Data Package.}

\item{eml_path}{Validated EML output path. Defaults to \code{metadata/eml.xml}
inside \code{path}; it is rebuilt deterministically before planning.}

\item{public}{Explicit logical access decision. \code{TRUE} requests anonymous
read access for every DataONE object and explicitly requests three DataONE
preservation replicas. \code{FALSE} creates a restricted KNB-only production
deposit, explicitly disables peer replication, and requires authenticated
exact-byte/SystemMetadata verification plus anonymous denial for every
object and zero anonymous catalog matches. The replication policy is part
of the exact reviewed manifest and is verified on remote readback. There
is no implicit access default.}

\item{manifest_path}{Recovery manifest path inside \code{path}. Defaults to
\code{publication/knb-manifest.json}.}

\item{dry_run}{Logical; when \code{TRUE} (the default), write only local plan
artifacts and never construct a DataONE adapter or read credentials.}

\item{confirm}{Explicit approval of the pre-existing exact dry-run plan and
live mutation. Its interactive default can never authorize a live call:
live mode requires that the argument was supplied and is exactly \code{TRUE}.}

\item{revision_manifest}{Optional path to the verified manifest for the
preceding KNB version. Supplying it plans an immutable DataONE revision:
the reviewed sidecar must contain a new \code{publication.revision_key}, the
metadata series stays stable, and the new EML/resource-map objects
obsolete their predecessors. Access cannot change in the same operation.}

\item{representation}{Publication representation. \code{"expanded"} publishes
the closed SDP artifact inventory as individually named objects whose
relative paths can reconstruct the package. \code{"archive"} (the compatibility
default) publishes one deterministic ZIP in addition to each source data
object. Neither mode scans arbitrary package files.}

\item{overwrite}{Logical; rebuild derived publication artifacts (the SDP
archive and \code{eml.xml}) when they already exist with different bytes. The
default \code{FALSE} refuses, which protects an artifact you may already have
published; pass \code{TRUE} to re-plan after correcting an input such as
\code{eml-mapping.yml}. Live publication is gated separately by \code{confirm}.}

\item{knb_environment}{Deposit environment: \code{"test"} or \code{"production"}.
Selects the DataONE network, member node, coordinating node, resolver,
Solr catalog endpoint, credential option, and default artifact paths
together -- these never vary independently. A dry run defaults to
\code{"test"}; a live call has no default and must state the environment
explicitly alongside \code{confirm = TRUE}. Matched exactly: there is no
partial matching, no custom endpoint, and no fallback between
environments. Test deposits use the separate \code{dataone_test_token}
credential, mint identifiers that cannot collide with production ones,
request zero replicas, and write their artifacts under
\verb{publication/test/} so the reviewed production \code{metadata/eml.xml} is
never replaced. They are a rehearsal: non-durable, not promotable, and
ineligible for a DOI.}
}
\value{
Invisibly returns publication status, the resolved
\code{knb_environment}, identifiers, normalized
manifest and resource-map paths, the optional SDP-archive path, the
representation, and the manifest.
}
\description{
Plans an immutable DataONE package containing the original data resources
named by \code{tables.csv}, one validated EML 2.2.0 metadata object, and a
deterministic OAI-ORE resource map. The \code{expanded} representation publishes
each allowlisted canonical SDP artifact as a named, EML-documented DataONE
object and records its package-relative path with PROV-O \code{atLocation}; it
does not create a ZIP or duplicate the source table. The compatibility
\code{archive} representation publishes one deterministic SDP ZIP. The default
operation is a credential-free, network-free dry run. Live
publication requires a pre-existing exact dry-run manifest and an explicitly
supplied \code{confirm = TRUE} approving that plan. Redistribution authority is
recorded separately in the reviewed EML sidecar.
}
\details{
DataONE credentials are read only inside the live adapter. Use a short-lived
DataONE JWT through the runtime option belonging to the selected
environment -- \code{dataone_token} for production, \code{dataone_test_token} for the
test node. They are separate credentials, and supplying one never satisfies
the other. Credentials are never accepted as function arguments or written
to the manifest.

There are two deposit environments, selected with \code{knb_environment}. The
KNB Test Node (\code{urn:node:mnTestKNB}, DataONE \code{STAGING}) is the rehearsal
target and the default for a dry run; production KNB (\code{urn:node:KNB},
DataONE \code{PROD}) is the durable one and must always be named explicitly. The
golden path develops a package against the test node first and posts to
production once it looks good there.

Within production, a live restricted deposit is the review mechanism; KNB
does not expose a separate server-side draft state. The persistent object
identifiers remain even while access is private. This function does not call KNB's
separate Publish action and never mints a DOI. If a reviewed dataset should
receive a DOI, request it for the science-metadata version through KNB when
making that version public. The DOI identifies the metadata version, not each
raw or supplementary object.

Revisions must be built in a fresh versioned SDP directory. Keep the prior
package and its verified manifest unchanged, write the corrected SDP to a
new directory with a new \code{publication.revision_key}, and choose a new local
manifest path there. If KNB's separate Publish action later creates a
DOI-bearing metadata version, that KNB-created version is not automatically
imported into a metasalmon manifest; do not plan another metasalmon revision
from the older pre-DOI manifest.

Publication currently materializes object bytes in memory for exact hashing
and readback. It is intended for modest tabular SDPs; large packages should
be tested in a dry run and may require a future streaming adapter.
}
\examples{
\dontrun{
publish_sdp_to_knb(
  "path/to/reviewed-sdp",
  public = FALSE,
  dry_run = TRUE
)
}
}
