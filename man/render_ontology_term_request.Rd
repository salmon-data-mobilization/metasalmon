% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/term-request-helpers.R
\name{render_ontology_term_request}
\alias{render_ontology_term_request}
\title{Render GitHub-ready ontology term request payloads}
\usage{
render_ontology_term_request(
  gaps,
  scope = c("auto", "smn", "profile"),
  ask = interactive(),
  profile_name = NULL,
  scope_overrides = NULL,
  issue_labels = NULL,
  term_request_template = .term_request_default_template,
  ontology_repo = "salmon-data-mobilization/salmon-domain-ontology"
)
}
\arguments{
\item{gaps}{Output from \code{detect_semantic_term_gaps()}.}

\item{scope}{One of \code{"auto"}, \code{"smn"}, or \code{"profile"}.
\itemize{
\item \code{"auto"}: honor \code{placement_recommendation} and ask for uncertainty
\item \code{"smn"}: route all requests to shared SMN
\item \code{"profile"}: route all requests to a profile
}}

\item{ask}{If \code{TRUE}, unresolved rows are asked interactively.}

\item{profile_name}{If routing to profiles, provide a default profile name.}

\item{scope_overrides}{Optional per-row scope overrides (\code{"smn"}, \code{"profile"},
\code{"skip"}). Useful in non-interactive pipelines.}

\item{issue_labels}{Optional labels to include on created GitHub issues.}

\item{term_request_template}{URL for the target issue template.}

\item{ontology_repo}{Repository slug to target when submitting issues.}
}
\value{
A tibble with one row per rendered request payload. Rows with
\code{request_scope == "skip"} are retained and can be filtered before
submission.
}
\description{
Convert gap candidates into request payload rows (title/body) suitable for
creating GitHub issues against the Salmon Domain Ontology repository by
default.
}
\details{
For interactive workflows this function can prompt users row-by-row for whether a
gap should be requested as a shared SMN term, a profile-specific term, or skipped.
}
\examples{
gap <- dplyr::tibble(
  dataset_id = "d1",
  table_id = "t1",
  column_name = "run_id",
  code_value = NA_character_,
  target_scope = "column",
  target_sdp_file = "column_dictionary.csv",
  target_sdp_field = "term_iri",
  target_row_key = "run_id",
  dictionary_role = "variable",
  search_query = "run id",
  column_label = "Run ID",
  column_description = "Dataset-specific run identifier",
  top_non_smn_source = "gbif",
  top_non_smn_label = "Run event id",
  top_non_smn_iri = NA_character_,
  top_non_smn_ontology = NA_character_,
  top_non_smn_match_type = "label",
  top_non_smn_score = 0.9,
  candidate_count = 2,
  non_smn_sources = "gbif, worms",
  placement_recommendation = "profile",
  placement_confidence = 0.82,
  placement_rationale = "Contains internal identifier patterns."
)

render_ontology_term_request(
  gap,
  scope = "auto",
  ask = FALSE,
  profile_name = "pacific-monitoring"
)

}
\seealso{
\code{\link[=detect_semantic_term_gaps]{detect_semantic_term_gaps()}}, \code{\link[=submit_term_request_issues]{submit_term_request_issues()}},
\code{\link[=validate_semantics]{validate_semantics()}}
}
