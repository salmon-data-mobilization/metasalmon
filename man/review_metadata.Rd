% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sdp-field-setters.R
\name{review_metadata}
\alias{review_metadata}
\title{Report the metadata a package still needs, with the call that fills it}
\usage{
review_metadata(path)
}
\arguments{
\item{path}{Path to the package directory.}
}
\value{
An \code{ms_metadata_review} tibble subclass, one row per unfilled field,
with the package path attached as the \code{review_path} attribute. Empty when
nothing is outstanding.
}
\description{
Lists every field that still blocks
\code{validate_salmon_datapackage(path, require_iris = TRUE)}, and prints the
exact \code{\link[=set_sdp_dataset]{set_sdp_dataset()}} / \code{\link[=set_sdp_table]{set_sdp_table()}} / \code{\link[=set_sdp_column]{set_sdp_column()}} /
\code{\link[=set_sdp_code]{set_sdp_code()}} call that fills it. Replace the \verb{<...>} placeholder in the
printed call with the real value and paste it -- the paste is the audit
trail, just as it is for \code{\link[=accept_suggestion]{accept_suggestion()}}.
}
\details{
This is the companion to \code{\link[=review_semantics]{review_semantics()}}, and it sees something that
review structurally cannot: \strong{a slot with no candidates at all}.
\code{review_semantics()} builds its queue from retrieved suggestions, so a field
nothing was found for never appears there. \code{review_metadata()} builds its
list from the package's own required-field rules, so an empty shortlist and
a full one look the same to it.

What it reports:
\itemize{
\item unresolved \verb{MISSING DESCRIPTION:} / \verb{MISSING METADATA:} / \verb{REVIEW REQUIRED:}
placeholders in any metadata field;
\item schema-required fields (\code{constraints.required}) that are blank;
\item measurement columns missing \code{term_iri}, \code{property_iri}, \code{entity_iri} or
\code{unit_iri};
\item \code{tables.csv} rows with a blank \code{observation_unit_iri}.
}

It never contacts a network or an LLM.
}
\examples{
\dontrun{
pkg <- create_sdp(resources, dataset_id = "demo-1")
review_metadata(pkg)
set_sdp_dataset(pkg, creator = "Fisheries and Oceans Canada")
}
}
\seealso{
\code{\link[=set_sdp_dataset]{set_sdp_dataset()}}, \code{\link[=review_semantics]{review_semantics()}},
\code{\link[=validate_salmon_datapackage]{validate_salmon_datapackage()}}
}
