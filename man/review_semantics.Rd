% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/review-console.R
\name{review_semantics}
\alias{review_semantics}
\title{Review semantic suggestions in the console}
\usage{
review_semantics(
  x,
  include_filled = FALSE,
  max_candidates = 5L,
  columns = NULL
)
}
\arguments{
\item{x}{A written package path, a dictionary carrying the
\code{semantic_suggestions} attribute, or the artifact list returned by
\code{infer_salmon_datapackage_artifacts()}.}

\item{include_filled}{Logical; if \code{TRUE}, also queue slots that already hold
a final (non-\verb{REVIEW:}) IRI. Defaults to \code{FALSE}.}

\item{max_candidates}{Maximum candidates shown per slot. \code{Inf} shows all.}

\item{columns}{Optional character vector restricting the queue to these
column names.}
}
\value{
An \code{ms_semantic_review} tibble subclass, one row per candidate, in
the order the ranked producer emitted them. Carries the package path (when
there is one) as the \code{review_path} attribute.
}
\description{
Builds a re-runnable review queue from suggestions that already exist. One
entry per unfilled semantic slot, each with its ranked shortlist and the
exact \code{\link[=accept_suggestion]{accept_suggestion()}} call that decides it -- printing that call is the
feature: paste it into a script and the decision becomes reproducible,
which the spreadsheet workflow this replaces never was.
}
\details{
\strong{This never contacts a network or an LLM.} It reads the
\code{semantic_suggestions} attribute (or \code{semantic_suggestions.csv}) that
\code{suggest_semantics()} / \code{create_sdp()} already produced. When those
suggestions carry LLM review -- only possible if they were generated with
\code{llm_assess = TRUE} -- this surfaces it; it never generates it.
}
\examples{
dict <- tibble::tibble(
  dataset_id = "demo-1",
  table_id = "spawners",
  column_name = "spawner_count",
  term_iri = NA_character_
)
attr(dict, "semantic_suggestions") <- tibble::tibble(
  dataset_id = "demo-1",
  table_id = "spawners",
  column_name = "spawner_count",
  code_value = NA_character_,
  dictionary_role = "variable",
  target_scope = "column",
  target_sdp_file = "column_dictionary.csv",
  target_sdp_field = "term_iri",
  target_row_key = "demo-1/spawners/spawner_count",
  label = "Spawner Abundance",
  iri = "https://w3id.org/smn/SpawnerAbundance",
  source = "smn",
  ontology = "smn",
  definition = "Mature salmon returning to spawn.",
  score = 4.9
)

review <- review_semantics(dict)
review
}
\seealso{
\code{\link[=accept_suggestion]{accept_suggestion()}}, \code{\link[=reject_suggestion]{reject_suggestion()}}, \code{\link[=apply_sdp_semantics]{apply_sdp_semantics()}}
}
