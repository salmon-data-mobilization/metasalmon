% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/semantics-helpers.R
\name{semantic_suggestions}
\alias{semantic_suggestions}
\title{Semantic suggestions attached to a dictionary or package}
\usage{
semantic_suggestions(x)
}
\arguments{
\item{x}{A dictionary carrying the attribute, a list in the shape returned
by \code{infer_salmon_datapackage_artifacts()} / \code{create_sdp()}-style inference,
or a length-one character path to a written Salmon Data Package.}
}
\value{
A tibble of candidate rows, or \code{NULL} when none are attached. The
\code{NULL} is deliberate: these accessors replace \code{attr(x, "semantic_suggestions")}
and must answer \code{is.null()} the same way it did.
}
\description{
\code{suggest_semantics()} and \code{infer_dictionary()} attach their candidate
shortlist to the returned dictionary as the \code{semantic_suggestions}
attribute, and \code{create_sdp()} writes the same table to
\code{semantic_suggestions.csv} inside the package. This is the supported way to
read either, so review tooling never has to reach for \code{attr()}.
}
\examples{
dict <- tibble::tibble(column_name = "spawner_count")
semantic_suggestions(dict)

attr(dict, "semantic_suggestions") <- tibble::tibble(
  column_name = "spawner_count",
  dictionary_role = "variable",
  iri = "https://w3id.org/smn/SpawnerAbundance"
)
semantic_suggestions(dict)
}
