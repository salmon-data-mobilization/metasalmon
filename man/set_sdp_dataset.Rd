% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sdp-field-setters.R
\name{set_sdp_dataset}
\alias{set_sdp_dataset}
\alias{set_sdp_table}
\alias{set_sdp_column}
\alias{set_sdp_code}
\title{Fill in a package's free-text metadata}
\usage{
set_sdp_dataset(
  path,
  ...,
  title = NULL,
  description = NULL,
  creator = NULL,
  contact_name = NULL,
  contact_email = NULL,
  contact_org = NULL,
  license = NULL,
  quiet = FALSE
)

set_sdp_table(
  path,
  table,
  ...,
  table_label = NULL,
  description = NULL,
  observation_unit = NULL,
  observation_unit_iri = NULL,
  quiet = FALSE
)

set_sdp_column(
  path,
  column,
  ...,
  table = NULL,
  column_label = NULL,
  column_description = NULL,
  unit_label = NULL,
  term_iri = NULL,
  property_iri = NULL,
  entity_iri = NULL,
  unit_iri = NULL,
  quiet = FALSE
)

set_sdp_code(
  path,
  column,
  code_value,
  ...,
  table = NULL,
  code_label = NULL,
  code_description = NULL,
  term_iri = NULL,
  vocabulary_iri = NULL,
  quiet = FALSE
)
}
\arguments{
\item{path}{Path to the package directory.}

\item{...}{Any other field the SDP schema declares for that file, as
\code{name = value}.}

\item{title, description, creator, contact_name, contact_email, contact_org, license}{\code{dataset.csv} fields.}

\item{quiet}{Logical; suppress the confirmation message.}

\item{table}{Table identifier. For \code{\link[=set_sdp_column]{set_sdp_column()}} and \code{\link[=set_sdp_code]{set_sdp_code()}}
it is needed only when the column name appears in more than one table;
\code{\link[=review_metadata]{review_metadata()}} always prints it.}

\item{table_label, observation_unit, observation_unit_iri}{\code{tables.csv} fields.}

\item{column}{Column name of the dictionary or codes row.}

\item{column_label, column_description, unit_label}{\code{column_dictionary.csv}
free-text fields.}

\item{term_iri, property_iri, entity_iri, unit_iri}{\code{column_dictionary.csv}
(and, for \code{term_iri}, \code{codes.csv}) semantic IRIs. Use these for a slot
retrieval found no candidate for; use \code{\link[=accept_suggestion]{accept_suggestion()}} when there is
a shortlist to choose from.}

\item{code_value}{The code value identifying a \code{codes.csv} row.}

\item{code_label, code_description, vocabulary_iri}{\code{codes.csv} fields.}
}
\value{
The package path, invisibly.
}
\description{
The scriptable replacement for opening \verb{metadata/*.csv} in a spreadsheet.
Each setter addresses one row and writes the named fields into it, keeping
\code{datapackage.json} in step in the same transactional write. Every field the
SDP schema declares for that file can be set: the ones most often unfilled
are named arguments for discoverability, and the rest are passed through
\code{...} and checked against the schema, so a misspelling is an error rather
than a silent no-op.
}
\details{
These are the calls \code{\link[=review_metadata]{review_metadata()}} prints. Replace the \verb{<...>}
placeholder with the real value and paste -- pasting one unedited is refused
with a message saying so, because a package whose \code{creator} reads \verb{<add creator, team, or originating program>} would pass strict validation while
saying nothing.

Pass \code{NA} to clear a field deliberately; a blank string is refused as
ambiguous.
}
\examples{
\dontrun{
pkg <- create_sdp(resources, dataset_id = "demo-1")
review_metadata(pkg)
set_sdp_dataset(
  pkg,
  creator = "Fisheries and Oceans Canada",
  contact_name = "Data Unit",
  contact_email = "data@example.org",
  license = "CC-BY-4.0"
)
set_sdp_table(pkg, "spawners", description = "One row per stream and year.")
set_sdp_column(pkg, "spawner_count", column_description = "Spawners counted.")
}
}
\seealso{
\code{\link[=review_metadata]{review_metadata()}}, \code{\link[=accept_suggestion]{accept_suggestion()}},
\code{\link[=validate_salmon_datapackage]{validate_salmon_datapackage()}}
}
