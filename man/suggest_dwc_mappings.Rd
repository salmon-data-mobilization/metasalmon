% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dwc-dp-helpers.R
\name{suggest_dwc_mappings}
\alias{suggest_dwc_mappings}
\title{Suggest Darwin Core Data Package mappings for dictionary columns}
\usage{
suggest_dwc_mappings(dict, max_per_column = 3)
}
\arguments{
\item{dict}{A dictionary tibble with \code{column_name}, and optionally
\code{column_label} and \code{column_description}.}

\item{max_per_column}{Maximum number of mapping suggestions per column.}
}
\value{
The dictionary tibble with a \code{dwc_mappings} attribute containing
suggestions (columns: \code{column_name}, \code{table_id}, \code{field_name}, \code{field_label},
\code{term_iri}, \code{match_score}, \code{match_basis}).
}
\description{
Uses DwC Conceptual Model + DwC-DP table schemas (cached locally) to suggest
likely table/field mappings for column dictionary entries, and returns the
associated Darwin Core property IRIs for review.
}
\examples{
\dontrun{
dict <- infer_dictionary(mtcars)
dict <- suggest_dwc_mappings(dict)
attr(dict, "dwc_mappings") \%>\% head()
}
}
