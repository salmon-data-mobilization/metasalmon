% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eml-export.R
\name{write_eml_from_sdp}
\alias{write_eml_from_sdp}
\title{Write reviewed EML 2.2.0 metadata from a Salmon Data Package}
\usage{
write_eml_from_sdp(
  path,
  output_path = NULL,
  mapping_path = NULL,
  overwrite = FALSE,
  supplementary_objects = NULL,
  require_revision_key = FALSE,
  knb_environment = "production"
)
}
\arguments{
\item{path}{Directory containing a Salmon Data Package.}

\item{output_path}{Output XML path. Defaults to \code{metadata/eml.xml} inside
\code{path}.}

\item{mapping_path}{Reviewed YAML mapping. Defaults to
\code{metadata/eml-mapping.yml} inside \code{path}.}

\item{overwrite}{Logical; replace a different existing output only when
\code{TRUE}. An identical existing file is treated as an idempotent success.}

\item{supplementary_objects}{Optional data frame describing canonical SDP
archives or expanded artifacts to expose as EML \code{otherEntity} elements.
Required columns are
\code{path}, \code{pid}, \code{format_id}, \code{checksum}, \code{object_name}, \code{entity_name}, and
\code{description}; optional \code{size}, when supplied, must match the file.
\code{entity_type} may distinguish an expanded artifact from an archive.
Objects use lowercase SHA-256 checksums and safe relative \code{object_name}
paths; only \code{application/zip} objects receive \code{compressionMethod = zip}.
\code{publish_sdp_to_knb()} supplies this plan automatically; ordinary
standalone EML export leaves the argument \code{NULL}.}

\item{require_revision_key}{Logical; when \code{TRUE}, require a reviewed
\code{publication.revision_key} in the EML mapping sidecar. The key creates a
new deterministic metadata package ID without changing the series ID.}

\item{knb_environment}{KNB deposit environment the document is written for:
\code{"production"} (the default) or \code{"test"}. It selects the DataONE member
node whose object URLs the EML distribution elements point at, and is
folded into the deterministic package and series identifiers so a test
identifier can never be mistaken for a production one. Accepted exactly;
there is no partial matching and no custom endpoint. The default is
\code{"production"} because this function writes the package's reviewed
\code{metadata/eml.xml}; \code{publish_sdp_to_knb()} supplies its own environment
and writes a test document to a separate path, leaving the reviewed
production record untouched.}
}
\value{
Invisibly returns a list containing the XML text, normalized output
path, EML version, metadata package ID, stable series ID, validation
result, revision key, deterministic data and supplementary-object plans,
the complete method registry (\code{methods}), and the subset asserted in EML
(\code{used_methods}).
}
\description{
Builds deterministic EML 2.2.0 XML from a strictly valid Salmon Data
Package and an explicit EML mapping sidecar. The sidecar is required because
EML concepts such as measurement scale, structured parties, dataset-level
method narrative, and rights cannot be inferred defensibly from the
canonical SDP tables. Method and protocol placements are read from the
sdp-0.3.0 fields — \code{tables.csv} (\code{method_iri}, \code{protocol_iri},
\code{protocol_citation}) and \code{dataset.csv} (\code{protocol_iri},
\code{protocol_citation}) — and emitted as method steps; row-varying procedures
actually used by the data are resolved through \code{codes.csv} \code{term_iri} and
listed in a dedicated method step. A legacy \code{metadata/methods.csv} registry
is an error: run \code{\link[=migrate_sdp_methods]{migrate_sdp_methods()}} first.
}
\details{
Measurement attributes receive exactly two semantic annotations in the
initial profile. Both reviewed OWL measurement-datum classes and SKOS
compound-variable concepts use Dublin Core Terms \code{subject} with the reviewed
\code{term_iri}, followed by QUDT \code{hasUnit} using the reviewed \code{unit_iri}. The
broader topic predicate is intentional: an OWL class is not necessarily an
OBOE \code{MeasurementType}, and schema-valid EML must not silently assert that
unsupported range. The exporter deliberately does not project incomplete
I-ADOPT roles or procedure annotations into EML.
}
\examples{
\dontrun{
write_eml_from_sdp("path/to/reviewed-sdp")
}
}
