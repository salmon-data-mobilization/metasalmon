% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/package-helpers.R
\name{write_salmon_datapackage}
\alias{write_salmon_datapackage}
\title{Write a Salmon Data Package from preassembled metadata}
\usage{
write_salmon_datapackage(
  resources,
  dataset_meta,
  table_meta,
  dict,
  codes = NULL,
  path,
  format = "csv",
  overwrite = FALSE,
  write_datapackage = TRUE,
  prune = FALSE
)
}
\arguments{
\item{resources}{Named list of data frames/tibbles (one per resource)}

\item{dataset_meta}{Tibble with dataset-level metadata (one row)}

\item{table_meta}{Tibble with table-level metadata (one row per table)}

\item{dict}{Dictionary tibble with column definitions}

\item{codes}{Optional tibble with code lists}

\item{path}{Character; directory path where package will be written}

\item{format}{Character; resource format: \code{"csv"} (default, only format supported)}

\item{overwrite}{Logical; if \code{FALSE} (default), errors when \code{path} is a
directory that already holds something. An existing but \emph{completely empty}
directory is written into without \code{overwrite} — there is nothing there to
destroy — while a dot-file, a stale \code{.metasalmon-package} sentinel, or an
empty \verb{data/} subdirectory all count as content and still require it. If
\code{TRUE}, the package is updated in place — see \code{prune}. Replacement is only
allowed for directories previously written by \code{metasalmon}.}

\item{write_datapackage}{Logical; if \code{TRUE} (default), write a root
\code{datapackage.json} descriptor declaring the SDP Frictionless profile after
package validation passes. Use \code{FALSE} for draft authoring output.}

\item{prune}{Logical; if \code{FALSE} (default), only files this writer owns are
replaced: the \verb{metadata/} SDP CSVs, the \verb{data/} resources declared in
\code{tables.csv} (including any a previous write declared and this one does
not), \code{datapackage.json}, and the ownership sentinel. Everything else is
preserved — reviewed SSSOM mappings and measurement decompositions under
\verb{metadata/semantic/}, EML and EDH XML, \code{eml-mapping.yml}, review notes, and
\verb{publication/} artifacts. If \code{TRUE}, every entry in the directory is
deleted first (the pre-0.2.0 behaviour). Requires \code{overwrite = TRUE}.}
}
\value{
Invisibly returns the path to the created package
}
\description{
Advanced/manual writer for cases where you already have the canonical Salmon
Data Package (SDP) metadata tables assembled. It writes the SDP CSV metadata
files under \verb{metadata/} (\code{dataset.csv}, \code{tables.csv},
\code{column_dictionary.csv}, and optional \code{codes.csv}) plus the data resource
files themselves under \verb{data/}. For interoperability with Frictionless-style
tooling, the function also emits a derived \code{datapackage.json} descriptor at
the package root.
}
\details{
The SDP CSV files remain the canonical package metadata. \code{datapackage.json}
is a convenience export, not the source of truth.

The write is transactional over the files it owns: every output is fully
rendered before anything on disk is deleted or replaced, and a failure while
installing the rendered files restores the previous package. An error partway
through therefore leaves an existing package intact rather than destroyed.
The one exception is \code{prune = TRUE}, which deletes files the writer does not
own and so cannot restore: the wipe still happens only after every
input-dependent step has succeeded, but a filesystem failure (disk full,
permissions) between the wipe and the install can lose the deleted files.
}
\examples{
\dontrun{
# Create a simple package
resources <- list(main_table = mtcars)
dataset_meta <- tibble::tibble(
  dataset_id = "test-1",
  title = "Test Dataset",
  description = "A test dataset"
)
table_meta <- tibble::tibble(
  dataset_id = "test-1",
  table_id = "main_table",
  file_name = "data/main_table.csv",
  table_label = "Main Table"
)
dict <- infer_dictionary(mtcars, dataset_id = "test-1", table_id = "main_table")
write_salmon_datapackage(
  resources, dataset_meta, table_meta, dict,
  path = tempdir()
)
}
}
