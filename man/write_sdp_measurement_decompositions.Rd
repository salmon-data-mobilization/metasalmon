% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/measurement-decompositions.R
\name{write_sdp_measurement_decompositions}
\alias{write_sdp_measurement_decompositions}
\title{Write ordered measurement decompositions into a Salmon Data Package}
\usage{
write_sdp_measurement_decompositions(
  path,
  decompositions = NULL,
  overwrite = FALSE
)
}
\arguments{
\item{path}{Existing Salmon Data Package directory.}

\item{decompositions}{\code{NULL} or a non-empty data frame matching the ordered
measurement-decomposition schema.}

\item{overwrite}{Logical; replace artifacts managed by this writer when
\code{TRUE}.}
}
\value{
The manifest path, invisibly, or \code{NULL} when \code{decompositions} is
\code{NULL}.
}
\description{
Writes explicit decomposition rows to
\code{metadata/semantic/measurement-decompositions.csv} and binds the exact
deterministic bytes to \code{measurement-decompositions.json}. The writer never
infers components, splits labels, or converts decompositions into SSSOM
mappings. Supplying \code{decompositions = NULL} is an explicit no-op.
}
\details{
Each row has the following closed schema:
\itemize{
\item \code{dataset_id}, \code{table_id}, and \code{column_name} bind the decomposition to one
measurement row in \code{metadata/column_dictionary.csv}.
\item \code{measurement_concept_iri} must exactly equal that dictionary row's
\code{term_iri}.
\item \code{component_order} is a positive, contiguous, per-measurement sequence.
\item \code{component_role} is one of \code{property}, \code{entity}, \code{constraint},
\code{statistical_modifier}, or \code{unit}; repeated roles are allowed.
\item \code{component_status} is \code{matched} or \code{gap}. A matched row requires an
absolute \code{component_iri}. A gap requires a blank \code{component_iri} plus a
non-empty \code{component_label} and \code{rationale}.
\item \code{component_relation} and \code{related_component_order} are normally blank.
\code{value_of_dimension} links a matched constraint value to an earlier
matched constraint dimension in the same measurement; for example, an
age-1 class can target its freshwater-age dimension without flattening the
two constraints.
\item \code{source}, \code{source_version}, \code{source_url}, and \code{provenance} identify the
pinned source and review evidence. \code{component_label} and \code{rationale}
preserve caller-supplied text; they are never tokenized or inferred.
}

Every non-empty dictionary \code{property_iri}, \code{entity_iri}, \code{constraint_iri},
\code{statistical_modifier_iri}, and \code{unit_iri} must appear as a matched component of the same
role. Semicolon-separated dictionary constraints are checked separately.
Additional same-role components and explicit gaps stay only in this
artifact, leaving the frozen SDP dictionary columns unchanged. This is an
ordered SDP semantic profile informed by I-ADOPT roles, not a claim of
native I-ADOPT conformance.
}
