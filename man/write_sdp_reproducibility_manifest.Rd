% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reproducibility-manifest.R
\name{write_sdp_reproducibility_manifest}
\alias{write_sdp_reproducibility_manifest}
\title{Write a closed reproducibility manifest into a Salmon Data Package}
\usage{
write_sdp_reproducibility_manifest(path, artifacts, overwrite = FALSE)
}
\arguments{
\item{path}{Existing Salmon Data Package directory.}

\item{artifacts}{Non-empty data frame with exactly \code{path}, \code{role}, and
\code{media_type} columns. Paths are package-relative. Roles are
\code{reviewed_semantic_selections}, \code{workflow}, \code{provenance}, or \code{source} and
must agree with the canonical directory layout.}

\item{overwrite}{Logical; replace an existing managed manifest when \code{TRUE}.}
}
\value{
The manifest path, invisibly.
}
\description{
Binds an explicit inventory of reviewed semantic selections, workflow
records, provenance, and source records to exact paths, media types, byte
sizes, and SHA-256 hashes in \code{reproducibility/manifest.json}. The writer does
not discover files. Validation requires the declarations to be closed over
the actual reproducibility tree, which prevents accidental publication of
local notes or editor backups.
}
