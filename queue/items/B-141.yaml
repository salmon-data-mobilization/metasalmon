id: B-141
kind: defect
title: The pkgdown build script publishes HUB.md and the pull-request template and rewrites the site under a newer toolchain
state: icebox
claimable: true
repo: metasalmon
venue: claude-code
stream: S11
severity: P3
blocked_by: []
legacy: ''
evidence: scripts/build-pkgdown.R
retires_when: 'scripts/build-pkgdown.R excludes HUB.md and .github/PULL_REQUEST_TEMPLATE.md from the rendered site the way it already excludes AGENTS.md and CLAUDE.md, and a full run on a clean main changes only the pages whose sources changed; measured on 2026-09-12 during #111, a full run rewrote 107 tracked files under docs/ from pkgdown 2.2.1 and pandoc 3.1.3 against a site built with 2.2.0 and 3.8.3, and produced docs/HUB.html and docs/PULL_REQUEST_TEMPLATE.html, so the script pins or documents the toolchain the checked-in site expects.'
