id: B-257
kind: defect
title: 'metasalmonpy''s read_sdp_csv() strips a trailing newline inside a quoted field where readr keeps it, and neither parity register records the difference'
state: icebox
claimable: false
repo: metasalmonpy
venue: claude-code
severity: P4
blocked_by: []
legacy: ''
evidence: knowledge/backlog.md
retires_when: 'The two readers agree about a quoted field whose value ends in a newline, or Brett has ruled that they differ on purpose. WHICH SIDE IS RIGHT IS OPEN, and this item does not decide it: AGENTS.md decides the direction of a divergence per divergence, and adding or amending a parity-register row is class 5 in HUB.md''s Which pull requests need Brett. If the ruling is that metasalmonpy moves, this item retires when read_sdp_csv() returns such a field as readr does, pinned by a test in tests/test_sdp_csv_reader.py that reads one CSV whose quoted field ends in a newline, demonstrated RED on the reader as it stands. Any other ruling replaces this item rather than retiring it, because what that ruling asks for is work outside this repository: if metasalmon moves, a metasalmon item; if the difference stays, a pair of register items, one per repository and each naming the other, that list this mismatch beside the tab before an opening quote that PARITY.md row 23 already names.'
