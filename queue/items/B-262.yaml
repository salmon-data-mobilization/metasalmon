id: B-262
kind: defect
title: 'The publication vignette''s step 9 says strict validation passes only when a package is publication-ready, which step 10 contradicts, and two closure comments keep the reviewer gloss B-192 removed from the vignette'
state: icebox
claimable: false
repo: metasalmon
venue: claude-code
stream: S11
severity: P3
blocked_by: []
legacy: ''
evidence: knowledge/backlog.md
retires_when: 'Step 9 of vignettes/post-review-package-publication.Rmd no longer says strict validation "should pass only when the package is actually publication-ready": it agrees with step 10, which says strict validation is not the publication gate, and with the B-192 run''s finding that strict validation passes on a package write_eml_from_sdp() and publish_sdp_to_knb() refuse. And the header comment of R/semantic-closure.R and the comment above .ms_closure_target_context() in the same file no longer say a code-resolved procedure is one "which no reviewer ever selected as a slot": they say, as the vignette since B-192 and the function''s roxygen section The two canonical sets are not one set already do, that the review-target set has no slot for it. A documentation and comment change; no behaviour changes.'
